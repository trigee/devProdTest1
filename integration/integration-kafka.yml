category: Messaging
commonfields:
  id: kafka
  version: -1
configuration:
- display: 'The Kafka brokers to connect to, as a comma separated list, in the form:
    ip:port,ip2:port2 '
  name: Address
  required: true
  supportedModules: []
  type: 0
- defaultvalue: Demisto
  display: Client ID
  name: ClientID
  required: true
  supportedModules: []
  type: 0
- display: CA certificate of Kafka server (type pem)
  name: cafile
  required: false
  supportedModules: []
  type: 12
- display: Client certificate (type pem)
  name: clientcert
  required: false
  supportedModules: []
  type: 12
- display: Client certificate key (type pem)
  name: clientkey
  required: false
  supportedModules: []
  type: 14
- display: Do not validate server certificate (insecure)
  name: insecure
  required: false
  supportedModules: []
  type: 8
- display: Import messges as incidents
  name: isFetch
  required: false
  supportedModules: []
  type: 8
- display: Alert type
  name: alertType
  required: false
  supportedModules: []
  type: 13
- defaultvalue: "0"
  display: Import from offset
  name: offset
  required: false
  supportedModules: []
  type: 0
- display: Import from topic
  name: topic
  required: false
  supportedModules: []
  type: 0
- defaultvalue: "0"
  display: Import from partition
  name: partition
  required: false
  supportedModules: []
  type: 0
contentitemexportablefields: {}
deprecated: true
description: Deprecated. Use the Kafka v2 integration instead.
detaileddescription: |-
  By default, the Kafka instance uses a non SSL connection. Turnning on the insecure flag, or providing any certificate will switch to SSL connection.
  To generate the files needed for SSL, you will need to create a Kafka client keystore. You can use the script at:
  [https://github.com/confluentinc/confluent-platform-security-tools/blob/master/kafka-generate-ssl.sh](https://github.com/confluentinc/confluent-platform-security-tools/blob/master/kafka-generate-ssl.sh)
  Then run the command **"keytool -list -rfc -keystore kafka.client.keystore.jks"** to verify that there are 3 entries in that keystore.
  run "keytool -exportcert -alias localhost -keystore kafka.client.keystore.jks -rfc -file certificate.pem" to export the certificate.pem, this is the Client certificate.
  Next, you need to extract the private.key. You can do this using the following commands:
  **keytool -v -importkeystore -srckeystore kafka.client.keystore.jks -srcalias localhost -destkeystore cert_and_key.p12 -deststoretype PKCS12 openssl pkcs12 -in cert_and_key.p12 -nocerts -nodes**

  This wil print the stdout the content of the private key, copy the lines (inclusive) between —–BEGIN PRIVATE KEY—– and —–END PRIVATE KEY—– to a new key.pem file. This is the Client certificate key nano key.pem.

  Finally, you need to extract the CA certificate:
  **keytool -exportcert -alias CARoot -keystore kafka.client.keystore.jks -rfc -file CARoot.pem**
display: Kafka
icon: kafka.png
ispasswordprotected: true
name: kafka
restrictioncenter: {}
script:
  commands:
  - arguments:
    - default: true
      description: commands.server.kafka.arguments.topic
      name: topic
      required: true
      supportedModules: []
    - description: commands.server.kafka.arguments.value
      name: value
      required: true
      supportedModules: []
    - description: commands.server.kafka.arguments.key
      name: partitioning-key
      supportedModules: []
    deprecated: true
    description: commands.server.kafka.publish.msg
    name: kafka-publish-msg
    supportedModules: []
  - arguments: []
    deprecated: true
    description: commands.server.kafka.print.topics.msg
    name: kafka-print-topics
    supportedModules: []
  - arguments:
    - default: true
      description: commands.server.kafka.arguments.topic
      name: topic
      required: true
      supportedModules: []
    - description: commands.server.kafka.arguments.offset
      name: offset
      supportedModules: []
    - description: commands.server.kafka.arguments.partitions
      name: partition
      supportedModules: []
    deprecated: true
    description: commands.server.kafka.fetch.msg
    name: kafka-consume-msg
    supportedModules: []
  - arguments:
    - default: true
      description: commands.server.kafka.arguments.topic
      name: topic
      required: true
      supportedModules: []
    deprecated: true
    description: commands.server.kafka.fetch.partitions
    name: kafka-fetch-partitions
    supportedModules: []
  runonce: false
  script: ""
  type: ""
signature: ""
