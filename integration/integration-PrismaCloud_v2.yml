category: Network Security
commonfields:
  id: PrismaCloud v2
  version: -1
configuration:
- additionalinfo: 'Prisma Cloud service tenant admin console URL. The URL for the
    Prisma Cloud service varies depending on the cluster on which your tenant is deployed.
    For more information see: https://pan.dev/prisma-cloud/api/cspm/api-urls/.'
  defaultvalue: https://api.prismacloud.io/
  display: Server URL
  name: url
  required: true
  section: Connect
  supportedModules: []
  type: 0
- display: Username / Access Key ID
  displaypassword: Password / Access Key Secret
  name: credentials
  required: true
  section: Connect
  supportedModules: []
  type: 9
- advanced: true
  defaultvalue: "false"
  display: Use system proxy settings
  name: proxy
  required: false
  section: Connect
  supportedModules: []
  type: 8
- advanced: true
  defaultvalue: "false"
  display: Trust any certificate (not secure)
  name: insecure
  required: false
  section: Connect
  supportedModules: []
  type: 8
- display: Incident type
  name: incidentType
  required: false
  section: Connect
  supportedModules: []
  type: 13
- additionalinfo: Maximum is limited to 200.
  defaultvalue: "50"
  display: Maximum number of incidents to fetch
  name: max_fetch
  required: false
  section: Collect
  supportedModules: []
  type: 0
- additionalinfo: Date or relative timestamp to start fetching incidents from, in
    the format of <number> <time unit>. For example, 2 minutes, 12 hours, 6 days,
    2 weeks, 3 months, 1 year, ISO timestamp. Default is 3 days.
  defaultvalue: 3 days
  display: First fetch time interval
  name: first_fetch
  required: false
  section: Collect
  supportedModules: []
  type: 0
- additionalinfo: Use this parameter to determine how far back to look in the search
    for incidents that were created before the last run time and did not match the
    query when they were created. When choosing to increase this value, duplicate
    incidents might occur at increase time.
  advanced: true
  defaultvalue: "20"
  display: 'Advanced: Time in minutes to look back when fetching incidents'
  name: look_back
  required: false
  section: Collect
  supportedModules: []
  type: 0
- additionalinfo: 'Comma-separated list of filter name and value, in the following
    format: filtername1=filtervalue1,filtername2=filtervalue2,etc. Names and possible
    values for filters can be found by running the "prisma-cloud-alert-filter-list"
    command.'
  advanced: true
  defaultvalue: alert.status=open
  display: Fetch only incidents matching these filters
  name: filters
  required: false
  section: Collect
  supportedModules: []
  type: 0
- display: Fetch incidents
  name: isFetch
  required: false
  section: Collect
  supportedModules: []
  type: 8
- additionalinfo: 'Choose the direction to mirror the incident: Incoming (from Prisma
    Cloud to Cortex), Outgoing (from Cortex to Prisma Cloud), or Incoming and Outgoing
    (from/to Cortex and Prisma Cloud). Cortex only parameter.'
  defaultvalue: None
  display: Incident Mirroring Direction
  hidden: true
  name: mirror_direction
  options:
  - None
  - Incoming
  - Outgoing
  - Incoming And Outgoing
  required: false
  section: Collect
  supportedModules: []
  type: 15
- additionalinfo: When selected, closing and re-opening the Prisma Cloud alert is
    mirrored in Cortex. Cortex only parameter.
  defaultvalue: "false"
  display: Close Mirrored XSOAR Incident
  hidden: true
  name: close_incident
  required: false
  section: Collect
  supportedModules: []
  type: 8
- additionalinfo: When selected, closing and re-opening the Cortex incident is mirrored
    in Prisma Cloud.
  defaultvalue: "false"
  display: Close Mirrored Prisma Cloud Alert
  name: close_alert
  required: false
  section: Collect
  supportedModules: []
  type: 8
- advanced: true
  defaultvalue: "false"
  display: Output results of V1 commands to the context data in the old format
  name: output_old_format
  required: false
  section: Connect
  supportedModules: []
  type: 8
- defaultvalue: "1"
  display: Alerts Fetch Interval
  name: alertFetchInterval
  required: false
  supportedModules: []
  type: 19
contentitemexportablefields:
  contentitemfields:
    definitionid: ""
    fromServerVersion: 6.5.0
    isoverridable: false
    itemVersion: 4.4.30
    packID: PrismaCloud
    packName: Prisma Cloud by Palo Alto Networks
    packPropagationLabels:
    - all
    prevname: ""
    propagationLabels: []
    supportedModules: []
    toServerVersion: ""
defaultclassifier: RedLock
defaultmapperin: RedLock-mapper
description: Prisma Cloud secures infrastructure, workloads and applications, across
  the entire cloud-native technology stack.
detaileddescription: |-
  ## Prisma Cloud
  Use the Prisma Cloud integration to manage alerts from Microsoft Azure, Google Cloud Platform, and AWS.

  API reference: [Prisma Cloud API reference](https://api.docs.prismacloud.io/reference#try-the-apis)

  ### Fetch Incidents Filtering
  The list of available names and possible values for fetch incidents filters can be retrieved by running the ***prisma-cloud-alert-filter-list*** command.
  The possible values for severities can be found under "policy.severity" entry. In order to add a severity, use the filter name "policy.severity"
  In order to add a rule name, use the filter name "alertRule.name". In order to add a policy name, use the filter name "policy.name".

  In order to use multiple values for the same filter and get alerts that have one of the values, state them several time, for example: _policy.severity=high,policy.severity=medium_.
  The filtering works the same way as in the Prisma Cloud UI. When providing several values for the same filter it gets only alerts that have one of these values, and when providing different values, it gets only alerts that have all the stated field values.

  Providing additional filters should be done in the following format: _filtername1=filtervalue1,filtername2=filtervalue2,etc_. For example: _alert.status=open,policy.severity=high,policy.severity=medium,cloud.type=gcp,policy.remediable=true_.


  By default, the integration fetches incidents in an open state.


  ---
  [View Integration Documentation](https://xsoar.pan.dev/docs/reference/integrations/prisma-cloud-v2)
display: Prisma Cloud v2
image: data:image/png;base64,
imagepath: content/packs/PrismaCloud/PrismaCloudv2_image.png
name: PrismaCloud v2
restrictioncenter: {}
script:
  commands:
  - arguments:
    - description: Page number to return. First page is 0.
      name: page
      supportedModules: []
      type: integer
    - description: Number of results in a page. Maximum is 1000.
      name: page_size
      supportedModules: []
      type: integer
    - description: Comma-separated list of names of Git users.
      isArray: true
      name: git_users
      supportedModules: []
    - description: A branch name.
      name: branch
      supportedModules: []
    - auto: PREDEFINED
      description: Comma-separated list of code issue categories. Is also available
        as a search criteria (search_scopes).
      isArray: true
      name: code_categories
      predefined:
      - IacMisconfiguration
      - IacExternalModule
      - ImageReferencerVul
      - ImageReferencerLicenses
      - Vulnerabilities
      - Licenses
      - Secrets
      supportedModules: []
    - auto: PREDEFINED
      description: Comma-separated list of categories related to Infrastructure-as-Code
        (IaC) issues.
      isArray: true
      name: iac_categories
      predefined:
      - IAM
      - Monitoring
      - Networking
      - Kubernetes
      - General
      - Storage
      - Public
      - Drift
      - Compute
      supportedModules: []
    - auto: PREDEFINED
      description: Comma-separated list of labels associated with Infrastructure-as-Code
        (IaC).
      isArray: true
      name: iac_labels
      predefined:
      - CustomPolicy
      - HasFix
      supportedModules: []
    - auto: PREDEFINED
      description: Comma-separated list of file types.
      isArray: true
      name: file_types
      predefined:
      - build.gradle
      - gradle.properties
      - build.gradle.kts
      - pom.xml
      - requirements.txt
      - package-lock.json
      - package.json
      - bower.json
      - go.sum
      - go.mod
      - tf
      - json
      - yml
      - yaml
      - Template
      - .checkov.baseline
      - Hcl
      - Dockerfile
      - gradle-wrapper.properties
      - METADATA
      - Bicep
      - Pipfile.lock
      - Pipfile
      - Yarn.lock
      - Gemfile
      - Gemfile.lock
      - gemspec
      - env
      - settings.py
      - main.py
      - application.py
      - config.py
      - app.js
      - config.js
      - dev.js
      - db.properties
      - application.properties
      - private.pem
      - privatekey.pem
      - Index.php
      - config.php
      - config.xml
      - strings.xml
      - app.module.ts
      - environment.ts
      - tpl
      - tfvars
      - unknown
      - csproj
      - packages.config
      - Directory.Packages.props
      - paket.dependencies
      - paket.references
      - paket.lock
      - sln
      - config
      - composer.json
      - composer.lock
      supportedModules: []
    - auto: PREDEFINED
      description: Whether to retrieve only fixable issues. Recommended to use this
        argument with another argument (code_categories, file_types, iac_categories...)
      name: fixable_only
      predefined:
      - "true"
      - "false"
      supportedModules: []
      type: Boolean
    - description: Comma-separated list of repository names.
      isArray: true
      name: repositories
      supportedModules: []
    - auto: PREDEFINED
      description: Comma-separated list of risk factors related to secrets.
      isArray: true
      name: secrets_risk_factors
      predefined:
      - PublicRepository
      - PrivateRepository
      - User
      - Privileged
      - Valid
      - Invalid
      - Unknown
      - FoundInHistory
      supportedModules: []
    - auto: PREDEFINED
      description: Comma-separated list of specific areas or categories within which
        to search. When specifying `search_scopes`, `search_term` argument is required.
      isArray: true
      name: search_scopes
      predefined:
      - IacMisconfiguration
      - IacExternalModule
      - ImageReferencerVul
      - ImageReferencerLicenses
      - Vulnerabilities
      - Licenses
      - Secrets
      supportedModules: []
    - auto: PREDEFINED
      description: Comma-separated list of criticality of issues.
      isArray: true
      name: severities
      predefined:
      - INFO
      - LOW
      - MEDIUM
      - HIGH
      - CRITICAL
      supportedModules: []
    - auto: PREDEFINED
      description: Comma-separated list of risk factors for vulnerabilities.
      isArray: true
      name: vulnerability_risk_factors
      predefined:
      - AttackComplexity
      - AttackVector
      - DoS
      - HasFix
      - RemoteExecution
      - ExploitInTheWild
      - ExploitPOC
      - IsUsed
      supportedModules: []
    - description: A keyword or phrase used to narrow down results within specific
        scopes (like a particular vulnerability). This argument is require when filtering
        results by search_scopes.
      name: search_term
      supportedModules: []
    - description: An infrastructure-as-code (IaC) resource. Relevant only to IacMisconfiguration,
        Vulnerabilities.
      name: iac_tags
      supportedModules: []
    - description: Comma-separated list of license types.
      isArray: true
      name: license_type
      supportedModules: []
    - auto: PREDEFINED
      description: Status of checks.
      name: check_status
      predefined:
      - Error
      - Passed
      - Suppressed
      - FixPending
      supportedModules: []
    - defaultValue: "50"
      description: The limit of issues to retrieve. If `page` and `page_size` arguments
        are provided, then the limit argument is ignored.
      name: limit
      supportedModules: []
    description: Retrieves the code errors detected by Application Security during
      periodic scans. At least one filtering argument is required, excluding `search_scopes`,
      `search_term`, and `limit`. For example, `fixable_only` or 'branch`.
    name: prisma-cloud-code-issues-list
    outputs:
    - contextPath: PrismaCloud.CodeIssue.modifiedOn
      description: The date and time when the code issue was last modified.
      type: String
    - contextPath: PrismaCloud.CodeIssue.isPublic
      description: Indicates whether the repository or resource is publicly accessible
        (true/false).
      type: String
    - contextPath: PrismaCloud.CodeIssue.fileType
      description: The type of file where the issue was detected (e.g., `.js`, `.py`).
      type: String
    - contextPath: PrismaCloud.CodeIssue.secretCommitRemove
      description: The commit identifier where a secret was removed.
      type: String
    - contextPath: PrismaCloud.CodeIssue.secretCommitAdd
      description: The commit identifier where a secret was added.
      type: String
    - contextPath: PrismaCloud.CodeIssue.gitUser
      description: The username of the person who made the commit associated with
        the code issue.
      type: String
    - contextPath: PrismaCloud.CodeIssue.codePath
      description: The path to the code file where the issue was detected.
      type: String
    - contextPath: PrismaCloud.CodeIssue.codeCategory
      description: The category or classification of the detected issue (e.g., security,
        compliance).
      type: String
    - contextPath: PrismaCloud.CodeIssue.author
      description: The original author of the code where the issue was detected.
      type: String
    - contextPath: PrismaCloud.CodeIssue.repositoryUuid
      description: The unique identifier of the repository where the issue was found.
      type: String
    - contextPath: PrismaCloud.CodeIssue.path
      description: The path in the repository where the issue was detected.
      type: String
    - contextPath: PrismaCloud.CodeIssue.repositorySource
      description: The source of the repository (e.g., GitHub, Bitbucket).
      type: String
    - contextPath: PrismaCloud.CodeIssue.firstDetected
      description: The date and time when the code issue was first detected.
      type: String
    - contextPath: PrismaCloud.CodeIssue.codeIssueLine
      description: The line number in the code where the issue was detected.
      type: String
    - contextPath: PrismaCloud.CodeIssue.labels
      description: Labels associated with the code issue for classification or categorization.
      type: String
    - contextPath: PrismaCloud.CodeIssue.labels.label
      description: A specific label related to the code issue.
      type: String
    - contextPath: PrismaCloud.CodeIssue.labels.metadata.imageName
      description: The name of the image associated with the label metadata.
      type: String
    - contextPath: PrismaCloud.CodeIssue.policy
      description: The policy that was violated by the detected issue.
      type: String
    - contextPath: PrismaCloud.CodeIssue.repository
      description: The name of the repository where the issue was found.
      type: String
    - contextPath: PrismaCloud.CodeIssue.resourceName
      description: The name of the resource associated with the code issue.
      type: String
    - contextPath: PrismaCloud.CodeIssue.resourceScanType
      description: The type of scan that detected the issue (e.g., static analysis,
        secret detection).
      type: String
    - contextPath: PrismaCloud.CodeIssue.resourceUuid
      description: The unique identifier of the resource associated with the code
        issue.
      type: String
    - contextPath: PrismaCloud.CodeIssue.severity
      description: The severity level of the detected issue (e.g., High, Medium, Low).
      type: String
    - contextPath: PrismaCloud.CodeIssue.violationId
      description: The unique identifier for the policy violation related to the code
        issue.
      type: String
    - contextPath: PrismaCloud.CodeIssue.causePackageId
      description: The ID of the package that caused the issue.
      type: String
    - contextPath: PrismaCloud.CodeIssue.causePackageName
      description: The name of the package that caused the issue.
      type: String
    - contextPath: PrismaCloud.CodeIssue.cveUuid
      description: The unique identifier of the Common Vulnerabilities and Exposures
        (CVE) related to the issue.
      type: String
    - contextPath: PrismaCloud.CodeIssue.cvss
      description: The Common Vulnerability Scoring System (CVSS) score for the detected
        issue.
      type: String
    - contextPath: PrismaCloud.CodeIssue.fixVersion
      description: The version of the code or package that contains the fix for the
        issue.
      type: String
    - contextPath: PrismaCloud.CodeIssue.pr
      description: The pull request (PR) associated with resolving the code issue.
      type: String
    - contextPath: PrismaCloud.CodeIssue.resourceId
      description: The identifier of the resource related to the code issue.
      type: String
    - contextPath: PrismaCloud.CodeIssue.riskFactors
      description: Risk factors associated with the code issue, such as high-impact
        areas or sensitive data.
      type: String
    - contextPath: PrismaCloud.CodeIssue.isIndirectPackage
      description: Indicates whether the issue is from an indirect package (true/false).
      type: String
    supportedModules: []
  - arguments:
    - description: Comma-separated list of alert IDs to be dismissed.
      isArray: true
      name: alert_ids
      supportedModules: []
    - description: Comma-separated list of policy IDs.
      isArray: true
      name: policy_ids
      supportedModules: []
    - description: The amount of time for snoozing alert. Both snooze value and unit
        must be specified if snoozing.
      name: snooze_value
      supportedModules: []
    - auto: PREDEFINED
      description: The time unit for snoozing alert. Both snooze value and unit must
        be specified if snoozing.
      name: snooze_unit
      predefined:
      - hour
      - day
      - week
      - month
      - year
      supportedModules: []
    - description: Reason for dismissal.
      name: dismissal_note
      required: true
      supportedModules: []
    - description: 'Start time for the search. Time is interpreted as UTC. Values
        can be in either ISO date format, relative time, or epoch timestamp. For example:
        "2019-10-21T23:45:00 GMT+3" (ISO date format), "3 days" (relative time), 1579039377301
        (epoch time).'
      name: time_range_date_from
      supportedModules: []
    - description: 'End time for the search. Time is interpreted as UTC. Values can
        be in either ISO date format, relative time, or epoch timestamp. For example:
        "2019-10-21T23:45:00 GMT+3" (ISO date format), "3 days" (relative time), 1579039377301
        (epoch time).'
      name: time_range_date_to
      supportedModules: []
    - auto: PREDEFINED
      description: The search time unit. The "login" and "epoch" options are only
        available if "time_range_value" is not provided.
      name: time_range_unit
      predefined:
      - hour
      - day
      - week
      - month
      - year
      - login
      - epoch
      supportedModules: []
    - description: The amount of "time_range_unit" to go back in time. For example,
        3 days, 5 weeks, etc.
      name: time_range_value
      supportedModules: []
    - description: 'Comma-separated list of filter name and value, in the following
        format: filtername1=filtervalue1,filtername2=filtervalue2,etc. Names and possible
        values for filters can be found by running the "prisma-cloud-alert-filter-list"
        command.'
      isArray: true
      name: filters
      supportedModules: []
    description: Dismiss or snooze the alerts matching the given filter. Either policy
      IDs or alert IDs must be provided. When no absolute time nor relative time arguments
      are provided, the default time range is all times. For snoozing, provide "snooze_unit"
      and "snooze_value" arguments.
    execution: true
    name: prisma-cloud-alert-dismiss
    supportedModules: []
  - arguments:
    - default: true
      description: The alert ID.
      name: alert_id
      required: true
      supportedModules: []
    - auto: PREDEFINED
      defaultValue: "true"
      description: Whether to retrieve the entire / trimmed alert model.
      name: detailed
      predefined:
      - "true"
      - "false"
      supportedModules: []
    description: Gets the details of an alert based on the alert ID.
    name: prisma-cloud-alert-get-details
    outputs:
    - contextPath: PrismaCloud.Alert.id
      description: The alert ID.
      type: String
    - contextPath: PrismaCloud.Alert.status
      description: The alert status.
      type: String
    - contextPath: PrismaCloud.Alert.reason
      description: The alert reason.
      type: String
    - contextPath: PrismaCloud.Alert.alertTime
      description: The time of the alert.
      type: Date
    - contextPath: PrismaCloud.Alert.firstSeen
      description: The time the alert was first seen.
      type: Date
    - contextPath: PrismaCloud.Alert.lastSeen
      description: The time the alert was last seen.
      type: Date
    - contextPath: PrismaCloud.Alert.eventOccurred
      description: The time the event occurred.
      type: Date
    - contextPath: PrismaCloud.Alert.alertRules
      description: Names of the alert rules that triggered this alert.
      type: String
    - contextPath: PrismaCloud.Alert.resource.resourceApiName
      description: The resource API name.
      type: String
    - contextPath: PrismaCloud.Alert.resource.id
      description: The resource ID.
      type: String
    - contextPath: PrismaCloud.Alert.resource.account
      description: The resource account.
      type: String
    - contextPath: PrismaCloud.Alert.resource.accountId
      description: The resource account ID.
      type: String
    - contextPath: PrismaCloud.Alert.resource.resourceType
      description: The resource type.
      type: String
    - contextPath: PrismaCloud.Alert.policy.policyId
      description: The policy ID.
      type: String
    - contextPath: PrismaCloud.Alert.policy.name
      description: The policy name.
      type: String
    - contextPath: PrismaCloud.Alert.policy.policyType
      description: The type of policy.
      type: String
    - contextPath: PrismaCloud.Alert.policy.severity
      description: The policy severity.
      type: String
    - contextPath: PrismaCloud.Alert.policy.recommendation
      description: The policy recommendation.
      type: String
    - contextPath: PrismaCloud.Alert.policy.remediation.description
      description: The policy remediation description.
      type: String
    - contextPath: PrismaCloud.Alert.policy.remediation.cliScriptTemplate
      description: The policy remediation CLI script template.
      type: String
    - contextPath: PrismaCloud.Alert.policy.description
      description: The policy description.
      type: String
    - contextPath: PrismaCloud.Alert.policy.labels
      description: The policy labels.
      type: Unknown
    - contextPath: PrismaCloud.Alert.resource.cloudType
      description: The resource cloud type.
      type: String
    - contextPath: PrismaCloud.Alert.resource.rrn
      description: The restricted resource name.
      type: String
    - contextPath: PrismaCloud.Alert.resource.regionId
      description: The resource region ID.
      type: String
    - contextPath: PrismaCloud.Alert.resource.url
      description: The resource URL.
      type: String
    - contextPath: PrismaCloud.Alert.policy.remediable
      description: Whether the policy is remediable.
      type: Boolean
    - contextPath: PrismaCloud.Alert.policy.systemDefault
      description: Whether the policy is the system default.
      type: Boolean
    - contextPath: PrismaCloud.Alert.policy.deleted
      description: Whether the policy was deleted.
      type: Boolean
    supportedModules: []
  - arguments: []
    description: List the acceptable filters and values for alerts.
    name: prisma-cloud-alert-filter-list
    outputs:
    - contextPath: PrismaCloud.AlertFilters.filterName
      description: The filter name.
      type: String
    - contextPath: PrismaCloud.AlertFilters.options
      description: The filter value options.
      type: String
    - contextPath: PrismaCloud.AlertFilters.staticFilter
      description: Whether the filter is static.
      type: Unknown
    supportedModules: []
  - arguments:
    - description: Comma-seperated list of alert IDs for which to get remediation
        details. Provided alert IDs must be associated with the same policy. If a
        policy is specified, all the alerts specified must belong to that policy.
      isArray: true
      name: alert_ids
      supportedModules: []
    - description: Policy ID for which to get remediation details.
      name: policy_id
      supportedModules: []
    - description: 'Start time for the search. Time is interpreted as UTC. Values
        can be in either ISO date format, relative time, or epoch timestamp. For example:
        "2019-10-21T23:45:00 GMT+3" (ISO date format), "3 days" (relative time), 1579039377301
        (epoch time).'
      name: time_range_date_from
      supportedModules: []
    - description: 'End time for the search. Time is interpreted as UTC. Values can
        be in either ISO date format, relative time, or epoch timestamp. For example:
        "2019-10-21T23:45:00 GMT+3" (ISO date format), "3 days" (relative time), 1579039377301
        (epoch time).'
      name: time_range_date_to
      supportedModules: []
    - auto: PREDEFINED
      description: The search time unit. The "login" and "epoch" options are only
        available if "time_range_value" is not provided.
      name: time_range_unit
      predefined:
      - hour
      - day
      - week
      - month
      - year
      - login
      - epoch
      supportedModules: []
    - description: The amount of "time_range_unit" to go back in time. For example,
        3 days, 5 weeks, etc.
      name: time_range_value
      supportedModules: []
    - defaultValue: "50"
      description: Maximum number of entries to return.
      name: limit
      supportedModules: []
    - auto: PREDEFINED
      description: Whether to retrieve all results. The "limit" argument will be ignored.
      name: all_results
      predefined:
      - "true"
      - "false"
      supportedModules: []
    description: Generates and returns a list of remediation commands for the specified
      alerts and policies. Data returned for a successful call include fully constructed
      commands for remediation. Either a policy ID or alert IDs must be provided.
      The returned information can be retrieved in the UI by clicking the "Remediate"
      button under the "Actions" column for supported alerts. When no absolute time
      nor relative time arguments are provided, the default time range is all times.
    name: prisma-cloud-remediation-command-list
    outputs:
    - contextPath: PrismaCloud.AlertRemediation.description
      description: Description of CLI remediation instructions.
      type: String
    - contextPath: PrismaCloud.AlertRemediation.scriptImpact
      description: Impact of CLI remediation instructions.
      type: String
    - contextPath: PrismaCloud.AlertRemediation.alertId
      description: The ID of the alert to which the remediation details apply.
      type: String
    - contextPath: PrismaCloud.AlertRemediation.CLIScript
      description: The exact CLI command string.
      type: String
    supportedModules: []
  - arguments:
    - default: true
      description: The alert ID.
      name: alert_id
      required: true
      supportedModules: []
    description: Remediates the alert with the specified ID, if that alert is associated
      with a remediable policy. In order to check what remediation would run, use
      the "prisma-cloud-remediation-command-list" command first.
    execution: true
    name: prisma-cloud-alert-remediate
    outputs:
    - contextPath: PrismaCloud.AlertRemediation.alertId
      description: The ID of the alert to which the remediation apply.
      type: String
    - contextPath: PrismaCloud.AlertRemediation.successful
      description: Whether the remediation was successful.
      type: Boolean
    - contextPath: PrismaCloud.AlertRemediation.failureReason
      description: The failure reason for the remediation.
      type: String
    - contextPath: PrismaCloud.AlertRemediation.errorValue
      description: The error value for the remediation.
      type: String
    supportedModules: []
  - arguments:
    - description: Comma-separated list of alert IDs to be reopened.
      isArray: true
      name: alert_ids
      supportedModules: []
    - description: Comma-separated list of policy IDs.
      isArray: true
      name: policy_ids
      supportedModules: []
    - description: 'Start time for the search. Time is interpreted as UTC. Values
        can be in either ISO date format, relative time, or epoch timestamp. For example:
        "2019-10-21T23:45:00 GMT+3" (ISO date format), "3 days" (relative time), 1579039377301
        (epoch time).'
      name: time_range_date_from
      supportedModules: []
    - description: 'End time for the search. Time is interpreted as UTC. Values can
        be in either ISO date format, relative time, or epoch timestamp. For example:
        "2019-10-21T23:45:00 GMT+3" (ISO date format), "3 days" (relative time), 1579039377301
        (epoch time).'
      name: time_range_date_to
      supportedModules: []
    - auto: PREDEFINED
      description: The search time unit. The "login" and "epoch" options are only
        available if "time_range_value" is not provided.
      name: time_range_unit
      predefined:
      - hour
      - day
      - week
      - month
      - year
      - login
      - epoch
      supportedModules: []
    - description: The amount of "time_range_unit" to go back in time. For example,
        3 days, 5 weeks, etc.
      name: time_range_value
      supportedModules: []
    - description: 'Comma-separated list of filter name and value, in the following
        format: filtername1=filtervalue1,filtername2=filtervalue2,etc. Names and possible
        values for filters can be found by running the "prisma-cloud-alert-filter-list"
        command.'
      isArray: true
      name: filters
      supportedModules: []
    description: Re-open the alerts matching the given filter. Either policy IDs or
      alert IDs must be provided. When no absolute time nor relative time arguments
      are provided, the default time range is all times.
    execution: true
    name: prisma-cloud-alert-reopen
    supportedModules: []
  - arguments:
    - description: 'Start time for the search. Time is interpreted as UTC. Values
        can be in either ISO date format, relative time, or epoch timestamp. For example:
        "2019-10-21T23:45:00 GMT+3" (ISO date format), "3 days" (relative time), 1579039377301
        (epoch time).'
      name: time_range_date_from
      supportedModules: []
    - description: 'End time for the search. Time is interpreted as UTC. Values can
        be in either ISO date format, relative time, or epoch timestamp. For example:
        "2019-10-21T23:45:00 GMT+3" (ISO date format), "3 days" (relative time), 1579039377301
        (epoch time).'
      name: time_range_date_to
      supportedModules: []
    - auto: PREDEFINED
      description: The search time unit. The "login" and "epoch" options are only
        available if "time_range_value" is not provided.
      name: time_range_unit
      predefined:
      - hour
      - day
      - week
      - month
      - year
      - login
      - epoch
      supportedModules: []
    - description: The amount of "time_range_unit" to go back in time. For example,
        3 days, 5 weeks, etc.
      name: time_range_value
      supportedModules: []
    - description: 'Comma-separated list of filter name and value, in the following
        format: filtername1=filtervalue1,filtername2=filtervalue2,etc. Names and possible
        values for filters can be found by running the "prisma-cloud-alert-filter-list"
        command.'
      isArray: true
      name: filters
      supportedModules: []
    - auto: PREDEFINED
      defaultValue: "true"
      description: Whether to retrieve the entire / trimmed alert model.
      name: detailed
      predefined:
      - "true"
      - "false"
      supportedModules: []
    - defaultValue: "50"
      description: Maximum number of entries to return.
      name: limit
      supportedModules: []
    - description: Token of the next page to retrive. When provided, other arguments
        are ignored.
      name: next_token
      supportedModules: []
    - auto: PREDEFINED
      description: 'The field to sort the results by. Possible values are: alertTime,firstSeen,lastSeen,lastUpdated.'
      name: sort_field
      predefined:
      - alertTime
      - firstSeen
      - lastSeen
      - lastUpdated
      supportedModules: []
    - auto: PREDEFINED
      defaultValue: asc
      description: 'The direction to sort the results by. Sort field must be specified
        if sorting. Possible values are: asc, desc. Default is asc.'
      name: sort_direction
      predefined:
      - asc
      - desc
      supportedModules: []
    description: Search alerts on the Prisma Cloud platform. When no absolute time
      nor relative time arguments are provided, the search will show alerts from the
      last 7 days.
    name: prisma-cloud-alert-search
    outputs:
    - contextPath: PrismaCloud.AlertPageToken.nextPageToken
      description: Next page token.
      type: String
    - contextPath: PrismaCloud.Alert.id
      description: The ID of the returned alert.
      type: String
    - contextPath: PrismaCloud.Alert.status
      description: The status of the returned alert.
      type: String
    - contextPath: PrismaCloud.Alert.reason
      description: The reason of the returned alert.
      type: String
    - contextPath: PrismaCloud.Alert.lastSeen
      description: The time the returned alert was last seen.
      type: String
    - contextPath: PrismaCloud.Alert.firstSeen
      description: The time the returned alert was first seen.
      type: String
    - contextPath: PrismaCloud.Alert.lastUpdated
      description: The time the returned alert was last updated.
      type: String
    - contextPath: PrismaCloud.Alert.alertTime
      description: The time of the returned alert.
      type: String
    - contextPath: PrismaCloud.Alert.policy.policyId
      description: The policy ID of the returned alert.
      type: String
    - contextPath: PrismaCloud.Alert.policy.name
      description: The policy name of the returned alert.
      type: String
    - contextPath: PrismaCloud.Alert.policy.policyType
      description: The policy type of the returned alert.
      type: String
    - contextPath: PrismaCloud.Alert.policy.severity
      description: The policy severity of the returned alert.
      type: String
    - contextPath: PrismaCloud.Alert.policy.remediable
      description: Whether the policy is remediable.
      type: Boolean
    - contextPath: PrismaCloud.Alert.policy.description
      description: The policy description of the returned alert.
      type: String
    - contextPath: PrismaCloud.Alert.policy.recommendation
      description: The policy recommendation of the returned alert.
      type: String
    - contextPath: PrismaCloud.Alert.policy.remediation.description
      description: The policy remediation description of the returned alert.
      type: String
    - contextPath: PrismaCloud.Alert.policy.remediation.cliScriptTemplate
      description: The policy CLI script template description of the returned alert.
      type: String
    - contextPath: PrismaCloud.Alert.policy.systemDefault
      description: Whether the policy is the system default.
      type: Boolean
    - contextPath: PrismaCloud.Alert.policy.deleted
      description: Whether the policy was deleted.
      type: Boolean
    - contextPath: PrismaCloud.Alert.resource.resourceType
      description: The resource type of the returned alert.
      type: String
    - contextPath: PrismaCloud.Alert.resource.name
      description: The resource name of the returned alert.
      type: String
    - contextPath: PrismaCloud.Alert.resource.account
      description: The resource account of the returned alert.
      type: String
    - contextPath: PrismaCloud.Alert.resource.cloudType
      description: The resource cloud type of the returned alert.
      type: String
    - contextPath: PrismaCloud.Alert.resource.rrn
      description: The restricted resource name of the returned alert.
      type: String
    supportedModules: []
  - arguments:
    - description: 'Start time for the search. Time is interpreted as UTC. Values
        can be in either ISO date format, relative time, or epoch timestamp. For example:
        "2019-10-21T23:45:00 GMT+3" (ISO date format), "3 days" (relative time), 1579039377301
        (epoch time).'
      name: time_range_date_from
      supportedModules: []
    - description: 'End time for the search. Time is interpreted as UTC. Values can
        be in either ISO date format, relative time, or epoch timestamp. For example:
        "2019-10-21T23:45:00 GMT+3" (ISO date format), "3 days" (relative time), 1579039377301
        (epoch time).'
      name: time_range_date_to
      supportedModules: []
    - auto: PREDEFINED
      description: The search time unit. The "login" and "epoch" options are only
        available if "time_range_value" is not provided.
      name: time_range_unit
      predefined:
      - hour
      - day
      - week
      - month
      - year
      - login
      - epoch
      supportedModules: []
    - description: The amount of "time_range_unit" to go back in time. For example,
        3 days, 5 weeks, etc.
      name: time_range_value
      supportedModules: []
    - description: 'Query to run in Prisma Cloud config API using RQL language. For
        more information see: https://docs.paloaltonetworks.com/prisma/prisma-cloud/prisma-cloud-rql-reference/rql-reference/config-query.'
      name: query
      required: true
      supportedModules: []
    - defaultValue: "50"
      description: Maximum number of entries to return.
      name: limit
      supportedModules: []
    - description: Search ID. Can be used to rerun the same search.
      name: search_id
      supportedModules: []
    - auto: PREDEFINED
      defaultValue: desc
      description: The direction to sort the results by. Both sort direction and field
        must be specified if sorting.
      name: sort_direction
      predefined:
      - asc
      - desc
      supportedModules: []
    - auto: PREDEFINED
      defaultValue: insertTs
      description: The field to sort the results by. Both sort direction and field
        must be specified if sorting.
      name: sort_field
      predefined:
      - id
      - time
      - apiName
      - customerId
      - insertTs
      - json
      - cloudAccount
      - cloudRegion
      - stateId
      supportedModules: []
    - auto: PREDEFINED
      defaultValue: "true"
      description: Whether to include the resource JSON or not. Default is "true".
        "false" will not return the resource JSON, "true" will return the resource
        JSON. Note that if the value is "true", some of the keys, which contain large
        data objects, are still omitted by default and in order to retrieve them,
        set the "include_additional_resource_fields" argument to "true". Doing so
        can increase the context size dramatically.
      name: include_resource_json
      predefined:
      - "true"
      - "false"
      supportedModules: []
    - auto: PREDEFINED
      defaultValue: "false"
      description: 'Whether to retrieve all of the resource JSON, including keys with
        large objects such as: "disks.shieldedInstanceInitialState" and "configure-sh"
        key under metadata. Default is "false".'
      name: include_additional_resource_fields
      predefined:
      - "true"
      - "false"
      supportedModules: []
    - auto: PREDEFINED
      defaultValue: "true"
      description: Enable heuristic search optimization for faster results. Default
        is true.
      name: heuristic_search
      predefined:
      - "true"
      - "false"
      supportedModules: []
    description: Search configuration inventory on the Prisma Cloud platform using
      RQL language. Use this command for all queries that start with "config". When
      no absolute time nor relative time arguments are provided, the default time
      range is all times.
    name: prisma-cloud-config-search
    outputs:
    - contextPath: PrismaCloud.Config.accountId
      description: Cloud account ID.
      type: String
    - contextPath: PrismaCloud.Config.accountName
      description: Cloud account name.
      type: String
    - contextPath: PrismaCloud.Config.allowDrillDown
      description: Whether to allow drill down.
      type: Boolean
    - contextPath: PrismaCloud.Config.cloudType
      description: Cloud type.
      type: String
    - contextPath: PrismaCloud.Config.deleted
      description: Whether the asset was deleted.
      type: Boolean
    - contextPath: PrismaCloud.Config.hasExtFindingRiskFactors
      description: Whether the configuration has external finding risk factors.
      type: Boolean
    - contextPath: PrismaCloud.Config.hasExternalFinding
      description: Whether the configuration has an external finding.
      type: Boolean
    - contextPath: PrismaCloud.Config.hasExternalIntegration
      description: Whether the configuration has an external integration.
      type: Boolean
    - contextPath: PrismaCloud.Config.hasNetwork
      description: Whether the configuration has a network.
      type: Boolean
    - contextPath: PrismaCloud.Config.id
      description: Prisma Cloud configuration ID.
      type: String
    - contextPath: PrismaCloud.Config.assetId
      description: Prisma Cloud asset ID.
      type: String
    - contextPath: PrismaCloud.Config.data
      description: Prisma Cloud asset specific data.
      type: Unknown
    - contextPath: PrismaCloud.Config.insertTs
      description: Insert timestamp.
      type: Date
    - contextPath: PrismaCloud.Config.createdTs
      description: Created timestamp.
      type: Date
    - contextPath: PrismaCloud.Config.name
      description: Asset name.
      type: String
    - contextPath: PrismaCloud.Config.regionId
      description: Cloud region ID.
      type: String
    - contextPath: PrismaCloud.Config.regionName
      description: Cloud region name.
      type: String
    - contextPath: PrismaCloud.Config.resourceType
      description: Cloud resource type.
      type: String
    - contextPath: PrismaCloud.Config.rrn
      description: Cloud restricted resource name.
      type: String
    - contextPath: PrismaCloud.Config.service
      description: Cloud service.
      type: String
    - contextPath: PrismaCloud.Config.stateId
      description: State ID.
      type: String
    supportedModules: []
  - arguments:
    - description: 'Start time for the search. Time is interpreted as UTC. Values
        can be in either ISO date format, relative time, or epoch timestamp. For example:
        "2019-10-21T23:45:00 GMT+3" (ISO date format), "3 days" (relative time), 1579039377301
        (epoch time).'
      name: time_range_date_from
      supportedModules: []
    - description: 'End time for the search. Time is interpreted as UTC. Values can
        be in either ISO date format, relative time, or epoch timestamp. For example:
        "2019-10-21T23:45:00 GMT+3" (ISO date format), "3 days" (relative time), 1579039377301
        (epoch time).'
      name: time_range_date_to
      supportedModules: []
    - auto: PREDEFINED
      description: The search time unit. The "login" and "epoch" options are only
        available if "time_range_value" is not provided.
      name: time_range_unit
      predefined:
      - hour
      - day
      - week
      - month
      - year
      - login
      - epoch
      supportedModules: []
    - description: The amount of "time_range_unit" to go back in time. For example,
        3 days, 5 weeks, etc.
      name: time_range_value
      supportedModules: []
    - description: 'Query to run in Prisma Cloud event API using RQL language. For
        more information see: https://docs.paloaltonetworks.com/prisma/prisma-cloud/prisma-cloud-rql-reference/rql-reference/event-query.'
      name: query
      required: true
      supportedModules: []
    - defaultValue: "50"
      description: Maximum number of entries to return.
      name: limit
      supportedModules: []
    - auto: PREDEFINED
      description: 'The field to sort the results by. Possible values are: cloudService,
        operation, cloudAccount, cloudRegion, id, time, crud, user.'
      name: sort_field
      predefined:
      - cloudService
      - operation
      - cloudAccount
      - cloudRegion
      - id
      - time
      - crud
      - user
      supportedModules: []
    - auto: PREDEFINED
      defaultValue: asc
      description: 'The direction to sort the results by. Sort field must be specified
        if sorting. Possible values are: asc, desc. Default is asc.'
      name: sort_direction
      predefined:
      - asc
      - desc
      supportedModules: []
    description: Search events inventory on the Prisma Cloud platform using RQL language.
      Use this command for all queries that start with "event". When no absolute time
      nor relative time arguments are provided, the default time range is all times.
      In order to reduce the returned data, set the "include_resource_json" argument
      to "false".
    name: prisma-cloud-event-search
    outputs:
    - contextPath: PrismaCloud.Event.subject
      description: Cloud event subject.
      type: String
    - contextPath: PrismaCloud.Event.accountName
      description: Cloud event account name.
      type: String
    - contextPath: PrismaCloud.Event.name
      description: Cloud event name.
      type: String
    - contextPath: PrismaCloud.Event.source
      description: Cloud event source.
      type: String
    - contextPath: PrismaCloud.Event.ip
      description: Cloud event IP address.
      type: String
    - contextPath: PrismaCloud.Event.eventTs
      description: Cloud event timestamp.
      type: Date
    - contextPath: PrismaCloud.Event.countryName
      description: Cloud event country name.
      type: String
    - contextPath: PrismaCloud.Event.stateName
      description: Cloud event state name.
      type: String
    - contextPath: PrismaCloud.Event.cityName
      description: Cloud event city name.
      type: String
    - contextPath: PrismaCloud.Event.location
      description: Cloud event location.
      type: String
    - contextPath: PrismaCloud.Event.account
      description: Cloud event account.
      type: String
    - contextPath: PrismaCloud.Event.regionId
      description: Cloud event region ID.
      type: Number
    - contextPath: PrismaCloud.Event.type
      description: Cloud event type.
      type: String
    - contextPath: PrismaCloud.Event.id
      description: Cloud event ID.
      type: Number
    - contextPath: PrismaCloud.Event.role
      description: Cloud event role.
      type: String
    - contextPath: PrismaCloud.Event.accessKeyUsed
      description: Whether the cloud event access key is used.
      type: Boolean
    - contextPath: PrismaCloud.Event.success
      description: Whether the cloud event is successful.
      type: Boolean
    - contextPath: PrismaCloud.Event.internal
      description: Whether the cloud event is internal.
      type: Boolean
    - contextPath: PrismaCloud.Event.cityId
      description: Cloud event city ID.
      type: Number
    - contextPath: PrismaCloud.Event.cityLatitude
      description: Cloud event city latitude.
      type: Number
    - contextPath: PrismaCloud.Event.cityLongitude
      description: Cloud event city longitude.
      type: Number
    - contextPath: PrismaCloud.Event.countryId
      description: Cloud event country ID.
      type: Number
    - contextPath: PrismaCloud.Event.dynamicData
      description: Cloud event dynamic data.
      type: String
    - contextPath: PrismaCloud.Event.stateId
      description: Cloud event state ID.
      type: Number
    supportedModules: []
  - arguments:
    - description: 'Start time for the search. Time is interpreted as UTC. Values
        can be in either ISO date format, relative time, or epoch timestamp. For example:
        "2019-10-21T23:45:00 GMT+3" (ISO date format), "3 days" (relative time), 1579039377301
        (epoch time).'
      name: time_range_date_from
      supportedModules: []
    - description: 'End time for the search. Time is interpreted as UTC. Values can
        be in either ISO date format, relative time, or epoch timestamp. For example:
        "2019-10-21T23:45:00 GMT+3" (ISO date format), "3 days" (relative time), 1579039377301
        (epoch time).'
      name: time_range_date_to
      supportedModules: []
    - auto: PREDEFINED
      description: The search time unit. The "login" and "epoch" options are only
        available if "time_range_value" is not provided.
      name: time_range_unit
      predefined:
      - hour
      - day
      - week
      - month
      - year
      - login
      - epoch
      supportedModules: []
    - description: The amount of "time_range_unit" to go back in time. For example,
        3 days, 5 weeks, etc.
      name: time_range_value
      supportedModules: []
    - description: 'Query to run in Prisma Cloud network API using RQL language. For
        more information see: https://docs.paloaltonetworks.com/prisma/prisma-cloud/prisma-cloud-rql-reference/rql-reference/network-query.'
      name: query
      required: true
      supportedModules: []
    - auto: PREDEFINED
      description: The cloud in which the network should be searched.
      name: cloud_type
      predefined:
      - aws
      - azure
      - gcp
      - alibaba_cloud
      - oci
      supportedModules: []
    - description: Search ID. Can be used to rerun the same search.
      name: search_id
      supportedModules: []
    description: 'Search networks inventory on the Prisma Cloud platform using RQL
      language. Use this command for all queries that start with "networks". When
      no absolute time nor relative time arguments are provided, the default time
      range is all times. In order to limit the results returning, use "limit search
      records to" at the end of the RQL query, followed by a value from one of these
      options: 1, 10, 100, 1000, and 10,000.'
    name: prisma-cloud-network-search
    outputs:
    - contextPath: PrismaCloud.Network.Node.id
      description: Cloud network node ID.
      type: Number
    - contextPath: PrismaCloud.Network.Node.name
      description: Cloud network node name.
      type: String
    - contextPath: PrismaCloud.Network.Node.ipAddr
      description: Cloud network node IP address.
      type: String
    - contextPath: PrismaCloud.Network.Node.grouped
      description: Whether the cloud network node is grouped.
      type: Boolean
    - contextPath: PrismaCloud.Network.Node.suspicious
      description: Whether the cloud network node is suspicious.
      type: Boolean
    - contextPath: PrismaCloud.Network.Node.vulnerable
      description: Whether the cloud network node is vulnerable.
      type: Boolean
    - contextPath: PrismaCloud.Network.Node.metadata
      description: Cloud network node metadata.
      type: Unknown
    - contextPath: PrismaCloud.Network.Connection.from
      description: Cloud network connection from node ID.
      type: Number
    - contextPath: PrismaCloud.Network.Connection.to
      description: Cloud network connection to node ID.
      type: Number
    - contextPath: PrismaCloud.Network.Connection.label
      description: Cloud network connection label.
      type: String
    - contextPath: PrismaCloud.Network.Connection.suspicious
      description: Whether the cloud network node is suspicious.
      type: Boolean
    - contextPath: PrismaCloud.Network.Connection.metadata
      description: Cloud network connection metadata.
      type: Unknown
    supportedModules: []
  - arguments:
    - description: ID number of the CICD run.
      name: cicd_run_id
      supportedModules: []
    - description: Comma-separated list of authors of the files.
      isArray: true
      name: authors
      supportedModules: []
    - description: Branch of the files.
      name: branch
      supportedModules: []
    - description: 'Comma-separated list of categories of the files. Available options
        are: IAM, Compute, Monitoring, Networking, Kubernetes, General, Storage, Secrets,
        Public, Vulnerabilities, Drift, BuildIntegrity, Licenses.'
      isArray: true
      name: categories
      supportedModules: []
    - auto: PREDEFINED
      description: The code status.
      name: code_status
      predefined:
      - hasFix
      supportedModules: []
    - description: 'Comma-separated list of file types of the files. Available options
        are: tf, json, yml, yaml, template, .checkov.baseline, hcl, Dockerfile, package.json,
        package-lock.json, bower.json, pom.xml, build.gradle, build.gradle.kts, gradle.properties,
        gradle-wrapper.properties, go.sum, go.mod, requirements.txt, METADATA, bicep,
        Pipfile.lock, Pipfile, yarn.lock, Gemfile, Gemfile.lock, gemspec, env, settings.py,
        main.py, application.py, config.py, app.js, config.js, dev.js, db.properties,
        application.properties, private.pem, privatekey.pem, index.php, config.php,
        config.xml, strings.xml, app.module.ts, environment.ts, tpl, tfvars, unknown.'
      isArray: true
      name: file_types
      supportedModules: []
    - description: Repository of the files.
      name: repository
      required: true
      supportedModules: []
    - description: Repository ID of the files.
      name: repository_id
      supportedModules: []
    - description: 'Comma-separated list of search options of the files. Available
        options are: path, code.'
      isArray: true
      name: search_options
      supportedModules: []
    - description: Search text in the files.
      name: search_text
      supportedModules: []
    - auto: PREDEFINED
      description: Search title of the files.
      name: search_title
      predefined:
      - title
      - constructive_title
      - descriptive_title
      supportedModules: []
    - description: 'Comma-separated list of severities of the files. Available options
        are: CRITICAL, HIGH, MEDIUM, LOW, INFO.'
      isArray: true
      name: severities
      supportedModules: []
    - description: 'Comma-separated list of source types of the files. Available options
        are: Github, Bitbucket, Gitlab, AzureRepos, cli, AWS, Azure, GCP, Docker,
        githubEnterprise, gitlabEnterprise, bitbucketEnterprise, terraformCloud, githubActions,
        circleci, codebuild, jenkins, tfcRunTasks, admissionController, terraformEnterprise.'
      isArray: true
      name: source_types
      required: true
      supportedModules: []
    - description: 'Comma-separated list of tag key and value, in the following format:
        tagkey1=tagvalue1,tagkey2=tagvalue2,etc.'
      isArray: true
      name: tags
      supportedModules: []
    - description: 'Comma-separated list of statuses of the files. Available options
        are: Errors, Suppressed, Passed, Fixed.'
      isArray: true
      name: statuses
      supportedModules: []
    - defaultValue: "50"
      description: Maximum number of entries to return.
      name: limit
      supportedModules: []
    - auto: PREDEFINED
      description: Whether to retrieve all results. The "limit" argument will be ignored.
      name: all_results
      predefined:
      - "true"
      - "false"
      supportedModules: []
    deprecated: true
    description: This command is deprecated. Use prisma-cloud-code-issues-list instead.
    name: prisma-cloud-error-file-list
    outputs:
    - contextPath: PrismaCloud.ErrorFile.filePath
      description: Error file path.
      type: String
    - contextPath: PrismaCloud.ErrorFile.suppressedErrorsCount
      description: The number of error file suppressed errors.
      type: Number
    - contextPath: PrismaCloud.ErrorFile.passedCount
      description: The number of error files passed.
      type: Number
    - contextPath: PrismaCloud.ErrorFile.openErrorsCount
      description: The number of error file open errors.
      type: Number
    - contextPath: PrismaCloud.ErrorFile.errorsCount
      description: The number of error file errors.
      type: Number
    - contextPath: PrismaCloud.ErrorFile.fixedCount
      description: The number of error files fixed.
      type: Number
    - contextPath: PrismaCloud.ErrorFile.type
      description: Error file type.
      type: String
    supportedModules: []
  - arguments: []
    description: Trigger asynchronous scan of all resources to refresh the current
      state at Prisma Cloud Code Security. In order to use this command, the "Code
      Security" module needs to be enabled and accessible in the Prisma Cloud UI.
    name: prisma-cloud-trigger-scan
    supportedModules: []
  - arguments:
    - description: Restricted Resource Name of the resource to get details about.
        Can be retrieved by running a command that has that RRN.
      name: rrn
      required: true
      supportedModules: []
    description: Get resource details.
    name: prisma-cloud-resource-get
    outputs:
    - contextPath: PrismaCloud.Resource.rrn
      description: Prisma Cloud restricted resource name.
      type: String
    - contextPath: PrismaCloud.Resource.id
      description: Prisma Cloud resource ID.
      type: String
    - contextPath: PrismaCloud.Resource.name
      description: Resource name.
      type: String
    - contextPath: PrismaCloud.Resource.url
      description: Resource URL.
      type: String
    - contextPath: PrismaCloud.Resource.accountId
      description: Cloud account ID.
      type: String
    - contextPath: PrismaCloud.Resource.accountName
      description: Cloud account name.
      type: String
    - contextPath: PrismaCloud.Resource.cloudType
      description: Cloud type.
      type: String
    - contextPath: PrismaCloud.Resource.regionId
      description: Cloud region ID.
      type: String
    - contextPath: PrismaCloud.Resource.regionName
      description: Cloud region Name.
      type: String
    - contextPath: PrismaCloud.Resource.service
      description: Cloud service.
      type: String
    - contextPath: PrismaCloud.Resource.resourceType
      description: Cloud resource type.
      type: String
    - contextPath: PrismaCloud.Resource.insertTs
      description: Insert timestamp.
      type: Date
    - contextPath: PrismaCloud.Resource.deleted
      description: Whether the resource was deleted.
      type: Boolean
    - contextPath: PrismaCloud.Resource.vpcId
      description: VPC ID.
      type: String
    - contextPath: PrismaCloud.Resource.vpcName
      description: VPC name.
      type: String
    - contextPath: PrismaCloud.Resource.tags
      description: Prisma Cloud resource tags.
      type: Unknown
    - contextPath: PrismaCloud.Resource.riskGrade
      description: Risk grade.
      type: String
    - contextPath: PrismaCloud.Resource.hasNetwork
      description: Whether the resource has a network.
      type: Boolean
    - contextPath: PrismaCloud.Resource.hasExternalFinding
      description: Whether the resource has an external finding.
      type: Boolean
    - contextPath: PrismaCloud.Resource.hasExternalIntegration
      description: Whether the resource has an external integration.
      type: Boolean
    - contextPath: PrismaCloud.Resource.allowDrillDown
      description: Whether to allow drill down.
      type: Boolean
    - contextPath: PrismaCloud.Resource.hasExtFindingRiskFactors
      description: Whether the resource has external finding risk factors.
      type: Boolean
    - contextPath: PrismaCloud.Resource.data
      description: Prisma Cloud resource specific data.
      type: Unknown
    supportedModules: []
  - arguments:
    - description: The resource list type.
      name: list_type
      supportedModules: []
    - description: The namespace to return only resources associated with it.
      name: namespace
      supportedModules: []
    - defaultValue: "50"
      description: Maximum number of entries to return.
      name: limit
      supportedModules: []
    - auto: PREDEFINED
      description: Whether to retrieve all results. The "limit" argument will be ignored.
      name: all_results
      predefined:
      - "true"
      - "false"
      supportedModules: []
    description: Returns all the resource lists. Maps to the Resource Lists under
      Settings > Resource Lists in the Console UI.
    name: prisma-cloud-resource-list
    outputs:
    - contextPath: PrismaCloud.ResourceList.id
      description: Prisma Cloud resource list ID.
      type: String
    - contextPath: PrismaCloud.ResourceList.name
      description: Resource list name.
      type: String
    - contextPath: PrismaCloud.ResourceList.resourceListType
      description: Resource list type.
      type: String
    - contextPath: PrismaCloud.ResourceList.description
      description: Resource list description.
      type: String
    - contextPath: PrismaCloud.ResourceList.lastModifiedBy
      description: Resource list last modified user.
      type: String
    - contextPath: PrismaCloud.ResourceList.lastModifiedTs
      description: Resource list last modified time.
      type: Date
    - contextPath: PrismaCloud.ResourceList.members
      description: Resource list members.
      type: Unknown
    - contextPath: PrismaCloud.ResourceList.namespaces
      description: Resource list namespaces.
      type: Unknown
    supportedModules: []
  - arguments:
    - description: The role id to get details of.
      name: role_id
      supportedModules: []
    - description: The resource list name to return only user roles associated with
        it.
      name: resource_list_name
      supportedModules: []
    - defaultValue: "50"
      description: Maximum number of entries to return.
      name: limit
      supportedModules: []
    - auto: PREDEFINED
      description: Whether to retrieve all results. The "limit" argument will be ignored.
      name: all_results
      predefined:
      - "true"
      - "false"
      supportedModules: []
    description: Retrieves user roles. Maps to Settings > Access Control > Roles in
      the Console UI.
    name: prisma-cloud-user-roles-list
    outputs:
    - contextPath: PrismaCloud.UserRoles.id
      description: Prisma Cloud user roles ID.
      type: String
    - contextPath: PrismaCloud.UserRoles.name
      description: User roles name.
      type: String
    - contextPath: PrismaCloud.UserRoles.resourceListIds
      description: User roles resource list IDs.
      type: Unknown
    - contextPath: PrismaCloud.UserRoles.description
      description: User roles description.
      type: String
    - contextPath: PrismaCloud.UserRoles.lastModifiedBy
      description: User roles last modified user.
      type: String
    - contextPath: PrismaCloud.UserRoles.lastModifiedTs
      description: User roles last modified time.
      type: Date
    - contextPath: PrismaCloud.UserRoles.associatedUsers
      description: User roles associated users.
      type: Unknown
    - contextPath: PrismaCloud.UserRoles.restrictDismissalAccess
      description: Whether dismissal access is restricted for the user role.
      type: Boolean
    - contextPath: PrismaCloud.UserRoles.roleType
      description: User roles role type.
      type: String
    - contextPath: PrismaCloud.UserRoles.additionalAttributes
      description: User roles additional attributes.
      type: Unknown
    - contextPath: PrismaCloud.UserRoles.codeRepositoryIds
      description: User roles code repository IDs.
      type: Unknown
    - contextPath: PrismaCloud.UserRoles.accountGroupIds
      description: User roles account group IDs.
      type: Unknown
    - contextPath: PrismaCloud.UserRoles.resourceLists
      description: User roles resource lists.
      type: Unknown
    - contextPath: PrismaCloud.UserRoles.permissionGroup
      description: User roles permission group.
      type: Unknown
    - contextPath: PrismaCloud.UserRoles.codeRepositories
      description: User roles code repositories.
      type: Unknown
    - contextPath: PrismaCloud.UserRoles.accountGroups
      description: User roles account groups.
      type: Unknown
    supportedModules: []
  - arguments:
    - defaultValue: "50"
      description: Maximum number of entries to return.
      name: limit
      supportedModules: []
    - auto: PREDEFINED
      description: Whether to retrieve all results. The "limit" argument will be ignored.
      name: all_results
      predefined:
      - "true"
      - "false"
      supportedModules: []
    - description: Usernames to return only users associated with.
      isArray: true
      name: usernames
      supportedModules: []
    description: Lists all users and service accounts for your tenant. Maps to Settings
      > Access Control > Users in the Console UI.
    name: prisma-cloud-users-list
    outputs:
    - contextPath: PrismaCloud.Users.email
      description: Prisma Cloud user email.
      type: String
    - contextPath: PrismaCloud.Users.firstName
      description: User first name.
      type: String
    - contextPath: PrismaCloud.Users.lastName
      description: User last name.
      type: String
    - contextPath: PrismaCloud.Users.timeZone
      description: User time zone.
      type: String
    - contextPath: PrismaCloud.Users.enabled
      description: User enabled.
      type: Boolean
    - contextPath: PrismaCloud.Users.lastModifiedBy
      description: User last modified user.
      type: String
    - contextPath: PrismaCloud.Users.lastModifiedTs
      description: User last modified time.
      type: Date
    - contextPath: PrismaCloud.Users.lastLoginTs
      description: User last login time.
      type: Date
    - contextPath: PrismaCloud.Users.displayName
      description: User display name.
      type: String
    - contextPath: PrismaCloud.Users.ssoBypassAllowed
      description: Whether SSO bypass is allowed for the user role.
      type: Boolean
    - contextPath: PrismaCloud.Users.accessKeysAllowed
      description: Whether access keys are allowed for the user role.
      type: Boolean
    - contextPath: PrismaCloud.Users.defaultRoleId
      description: User default role ID.
      type: String
    - contextPath: PrismaCloud.Users.roleIds
      description: User role IDs.
      type: Unknown
    - contextPath: PrismaCloud.Users.roles
      description: User roles.
      type: Unknown
    - contextPath: PrismaCloud.Users.username
      description: User username.
      type: String
    - contextPath: PrismaCloud.Users.type
      description: User type.
      type: String
    - contextPath: PrismaCloud.Users.enableKeyExpiration
      description: Whether key expiration is enabled for the user role.
      type: Boolean
    - contextPath: PrismaCloud.Users.accessKeysCount
      description: User access keys count.
      type: Number
    supportedModules: []
  - arguments:
    - auto: PREDEFINED
      defaultValue: "false"
      description: Whether to exclude account group details.
      name: exclude_account_group_details
      predefined:
      - "true"
      - "false"
      supportedModules: []
    - defaultValue: "50"
      description: Maximum number of entries to return.
      name: limit
      supportedModules: []
    - auto: PREDEFINED
      description: Whether to retrieve all results. The "limit" argument will be ignored.
      name: all_results
      predefined:
      - "true"
      - "false"
      supportedModules: []
    description: List accounts.
    name: prisma-cloud-account-list
    outputs:
    - contextPath: PrismaCloud.Account.name
      description: Account name.
      type: String
    - contextPath: PrismaCloud.Account.cloudType
      description: Account cloud type.
      type: String
    - contextPath: PrismaCloud.Account.accountType
      description: Account type.
      type: String
    - contextPath: PrismaCloud.Account.enabled
      description: Whether the account is enabled.
      type: Boolean
    - contextPath: PrismaCloud.Account.lastModifiedTs
      description: Account last modified time.
      type: Date
    - contextPath: PrismaCloud.Account.storageScanEnabled
      description: Whether account storage scan is enabled.
      type: Boolean
    - contextPath: PrismaCloud.Account.protectionMode
      description: Account protection mode.
      type: String
    - contextPath: PrismaCloud.Account.ingestionMode
      description: Account ingestion mode.
      type: Number
    - contextPath: PrismaCloud.Account.deploymentType
      description: Account deployment type.
      type: String
    - contextPath: PrismaCloud.Account.groupIds
      description: Account group IDs.
      type: Unknown
    - contextPath: PrismaCloud.Account.groups
      description: Account groups.
      type: Unknown
    - contextPath: PrismaCloud.Account.status
      description: Account status.
      type: String
    - contextPath: PrismaCloud.Account.numberOfChildAccounts
      description: The number of child accounts.
      type: Number
    - contextPath: PrismaCloud.Account.accountId
      description: Account ID.
      type: String
    - contextPath: PrismaCloud.Account.addedOn
      description: Account added on time.
      type: Date
    supportedModules: []
  - arguments:
    - description: A comma-separated list of accound IDs. To get account IDs, run
        the "prisma-cloud-account-list" command.
      isArray: true
      name: account_ids
      required: true
      supportedModules: []
    description: Get the statuses of the provided accounts.
    name: prisma-cloud-account-status-get
    outputs:
    - contextPath: PrismaCloud.Account.accountId
      description: Account ID.
      type: String
    - contextPath: PrismaCloud.Account.name
      description: Account name.
      type: String
    - contextPath: PrismaCloud.Account.status
      description: Account status.
      type: String
    - contextPath: PrismaCloud.Account.message
      description: Account message.
      type: String
    - contextPath: PrismaCloud.Account.remediation
      description: Account remediation action.
      type: String
    supportedModules: []
  - arguments:
    - description: A comma-separated list of account IDs. To get account IDs, run
        the "prisma-cloud-account-list" command.
      isArray: true
      name: account_ids
      required: true
      supportedModules: []
    description: Get the owners of the provided accounts.
    name: prisma-cloud-account-owner-list
    outputs:
    - contextPath: PrismaCloud.Account.accountId
      description: Account ID.
      type: String
    - contextPath: PrismaCloud.Account.emails
      description: Account owner emails.
      type: Unknown
    supportedModules: []
  - arguments:
    - description: Restricted Resource Name of the resource to get host finding of.
        Can be retrieved by running a command that has that RRN.
      name: rrn
      required: true
      supportedModules: []
    - description: 'Comma separated list of finding types to look for. Available options
        are: guard_duty_host, guard_duty_iam, inspector_sbp, compliance_cis, host_vulnerability_cve.
        When left empty, will return all options.'
      isArray: true
      name: finding_types
      supportedModules: []
    - description: 'Comma separated list of risk factors to look for. Available options
        are: CRITICAL_SEVERITY, HIGH_SEVERITY, MEDIUM_SEVERITY, HAS_FIX, REMOTE_EXECUTION,
        DOS, RECENT_VULNERABILITY, EXPLOIT_EXISTS, ATTACK_COMPLEXITY_LOW, ATTACK_VECTOR_NETWORK,
        REACHABLE_FROM_THE_INTERNET, LISTENING_PORTS, CONTAINER_IS_RUNNING_AS_ROOT,
        NO_MANDATORY_SECURITY_PROFILE_APPLIED, RUNNING_AS_PRIVILEGED_CONTAINER, PACKAGE_IN_USE.
        When left empty, will return all options.'
      isArray: true
      name: risk_factors
      supportedModules: []
    - defaultValue: "50"
      description: Maximum number of entries to return.
      name: limit
      supportedModules: []
    - auto: PREDEFINED
      description: Whether to retrieve all results. The "limit" argument will be ignored.
      name: all_results
      predefined:
      - "true"
      - "false"
      supportedModules: []
    description: Get resource host finding list.
    name: prisma-cloud-host-finding-list
    outputs:
    - contextPath: PrismaCloud.HostFinding.accountId
      description: Host finding account ID.
      type: String
    - contextPath: PrismaCloud.HostFinding.regionId
      description: Host finding region ID.
      type: String
    - contextPath: PrismaCloud.HostFinding.findingId
      description: Host finding ID.
      type: String
    - contextPath: PrismaCloud.HostFinding.type
      description: Host finding type.
      type: String
    - contextPath: PrismaCloud.HostFinding.source
      description: Host finding source.
      type: String
    - contextPath: PrismaCloud.HostFinding.severity
      description: Host finding severity.
      type: String
    - contextPath: PrismaCloud.HostFinding.status
      description: Host finding status.
      type: String
    - contextPath: PrismaCloud.HostFinding.createdOn
      description: The date on which the host finding was created.
      type: Date
    - contextPath: PrismaCloud.HostFinding.updatedOn
      description: The date on which the host finding was updated.
      type: Date
    - contextPath: PrismaCloud.HostFinding.normalizedNames
      description: Host finding normalized names.
      type: Unknown
    - contextPath: PrismaCloud.HostFinding.scanId
      description: Host finding scan ID.
      type: String
    - contextPath: PrismaCloud.HostFinding.resourceCloudId
      description: Host finding resource cloud ID.
      type: String
    - contextPath: PrismaCloud.HostFinding.sourceData.accountId
      description: Host finding source data account ID.
      type: String
    - contextPath: PrismaCloud.HostFinding.sourceData.arn
      description: Host finding source data ARN.
      type: String
    - contextPath: PrismaCloud.HostFinding.title
      description: Host finding title.
      type: String
    - contextPath: PrismaCloud.HostFinding.description
      description: Host finding description.
      type: String
    - contextPath: PrismaCloud.HostFinding.resourceUrl
      description: Host finding resource URL.
      type: String
    - contextPath: PrismaCloud.HostFinding.rlUpdatedOn
      description: The date on which the RL was updated.
      type: Date
    - contextPath: PrismaCloud.HostFinding.externalFindingId
      description: External finding ID.
      type: String
    - contextPath: PrismaCloud.HostFinding.sourceData
      description: Host finding source data.
      type: Unknown
    - contextPath: PrismaCloud.HostFinding.score
      description: Host finding score.
      type: String
    - contextPath: PrismaCloud.HostFinding.count
      description: The number of host findings.
      type: Number
    supportedModules: []
  - arguments:
    - description: User ID to look for. Must be provided with the "query" argument.
      name: user_id
      supportedModules: []
    - description: 'IAM query to run in Prisma Cloud config API using RQL language.
        For more information see: https://docs.paloaltonetworks.com/prisma/prisma-cloud/prisma-cloud-rql-reference/rql-reference/iam-query.'
      name: query
      supportedModules: []
    - defaultValue: "50"
      description: Maximum number of entries to return.
      name: limit
      supportedModules: []
    - description: Token of the next page to retrive.
      name: next_token
      supportedModules: []
    description: Get permission list. You must provide either "query" or "next_token".
    name: prisma-cloud-permission-list
    outputs:
    - contextPath: PrismaCloud.PermissionPageToken.nextPageToken
      description: Next page token.
      type: String
    - contextPath: PrismaCloud.Permission.id
      description: Permission ID.
      type: String
    - contextPath: PrismaCloud.Permission.sourceCloudType
      description: Permission source cloud type.
      type: String
    - contextPath: PrismaCloud.Permission.sourceCloudAccount
      description: Permission source cloud account.
      type: String
    - contextPath: PrismaCloud.Permission.sourceResourceId
      description: Permission source resource ID.
      type: String
    - contextPath: PrismaCloud.Permission.destCloudType
      description: Permission destination cloud type.
      type: String
    - contextPath: PrismaCloud.Permission.destCloudServiceName
      description: Permission destination cloud service name.
      type: String
    - contextPath: PrismaCloud.Permission.destResourceType
      description: Permission destination resource type.
      type: String
    - contextPath: PrismaCloud.Permission.effectiveActionName
      description: Permission effective action name.
      type: String
    - contextPath: PrismaCloud.Permission.grantedByCloudType
      description: Permission granted by cloud type.
      type: String
    - contextPath: PrismaCloud.Permission.grantedByCloudPolicyId
      description: Permission granted by cloud policy ID.
      type: String
    - contextPath: PrismaCloud.Permission.grantedByCloudPolicyName
      description: Permission granted by cloud policy name.
      type: String
    - contextPath: PrismaCloud.Permission.grantedByCloudPolicyType
      description: Permission granted by cloud policy type.
      type: String
    - contextPath: PrismaCloud.Permission.grantedByCloudPolicyRrn
      description: Permission granted by cloud policy restricted resource name.
      type: String
    - contextPath: PrismaCloud.Permission.grantedByCloudEntityId
      description: Permission granted by cloud entity ID.
      type: String
    - contextPath: PrismaCloud.Permission.grantedByCloudEntityName
      description: Permission granted by cloud entity name.
      type: String
    - contextPath: PrismaCloud.Permission.grantedByCloudEntityRrn
      description: Permission granted by cloud entity restricted resource name.
      type: String
    - contextPath: PrismaCloud.Permission.sourcePublic
      description: Whether the permission source is public.
      type: Boolean
    - contextPath: PrismaCloud.Permission.sourceCloudRegion
      description: Permission source cloud region.
      type: String
    - contextPath: PrismaCloud.Permission.sourceCloudServiceName
      description: Permission source cloud service name.
      type: String
    - contextPath: PrismaCloud.Permission.sourceResourceName
      description: Permission source resource name.
      type: String
    - contextPath: PrismaCloud.Permission.sourceResourceType
      description: Permission source resource type.
      type: String
    - contextPath: PrismaCloud.Permission.sourceIdpService
      description: Permission source IDP service.
      type: String
    - contextPath: PrismaCloud.Permission.sourceIdpDomain
      description: Permission source IDP domain.
      type: String
    - contextPath: PrismaCloud.Permission.sourceIdpEmail
      description: Permission source IDP email.
      type: String
    - contextPath: PrismaCloud.Permission.sourceIdpUsername
      description: Permission source IDP username.
      type: String
    - contextPath: PrismaCloud.Permission.sourceIdpGroup
      description: Permission source IDP group.
      type: String
    - contextPath: PrismaCloud.Permission.sourceIdpRrn
      description: Permission source IDP restricted resource name.
      type: String
    - contextPath: PrismaCloud.Permission.sourceCloudResourceRrn
      description: Permission source cloud resource restricted resource name.
      type: String
    - contextPath: PrismaCloud.Permission.destCloudAccount
      description: Permission destination cloud account.
      type: String
    - contextPath: PrismaCloud.Permission.destCloudRegion
      description: Permission destination cloud region.
      type: String
    - contextPath: PrismaCloud.Permission.destResourceName
      description: Permission destination resource name.
      type: String
    - contextPath: PrismaCloud.Permission.destResourceId
      description: Permission destination resource ID.
      type: String
    - contextPath: PrismaCloud.Permission.destCloudResourceRrn
      description: Permission destination cloud resource restricted resource name.
      type: String
    - contextPath: PrismaCloud.Permission.grantedByCloudEntityType
      description: Permission granted by cloud entity type.
      type: String
    - contextPath: PrismaCloud.Permission.accessedResourcesCount
      description: Permission accessed resources count.
      type: String
    - contextPath: PrismaCloud.Permission.lastAccessDate
      description: Permission last access date.
      type: String
    - contextPath: PrismaCloud.Permission.lastAccessStatus
      description: Permission last access status.
      type: String
    - contextPath: PrismaCloud.Permission.isWildCardDestCloudResourceName
      description: Whether the destination cloud resource name is a wildcard.
      type: Boolean
    - contextPath: PrismaCloud.Permission.exceptions
      description: Permission exceptions.
      type: Unknown
    - contextPath: PrismaCloud.Permission.grantedByLevelType
      description: Permission granted by level type.
      type: String
    - contextPath: PrismaCloud.Permission.grantedByLevelId
      description: Permission granted by level ID.
      type: String
    - contextPath: PrismaCloud.Permission.grantedByLevelName
      description: Permission granted by level name.
      type: String
    - contextPath: PrismaCloud.Permission.grantedByLevelRrn
      description: Permission granted by level restricted resource name.
      type: String
    supportedModules: []
  - arguments:
    - description: Start time for the search in the following string format - MM/DD/YYYY.
      name: time-range-date-from
      supportedModules: []
    - description: End time for the search in the following format - MM/DD/YYYY.
      name: time-range-date-to
      supportedModules: []
    - description: The number of units to go back in time.
      name: time-range-value
      supportedModules: []
    - auto: PREDEFINED
      description: The search unit. login and epoch are only available if timeRangeValue
        is not provided.
      name: time-range-unit
      predefined:
      - hour
      - day
      - week
      - month
      - year
      - login
      - epoch
      supportedModules: []
    - auto: PREDEFINED
      description: The policy name.
      name: policy-name
      predefined:
      - IAM password policy does not have a symbol
      - IAM password policy does not expire in 90 days
      - IAM password policy does not have a lowercase character
      - IAM password policy does not have a minimum of 14 characters
      - IAM password policy allows password reuse
      - Default Security Group does not restrict all traffic
      - IAM password policy does not have password expiration period
      - IAM password policy does not exist
      - Access keys are not rotated for 90 days
      - Security Groups allow internet traffic from internet to RDP port (3389)
      - Internet connectivity via tcp over insecure port
      - IAM policy allow full administrative privileges
      - Primitive IAM roles should not be used
      - Internet exposed instances
      - IAM user has both Console access and Access Keys
      - S3 buckets are accessible to public
      - Access logging not enabled on all CloudTrail buckets
      - CloudTrail trail is not integrated with CloudWatch Log
      - Security Groups allow internet traffic to SSH port (22)
      - CloudTrail logs are not encrypted using Customer Master Keys (CMKs)
      - Excessive login failures
      - VPC endpoints were not used for consuming S3 storage from within the VPC
      - Access logging not enabled on S3 buckets
      - S3 buckets do not have server side encryption
      - Account hijacking attempts
      - Security groups allow internet traffic
      - VPC subnets should not allow automatic public IP assignment
      - VPC Flow Logs not enabled
      - MFA not enabled for IAM users
      - Inactive users for more than 30 days
      supportedModules: []
    - description: The policy label.
      name: policy-label
      supportedModules: []
    - description: The number of alerts to return.
      name: limit
      supportedModules: []
    - description: The policy compliance standard.
      name: policy-compliance-standard
      supportedModules: []
    - description: The cloud account ID.
      name: cloud-account-id
      supportedModules: []
    - description: The cloud account name.
      name: cloud-account
      supportedModules: []
    - description: The cloud region name.
      name: cloud-region
      supportedModules: []
    - description: The alert rule name.
      name: alert-rule-name
      supportedModules: []
    - description: The resource ID.
      name: resource-id
      supportedModules: []
    - description: The resource name.
      name: resource-name
      supportedModules: []
    - description: The resource type.
      name: resource-type
      supportedModules: []
    - auto: PREDEFINED
      defaultValue: open
      description: The alert status.
      name: alert-status
      predefined:
      - open
      - resolved
      - dismissed
      supportedModules: []
    - description: The alert ID.
      name: alert-id
      supportedModules: []
    - auto: PREDEFINED
      description: The cloud type.
      name: cloud-type
      predefined:
      - aws
      - azure
      - gcp
      supportedModules: []
    - auto: PREDEFINED
      deprecated: true
      description: The risk grade.
      name: risk-grade
      predefined:
      - A
      - B
      - C
      - F
      supportedModules: []
    - auto: PREDEFINED
      description: The policy type.
      name: policy-type
      predefined:
      - anomaly
      - audit_event
      - config
      - network
      supportedModules: []
    - auto: PREDEFINED
      description: The policy severity.
      name: policy-severity
      predefined:
      - high
      - medium
      - low
      supportedModules: []
    deprecated: true
    description: Search alerts on the Prisma Cloud platform. If no time-range arguments
      are given, the search will filter only alerts from the last 7 days.
    name: redlock-search-alerts
    outputs:
    - contextPath: PrismaCloud.AlertPageToken.nextPageToken
      description: Next page token.
      type: String
    - contextPath: PrismaCloud.Alert.id
      description: The ID of the returned alert.
      type: String
    - contextPath: PrismaCloud.Alert.status
      description: The status of the returned alert.
      type: String
    - contextPath: PrismaCloud.Alert.reason
      description: The reason for the returned alert.
      type: String
    - contextPath: PrismaCloud.Alert.lastSeen
      description: The time the returned alert was last seen.
      type: String
    - contextPath: PrismaCloud.Alert.firstSeen
      description: The time the returned alert was first seen.
      type: String
    - contextPath: PrismaCloud.Alert.lastUpdated
      description: The time the returned alert was last updated.
      type: String
    - contextPath: PrismaCloud.Alert.alertTime
      description: The time of the returned alert.
      type: String
    - contextPath: PrismaCloud.Alert.policy.policyId
      description: The policy ID of the returned alert.
      type: String
    - contextPath: PrismaCloud.Alert.policy.name
      description: The policy name of the returned alert.
      type: String
    - contextPath: PrismaCloud.Alert.policy.policyType
      description: The policy type of the returned alert.
      type: String
    - contextPath: PrismaCloud.Alert.policy.severity
      description: The policy severity of the returned alert.
      type: String
    - contextPath: PrismaCloud.Alert.policy.remediable
      description: Whether the policy is remediable.
      type: Boolean
    - contextPath: PrismaCloud.Alert.policy.description
      description: The policy description of the returned alert.
      type: String
    - contextPath: PrismaCloud.Alert.policy.recommendation
      description: The policy recommendation of the returned alert.
      type: String
    - contextPath: PrismaCloud.Alert.policy.remediation.description
      description: The policy remediation description of the returned alert.
      type: String
    - contextPath: PrismaCloud.Alert.policy.remediation.cliScriptTemplate
      description: The policy CLI script template description of the returned alert.
      type: String
    - contextPath: PrismaCloud.Alert.policy.systemDefault
      description: Whether the policy is the system default.
      type: Boolean
    - contextPath: PrismaCloud.Alert.policy.deleted
      description: Whether the policy was deleted.
      type: Boolean
    - contextPath: PrismaCloud.Alert.resource.resourceType
      description: The resource type of the returned alert.
      type: String
    - contextPath: PrismaCloud.Alert.resource.name
      description: The resource name of the returned alert.
      type: String
    - contextPath: PrismaCloud.Alert.resource.account
      description: The resource account of the returned alert.
      type: String
    - contextPath: PrismaCloud.Alert.resource.cloudType
      description: The resource cloud type of the returned alert.
      type: String
    - contextPath: PrismaCloud.Alert.resource.rrn
      description: The restricted resource name of the returned alert.
      type: String
    supportedModules: []
  - arguments:
    - default: true
      description: The alert ID.
      name: alert-id
      required: true
      supportedModules: []
    deprecated: true
    description: Gets the details of an alert based on the alert ID.
    name: redlock-get-alert-details
    supportedModules: []
  - arguments:
    - description: Comma-separated list of string IDs to be dismissed.
      name: alert-id
      supportedModules: []
    - description: The amount of time to snooze. Both snooze value and unit must be
        specified.
      name: snooze-value
      supportedModules: []
    - auto: PREDEFINED
      description: The time unit for snoozing alert.  Both snooze value and unit must
        be specified if snoozing.
      name: snooze-unit
      predefined:
      - hour
      - day
      - week
      - month
      - year
      - epoch
      supportedModules: []
    - description: Reason for dismissal.
      name: dismissal-note
      required: true
      supportedModules: []
    - description: Start time for the search in the following string format - MM/DD/YYYY.
      name: time-range-date-from
      supportedModules: []
    - description: End time for the search in the following format - MM/DD/YYYY.
      name: time-range-date-to
      supportedModules: []
    - description: The number of units to go back in time.
      name: time-range-value
      supportedModules: []
    - auto: PREDEFINED
      description: The search unit.
      name: time-range-unit
      predefined:
      - hour
      - day
      - week
      - month
      - year
      - login
      - epoch
      supportedModules: []
    - auto: PREDEFINED
      description: The policy name.
      name: policy-name
      predefined:
      - IAM password policy does not have a symbol
      - IAM password policy does not expire in 90 days
      - IAM password policy does not have a lowercase character
      - IAM password policy does not have a minimum of 14 characters
      - IAM password policy allows password reuse
      - Default Security Group does not restrict all traffic
      - IAM password policy does not have password expiration period
      - IAM password policy does not exist
      - Access keys are not rotated for 90 days
      - Security Groups allow internet traffic from internet to RDP port (3389)
      - Internet connectivity via tcp over insecure port
      - IAM policy allow full administrative privileges
      - Primitive IAM roles should not be used
      - Internet exposed instances
      - IAM user has both Console access and Access Keys
      - S3 buckets are accessible to public
      - Access logging not enabled on all CloudTrail buckets
      - CloudTrail trail is not integrated with CloudWatch Log
      - Security Groups allow internet traffic to SSH port (22)
      - CloudTrail logs are not encrypted using Customer Master Keys (CMKs)
      - Excessive login failures
      - VPC endpoints were not used for consuming S3 storage from within the VPC
      - Access logging not enabled on S3 buckets
      - S3 buckets do not have server side encryption
      - Account hijacking attempts
      - Security groups allow internet traffic
      - VPC subnets should not allow automatic public IP assignment
      - VPC Flow Logs not enabled
      - MFA not enabled for IAM users
      - Inactive users for more than 30 days
      supportedModules: []
    - description: The policy label.
      name: policy-label
      supportedModules: []
    - description: The policy compliance standard.
      name: policy-compliance-standard
      supportedModules: []
    - description: The cloud account.
      name: cloud-account
      supportedModules: []
    - description: The cloud region.
      name: cloud-region
      supportedModules: []
    - description: The alert rule name.
      name: alert-rule-name
      supportedModules: []
    - description: The resource ID.
      name: resource-id
      supportedModules: []
    - description: The resource name.
      name: resource-name
      supportedModules: []
    - description: The resource type.
      name: resource-type
      supportedModules: []
    - auto: PREDEFINED
      description: The alert status.
      name: alert-status
      predefined:
      - open
      - resolved
      - dismissed
      supportedModules: []
    - auto: PREDEFINED
      description: The cloud type.
      name: cloud-type
      predefined:
      - aws
      - azure
      - gcp
      supportedModules: []
    - auto: PREDEFINED
      deprecated: true
      description: The risk grade.
      name: risk-grade
      predefined:
      - A
      - B
      - C
      - F
      supportedModules: []
    - auto: PREDEFINED
      description: The policy type.
      name: policy-type
      predefined:
      - anomaly
      - audit_event
      - config
      - network
      supportedModules: []
    - auto: PREDEFINED
      description: The policy severity.
      name: policy-severity
      predefined:
      - high
      - medium
      - low
      supportedModules: []
    - description: Comma-separated string of policy IDs.
      isArray: true
      name: policy-id
      supportedModules: []
    deprecated: true
    description: Dismiss the alerts matching the given filter. Must provide either
      policy IDs or alert IDs.
    execution: true
    name: redlock-dismiss-alerts
    supportedModules: []
  - arguments:
    - description: The IDs of alerts to reopen.
      name: alert-id
      supportedModules: []
    - description: Start time for search in the following string format -  MM/DD/YYYY.
      name: time-range-date-from
      supportedModules: []
    - description: End time for search in the following format -  MM/DD/YYYY.
      name: time-range-date-to
      supportedModules: []
    - description: The number of units to go back in time.
      name: time-range-value
      supportedModules: []
    - auto: PREDEFINED
      description: The search unit.
      name: time-range-unit
      predefined:
      - hour
      - day
      - week
      - month
      - year
      - login
      - epoch
      supportedModules: []
    - auto: PREDEFINED
      description: The policy name.
      name: policy-name
      predefined:
      - IAM password policy does not have a symbol
      - IAM password policy does not expire in 90 days
      - IAM password policy does not have a lowercase character
      - IAM password policy does not have a minimum of 14 characters
      - IAM password policy allows password reuse
      - Default Security Group does not restrict all traffic
      - IAM password policy does not have password expiration period
      - IAM password policy does not exist
      - Access keys are not rotated for 90 days
      - Security Groups allow internet traffic from internet to RDP port (3389)
      - Internet connectivity via tcp over insecure port
      - IAM policy allow full administrative privileges
      - Primitive IAM roles should not be used
      - Internet exposed instances
      - IAM user has both Console access and Access Keys
      - S3 buckets are accessible to public
      - Access logging not enabled on all CloudTrail buckets
      - CloudTrail trail is not integrated with CloudWatch Log
      - Security Groups allow internet traffic to SSH port (22)
      - CloudTrail logs are not encrypted using Customer Master Keys (CMKs)
      - Excessive login failures
      - VPC endpoints were not used for consuming S3 storage from within the VPC
      - Access logging not enabled on S3 buckets
      - S3 buckets do not have server side encryption
      - Account hijacking attempts
      - Security groups allow internet traffic
      - VPC subnets should not allow automatic public IP assignment
      - VPC Flow Logs not enabled
      - MFA not enabled for IAM users
      - Inactive users for more than 30 days
      supportedModules: []
    - description: The policy label.
      name: policy-label
      supportedModules: []
    - description: The policy compliance standard.
      name: policy-compliance-standard
      supportedModules: []
    - description: The cloud account.
      name: cloud-account
      supportedModules: []
    - description: The cloud region.
      name: cloud-region
      supportedModules: []
    - description: The alert rule name.
      name: alert-rule-name
      supportedModules: []
    - description: The resource ID.
      name: resource-id
      supportedModules: []
    - description: The resource name.
      name: resource-name
      supportedModules: []
    - description: The resource type.
      name: resource-type
      supportedModules: []
    - auto: PREDEFINED
      description: The alert status.
      name: alert-status
      predefined:
      - open
      - resolved
      - dismissed
      supportedModules: []
    - auto: PREDEFINED
      description: The cloud type.
      name: cloud-type
      predefined:
      - aws
      - azure
      - gcp
      supportedModules: []
    - auto: PREDEFINED
      deprecated: true
      description: The risk grade.
      name: risk-grade
      predefined:
      - A
      - B
      - C
      - F
      supportedModules: []
    - auto: PREDEFINED
      description: The policy type.
      name: policy-type
      predefined:
      - anomaly
      - audit_event
      - config
      - network
      supportedModules: []
    - auto: PREDEFINED
      description: The policy severity.
      name: policy-severity
      predefined:
      - high
      - medium
      - low
      supportedModules: []
    deprecated: true
    description: Re-open the alerts matching the given filter. Must provide either
      policy IDs or alert IDs.
    execution: true
    name: redlock-reopen-alerts
    supportedModules: []
  - arguments:
    - description: The alert ID for which to get remediation details.
      name: alert-id
      supportedModules: []
    deprecated: true
    description: Gets remediation details for the given alert.
    name: redlock-get-remediation-details
    supportedModules: []
  - arguments: []
    deprecated: true
    description: List the acceptable filters and values for alerts.
    name: redlock-list-alert-filters
    supportedModules: []
  - arguments:
    - description: Start time for the search, in the following string format -  MM/DD/YYYY.
      name: time-range-date-from
      supportedModules: []
    - description: End time for the search, in the following format -  MM/DD/YYYY.
      name: time-range-date-to
      supportedModules: []
    - description: The number of time range value units for the search. For example,
        3 days, 5 weeks, etc.
      name: time-range-value
      supportedModules: []
    - auto: PREDEFINED
      description: |-
        The search unit. The "login" and "epoch" options are only available if timeRangeValue
        is not provided.
      name: time-range-unit
      predefined:
      - hour
      - day
      - week
      - month
      - year
      - login
      - epoch
      supportedModules: []
    - description: Query to run in Prisma Cloud config API using RQL language.
      name: query
      required: true
      supportedModules: []
    - defaultValue: "100"
      description: Maximum number of entries to return.
      name: limit
      supportedModules: []
    deprecated: true
    description: Search the configuration inventory on the Prisma Cloud platform using
      RQL language.
    name: redlock-search-config
    outputs:
    - contextPath: PrismaCloud.Config.accountId
      description: Cloud account ID.
      type: String
    - contextPath: PrismaCloud.Config.accountName
      description: Cloud account name.
      type: String
    - contextPath: PrismaCloud.Config.allowDrillDown
      description: Whether to allow drill down.
      type: Boolean
    - contextPath: PrismaCloud.Config.cloudType
      description: Cloud type.
      type: String
    - contextPath: PrismaCloud.Config.deleted
      description: Whether the asset was deleted.
      type: Boolean
    - contextPath: PrismaCloud.Config.hasExtFindingRiskFactors
      description: Whether the configuration has external finding risk factors.
      type: Boolean
    - contextPath: PrismaCloud.Config.hasExternalFinding
      description: Whether the configuration has an external finding.
      type: Boolean
    - contextPath: PrismaCloud.Config.hasExternalIntegration
      description: Whether the configuration has an external integration.
      type: Boolean
    - contextPath: PrismaCloud.Config.hasNetwork
      description: Whether the configuration has a network.
      type: Boolean
    - contextPath: PrismaCloud.Config.id
      description: Prisma Cloud configuration ID.
      type: String
    - contextPath: PrismaCloud.Config.assetId
      description: Prisma Cloud asset ID.
      type: String
    - contextPath: PrismaCloud.Config.data
      description: Prisma Cloud asset specific data.
      type: Unknown
    - contextPath: PrismaCloud.Config.insertTs
      description: Insert timestamp.
      type: Date
    - contextPath: PrismaCloud.Config.createdTs
      description: Created timestamp.
      type: Date
    - contextPath: PrismaCloud.Config.name
      description: Asset name.
      type: String
    - contextPath: PrismaCloud.Config.regionId
      description: Cloud region ID.
      type: String
    - contextPath: PrismaCloud.Config.regionName
      description: Cloud region Name.
      type: String
    - contextPath: PrismaCloud.Config.resourceType
      description: Cloud resource type.
      type: String
    - contextPath: PrismaCloud.Config.rrn
      description: Cloud restricted resource name.
      type: String
    - contextPath: PrismaCloud.Config.service
      description: Cloud service.
      type: String
    - contextPath: PrismaCloud.Config.stateId
      description: State ID.
      type: String
    supportedModules: []
  - arguments:
    - description: The maximum number of records to return. '; limit search records
        to {}' is appended to every query where {} is the maximum number of records
        to return. If no value is provided, only 1 record is returned.
      name: limit
      supportedModules: []
    - description: 'The RQL config query to run. Examples of RQL config queries can
        be found here: https://docs.paloaltonetworks.com/prisma/prisma-cloud/prisma-cloud-rql-reference/rql-reference/config-query/config-query-examples.html.
        Note that ` limit search records to 1` is automatically appended to each query
        and a `;` may need to be added to the end of the RQL input to make the entire
        query valid.  The limit parameter adjusts this to be a value other than 1.'
      name: rql
      required: true
      supportedModules: []
    deprecated: true
    description: Returns the results of an RQL config query.
    name: redlock-get-rql-response
    supportedModules: []
  - arguments:
    - description: Start time for the search, in the following format -  MM/DD/YYYY.
      name: time-range-date-from
      supportedModules: []
    - description: End time for the search, in the following format -  MM/DD/YYYY.
      name: time-range-date-to
      supportedModules: []
    - description: The number of time range value units for the search. For example,
        3 days, 5 weeks, etc.
      name: time-range-value
      supportedModules: []
    - auto: PREDEFINED
      description: |-
        The search unit. The "login" and "epoch" options are only available if timeRangeValue
        is not provided.
      name: time-range-unit
      predefined:
      - hour
      - day
      - week
      - month
      - year
      - login
      - epoch
      supportedModules: []
    - description: Query to run in Prisma Cloud search API using RQL language.
      name: query
      required: true
      supportedModules: []
    - defaultValue: "100"
      description: Maximum number of entries to return.
      name: limit
      supportedModules: []
    deprecated: true
    description: Search events on the Prisma Cloud platform using RQL language.
    name: redlock-search-event
    supportedModules: []
  - arguments:
    - description: Start time for the search, in the following format -  MM/DD/YYYY.
      name: time-range-date-from
      supportedModules: []
    - description: End time for the search, in the following format -  MM/DD/YYYY.
      name: time-range-date-to
      supportedModules: []
    - description: The number of time range value units for the search. For example,
        3 days, 5 weeks, etc.
      name: time-range-value
      supportedModules: []
    - auto: PREDEFINED
      description: |-
        The search unit. The "login" and "epoch" options are only available if timeRangeValue
        is not provided.
      name: time-range-unit
      predefined:
      - hour
      - day
      - week
      - month
      - year
      - login
      - epoch
      supportedModules: []
    - description: Query to run in Prisma Cloud search API using RQL language.
      name: query
      required: true
      supportedModules: []
    - auto: PREDEFINED
      description: The cloud in which the network should be searched.
      name: cloud-type
      predefined:
      - aws
      - azure
      - gcp
      - alibaba_cloud
      - oci
      supportedModules: []
    deprecated: true
    description: Search networks on the Prisma Cloud platform using RQL language.
    name: redlock-search-network
    supportedModules: []
  - arguments:
    - description: Access key name.
      name: name
      required: true
      supportedModules: []
    - description: Timestamp in milliseconds when access key expires. Default:0.
      name: expires-on
      supportedModules: []
    description: Adds a new access key for the current user. If you have API access,
      you can create up to two access keys.
    name: prisma-cloud-access-key-create
    outputs:
    - contextPath: PrismaCloud.AccessKeys.id
      description: Access key ID.
      type: String
    - contextPath: PrismaCloud.AccessKeys.secretKey
      description: Access key secret.
      type: String
    supportedModules: []
  - arguments:
    - description: Returns the metadata of the access key that has the specified ID.
      name: access-key
      supportedModules: []
    - description: Maximum number of entries to return.
      name: limit
      supportedModules: []
    description: Returns all access keys for your tenant if you have a Prisma Cloud
      System Admin role. Returns just your access keys if you don't have this role.
    name: prisma-cloud-access-keys-list
    outputs:
    - contextPath: PrismaCloud.AccessKeys.id
      description: Access key ID.
      type: String
    - contextPath: PrismaCloud.AccessKeys.createdBy
      description: The user who created the access key.
      type: String
    - contextPath: PrismaCloud.AccessKeys.createdTs
      description: Time access key was created.
      type: Number
    - contextPath: PrismaCloud.AccessKeys.expiresOn
      description: The time the access key expires.
      type: Number
    - contextPath: PrismaCloud.AccessKeys.lastUsedTime
      description: The last time the access key was used.
      type: Number
    - contextPath: PrismaCloud.AccessKeys.name
      description: The name of the access key.
      type: String
    - contextPath: PrismaCloud.AccessKeys.role
      description: Access key user role.
      type: Object
    - contextPath: PrismaCloud.AccessKeys.roleType
      description: User role permission type.
      type: String
    - contextPath: PrismaCloud.AccessKeys.status
      description: Access key status.
      type: String
    - contextPath: PrismaCloud.AccessKeys.username
      description: Access key user name.
      type: String
    supportedModules: []
  - arguments:
    - description: Access key ID.
      name: access-key
      required: true
      supportedModules: []
    description: Deactivates the access key that has the specified ID.
    name: prisma-cloud-access-key-disable
    supportedModules: []
  - arguments:
    - description: Access key ID.
      name: access-key
      required: true
      supportedModules: []
    description: Activates the access key that has the specified ID. You cannot activate
      an expired access key.
    name: prisma-cloud-access-key-enable
    supportedModules: []
  - arguments:
    - description: Access key ID.
      name: access-key
      required: true
      supportedModules: []
    description: Deletes the access key that has the specified ID.
    name: prisma-cloud-access-key-delete
    supportedModules: []
  - arguments:
    - description: Unified Asset ID or RRN (Restricted Resource Name).
      name: asset_id
      required: true
      supportedModules: []
    - auto: PREDEFINED
      description: External finding types.
      name: finding_type
      predefined:
      - COMPLIANCE_CIS
      - GUARD_DUTY_HOST
      - GUARD_DUTY_IAM
      - INSPECTOR_RBA
      - INSPECTOR_SBP
      - NETWORK_REACHABILITY
      - AZURE_SECURITY_CENTER_ALERTS
      - UNCLASSIFIED
      - COMMAND_AND_CONTROL
      - CREDENTIAL_ACCESS
      - CROSS_ACCOUNT_TRUST
      - DATA_EXFILTRATION
      - DEFENSE_EVASION
      - DISCOVERY
      - HIGH_PRIVILEGED_ROLE
      - INITIAL_ACCESS
      - INTERNET_EXPOSURE
      - KEYS_AND_SECRETS
      - LATERAL_MOVEMENT
      - MALWARE
      - MFA
      - MISCONFIGURATION
      - NETWORK_ANOMALY
      - PRIVILEGE_ESCALATION
      - RECONNAISSANCE
      - RESOURCE_HIJACKING
      - SSH_BRUTE_FORCE
      - UNAUTHORIZED_ACCESS
      - UNENCRYPTED_DATA
      - UNUSED_PRIVILEGES
      - USER_ANOMALY
      - WEAK_PASSWORD
      - SENSITIVE_DATA_EXPOSURE
      - INJECTIONS
      - VULNERABILITY_SCANNING
      - SHELLSHOCK
      - KNOWN_BOTS
      - UNKNOWN_BOTS
      - VIRTUAL_PATCHES
      - WEB_ATTACK
      - BOT_ACTIVITY
      - WEB_SCRAPING
      - CUSTOM
      - VULNERABILITY
      - HOST_VULNERABILITY_CVE
      - CONTAINER_IMAGE_VULNERABILITY_CVE
      - VIRTUAL_IMAGE_VULNERABILITY_CVE
      - SERVERLESS_VULNERABILITY
      - PACKAGE_VULNERABILITY
      supportedModules: []
    - auto: PREDEFINED
      description: Comma-separated list of external finding risk factors.
      isArray: true
      name: risk_factors
      predefined:
      - CRITICAL_SEVERITY
      - HIGH_SEVERITY
      - MEDIUM_SEVERITY
      - HAS_FIX
      - REMOTE_EXECUTION
      - DOS
      - RECENT_VULNERABILITY
      - EXPLOIT_EXISTS
      - ATTACK_COMPLEXITY_LOW
      - ATTACK_VECTOR_NETWORK
      - REACHABLE_FROM_THE_INTERNET
      - LISTENING_PORTS
      - CONTAINER_IS_RUNNING_AS_ROOT
      - NO_MANDATORY_SECURITY_PROFILE_APPLIED
      - RUNNING_AS_PRIVILEGED_CONTAINER
      - PACKAGE_IN_USE
      - DOS_LOW
      - DOS_HIGH
      - EXPLOIT_EXISTS_IN_THE_WILD
      - EXPLOIT_EXISTS_POC
      - SENSITIVE_INFORMATION
      - ROOT_MOUNT
      - RUNTIME_SOCKET
      - HOST_ACCESS
      supportedModules: []
    - description: Timeline item ID.
      name: timeline_item_id
      supportedModules: []
    - description: Comma-separated list of alert IDs.
      isArray: true
      name: alert_ids
      supportedModules: []
    - description: Limit number of records.
      name: limit
      supportedModules: []
    - description: Permissions type.
      name: permission_type
      supportedModules: []
    - description: Next page token.
      name: page_token
      supportedModules: []
    - auto: PREDEFINED
      description: Filter Hyperion findings.
      name: prisma_cloud_findings_only
      predefined:
      - "true"
      - "false"
      supportedModules: []
    - description: CVE ID, Package ID, or Layer ID. The value needs to be fetched
        from one of the groups by API calls.
      name: vulnerability_info_type_id
      supportedModules: []
    - auto: PREDEFINED
      description: Supported vulnerability types.
      name: vulnerability_info_type
      predefined:
      - CVE
      - PACKAGE
      - LAYER
      supportedModules: []
    description: Returns detailed information for the asset with the given ID.
    name: prisma-cloud-asset-get
    outputs:
    - contextPath: PrismaCloud.Asset.problem.title
      description: The title of the problem.
      type: String
    - contextPath: PrismaCloud.Asset.problem.status.statusCode
      description: The status code of the problem.
      type: Number
    - contextPath: PrismaCloud.Asset.problem.status.reasonPhrase
      description: The reason for the problem.
      type: String
    - contextPath: PrismaCloud.Asset.problem.detail
      description: The details of the problem.
      type: String
    - contextPath: PrismaCloud.Asset.problem.instance
      description: The instance of the problem.
      type: String
    - contextPath: PrismaCloud.Asset.problem.type
      description: The type of the problem.
      type: String
    - contextPath: PrismaCloud.Asset.problem.parameters
      description: The parameters of the problem.
      type: Unknown
    - contextPath: PrismaCloud.Asset.id
      description: The ID of the asset.
      type: String
    - contextPath: PrismaCloud.Asset.prismaId
      description: The Prisma ID of the asset.
      type: String
    - contextPath: PrismaCloud.Asset.appName
      description: The app name of the asset.
      type: String
    - contextPath: PrismaCloud.Asset.externalAssetId
      description: The external asset ID of the asset.
      type: String
    - contextPath: PrismaCloud.Asset.assetTypeId
      description: The asset type ID of the asset.
      type: Number
    - contextPath: PrismaCloud.Asset.cloudType
      description: The cloud type of the asset.
      type: String
    - contextPath: PrismaCloud.Asset.createdTs
      description: The creation timestamp of the asset.
      type: Number
    - contextPath: PrismaCloud.Asset.insertTs
      description: The insertion timestamp of the asset.
      type: Number
    - contextPath: PrismaCloud.Asset.data
      description: The data of the asset.
      type: Unknown
    - contextPath: PrismaCloud.Asset.dynamicData
      description: The dynamic data of the asset.
      type: Unknown
    - contextPath: PrismaCloud.Asset.riskGrade
      description: The risk grade of the asset.
      type: String
    - contextPath: PrismaCloud.Asset.stateId
      description: The state ID of the asset.
      type: String
    - contextPath: PrismaCloud.Asset.name
      description: The name of the asset.
      type: String
    - contextPath: PrismaCloud.Asset.regionId
      description: The region ID of the asset.
      type: String
    - contextPath: PrismaCloud.Asset.regionName
      description: The region name of the asset.
      type: String
    - contextPath: PrismaCloud.Asset.altAssetId
      description: The alternative asset ID of the asset.
      type: String
    - contextPath: PrismaCloud.Asset.url
      description: The URL of the asset.
      type: String
    - contextPath: PrismaCloud.Asset.vpcId
      description: The VPC ID of the asset.
      type: String
    - contextPath: PrismaCloud.Asset.vpcExternalAssetId
      description: The VPC external asset ID of the asset.
      type: String
    - contextPath: PrismaCloud.Asset.vpcName
      description: The VPC name of the asset.
      type: String
    - contextPath: PrismaCloud.Asset.tags
      description: The tags of the asset.
      type: Unknown
    - contextPath: PrismaCloud.Asset.relationshipCounts
      description: The relationship counts of the asset.
      type: Number
    - contextPath: PrismaCloud.Asset.vulnerabilityCounts
      description: The vulnerability counts of the asset.
      type: Unknown
    - contextPath: PrismaCloud.Asset.accountName
      description: The account name of the asset.
      type: String
    - contextPath: PrismaCloud.Asset.assetType
      description: The account type of the asset.
      type: String
    - contextPath: PrismaCloud.Asset.serviceName
      description: The service name of the asset.
      type: String
    - contextPath: PrismaCloud.Asset.resourceType
      description: The resource type of the asset.
      type: String
    - contextPath: PrismaCloud.Asset.deleted
      description: The deletion state of the asset.
      type: Boolean
    - contextPath: PrismaCloud.Asset.assetClassId
      description: The asset class ID of the asset.
      type: String
    - contextPath: PrismaCloud.Asset.assetClass
      description: The asset class of the asset.
      type: String
    - contextPath: PrismaCloud.Asset.attributes
      description: The attributes of the asset.
      type: Unknown
    - contextPath: PrismaCloud.Asset.alertsCount.severity
      description: The severity of the alerts of the asset.
      type: String
    - contextPath: PrismaCloud.Asset.alertsCount.count
      description: The alert count of the asset.
      type: Number
    - contextPath: PrismaCloud.Asset.ipAddresses
      description: The IP addresses of the asset.
      type: String
    - contextPath: PrismaCloud.Asset.trueInternetExposure
      description: The internet exposure state of the asset.
      type: String
    - contextPath: PrismaCloud.Asset.dataSecurity
      description: The data security of the asset.
      type: Unknown
    supportedModules: []
  - arguments:
    - description: Unified asset ID or RRN (Restricted Resource Name).
      name: asset_id
      required: true
      supportedModules: []
    - auto: PREDEFINED
      defaultValue: asset
      description: Asset domain service query type.
      name: type
      predefined:
      - external_finding
      - asset
      - asset_lite
      - alerts
      - alerts_counts
      - alert_summary
      - finding_summary
      - attack_path
      - attack_path_summary
      - raw_config
      - network
      - timeline
      - external_integration
      - asset_data_indicators
      - relationships
      - findings
      - vulnerabilities
      - permissions
      - package_info
      - labels
      - vulnerability_aggregates
      - process_info
      - vulnerabilities_group_by_type
      - asset_cwp_vulns
      - app_contexts
      - attributes
      - data_security
      - data_security_summary
      required: true
      supportedModules: []
    - auto: PREDEFINED
      description: External finding types.
      name: finding_type
      predefined:
      - COMPLIANCE_CIS
      - GUARD_DUTY_HOST
      - GUARD_DUTY_IAM
      - INSPECTOR_RBA
      - INSPECTOR_SBP
      - NETWORK_REACHABILITY
      - AZURE_SECURITY_CENTER_ALERTS
      - UNCLASSIFIED
      - COMMAND_AND_CONTROL
      - CREDENTIAL_ACCESS
      - CROSS_ACCOUNT_TRUST
      - DATA_EXFILTRATION
      - DEFENSE_EVASION
      - DISCOVERY
      - HIGH_PRIVILEGED_ROLE
      - INITIAL_ACCESS
      - INTERNET_EXPOSURE
      - KEYS_AND_SECRETS
      - LATERAL_MOVEMENT
      - MALWARE
      - MFA
      - MISCONFIGURATION
      - NETWORK_ANOMALY
      - PRIVILEGE_ESCALATION
      - RECONNAISSANCE
      - RESOURCE_HIJACKING
      - SSH_BRUTE_FORCE
      - UNAUTHORIZED_ACCESS
      - UNENCRYPTED_DATA
      - UNUSED_PRIVILEGES
      - USER_ANOMALY
      - WEAK_PASSWORD
      - SENSITIVE_DATA_EXPOSURE
      - INJECTIONS
      - VULNERABILITY_SCANNING
      - SHELLSHOCK
      - KNOWN_BOTS
      - UNKNOWN_BOTS
      - VIRTUAL_PATCHES
      - WEB_ATTACK
      - BOT_ACTIVITY
      - WEB_SCRAPING
      - CUSTOM
      - VULNERABILITY
      - HOST_VULNERABILITY_CVE
      - CONTAINER_IMAGE_VULNERABILITY_CVE
      - VIRTUAL_IMAGE_VULNERABILITY_CVE
      - SERVERLESS_VULNERABILITY
      - PACKAGE_VULNERABILITY
      supportedModules: []
    - auto: PREDEFINED
      description: Comma-separated list of external finding risk factors.
      isArray: true
      name: risk_factors
      predefined:
      - CRITICAL_SEVERITY
      - HIGH_SEVERITY
      - MEDIUM_SEVERITY
      - HAS_FIX
      - REMOTE_EXECUTION
      - DOS
      - RECENT_VULNERABILITY
      - EXPLOIT_EXISTS
      - ATTACK_COMPLEXITY_LOW
      - ATTACK_VECTOR_NETWORK
      - REACHABLE_FROM_THE_INTERNET
      - LISTENING_PORTS
      - CONTAINER_IS_RUNNING_AS_ROOT
      - NO_MANDATORY_SECURITY_PROFILE_APPLIED
      - RUNNING_AS_PRIVILEGED_CONTAINER
      - PACKAGE_IN_USE
      - DOS_LOW
      - DOS_HIGH
      - EXPLOIT_EXISTS_IN_THE_WILD
      - EXPLOIT_EXISTS_POC
      - SENSITIVE_INFORMATION
      - ROOT_MOUNT
      - RUNTIME_SOCKET
      - HOST_ACCESS
      supportedModules: []
    - description: Timeline item ID.
      name: timeline_item_id
      supportedModules: []
    - description: Comma-separated list of alert IDs.
      isArray: true
      name: alert_ids
      supportedModules: []
    - description: Limit number of records.
      name: limit
      supportedModules: []
    - description: Permissions type.
      name: permission_type
      supportedModules: []
    - description: Next page token.
      name: page_token
      supportedModules: []
    - auto: PREDEFINED
      description: Filter Hyperion findings.
      name: prisma_cloud_findings_only
      predefined:
      - "true"
      - "false"
      supportedModules: []
    - description: CVE ID, package ID, or layer ID. The value needs to be fetched
        from one of the group by API calls.
      name: vulnerability_info_type_id
      supportedModules: []
    - auto: PREDEFINED
      description: Supported vulnerability types.
      name: vulnerability_info_type
      predefined:
      - CVE
      - PACKAGE
      - LAYER
      supportedModules: []
    description: Returns detailed information for the asset with the given ID.
    name: prisma-cloud-asset-generic-get
    supportedModules: []
  - arguments:
    - description: Unified asset ID or RRN (Restricted Resource Name).
      name: asset_id
      required: true
      supportedModules: []
    - auto: PREDEFINED
      description: External finding types.
      name: finding_type
      predefined:
      - COMPLIANCE_CIS
      - GUARD_DUTY_HOST
      - GUARD_DUTY_IAM
      - INSPECTOR_RBA
      - INSPECTOR_SBP
      - NETWORK_REACHABILITY
      - AZURE_SECURITY_CENTER_ALERTS
      - UNCLASSIFIED
      - COMMAND_AND_CONTROL
      - CREDENTIAL_ACCESS
      - CROSS_ACCOUNT_TRUST
      - DATA_EXFILTRATION
      - DEFENSE_EVASION
      - DISCOVERY
      - HIGH_PRIVILEGED_ROLE
      - INITIAL_ACCESS
      - INTERNET_EXPOSURE
      - KEYS_AND_SECRETS
      - LATERAL_MOVEMENT
      - MALWARE
      - MFA
      - MISCONFIGURATION
      - NETWORK_ANOMALY
      - PRIVILEGE_ESCALATION
      - RECONNAISSANCE
      - RESOURCE_HIJACKING
      - SSH_BRUTE_FORCE
      - UNAUTHORIZED_ACCESS
      - UNENCRYPTED_DATA
      - UNUSED_PRIVILEGES
      - USER_ANOMALY
      - WEAK_PASSWORD
      - SENSITIVE_DATA_EXPOSURE
      - INJECTIONS
      - VULNERABILITY_SCANNING
      - SHELLSHOCK
      - KNOWN_BOTS
      - UNKNOWN_BOTS
      - VIRTUAL_PATCHES
      - WEB_ATTACK
      - BOT_ACTIVITY
      - WEB_SCRAPING
      - CUSTOM
      - VULNERABILITY
      - HOST_VULNERABILITY_CVE
      - CONTAINER_IMAGE_VULNERABILITY_CVE
      - VIRTUAL_IMAGE_VULNERABILITY_CVE
      - SERVERLESS_VULNERABILITY
      - PACKAGE_VULNERABILITY
      supportedModules: []
    - auto: PREDEFINED
      description: Comma-separated list of external finding risk factors.
      isArray: true
      name: risk_factors
      predefined:
      - CRITICAL_SEVERITY
      - HIGH_SEVERITY
      - MEDIUM_SEVERITY
      - HAS_FIX
      - REMOTE_EXECUTION
      - DOS
      - RECENT_VULNERABILITY
      - EXPLOIT_EXISTS
      - ATTACK_COMPLEXITY_LOW
      - ATTACK_VECTOR_NETWORK
      - REACHABLE_FROM_THE_INTERNET
      - LISTENING_PORTS
      - CONTAINER_IS_RUNNING_AS_ROOT
      - NO_MANDATORY_SECURITY_PROFILE_APPLIED
      - RUNNING_AS_PRIVILEGED_CONTAINER
      - PACKAGE_IN_USE
      - DOS_LOW
      - DOS_HIGH
      - EXPLOIT_EXISTS_IN_THE_WILD
      - EXPLOIT_EXISTS_POC
      - SENSITIVE_INFORMATION
      - ROOT_MOUNT
      - RUNTIME_SOCKET
      - HOST_ACCESS
      supportedModules: []
    - description: Timeline item ID.
      name: timeline_item_id
      supportedModules: []
    - description: Comma-separated list of alert IDs.
      isArray: true
      name: alert_ids
      supportedModules: []
    - description: Limit number of records.
      name: limit
      supportedModules: []
    - description: Permissions type.
      name: permission_type
      supportedModules: []
    - description: Next page token.
      name: page_token
      supportedModules: []
    - auto: PREDEFINED
      description: Filter Hyperion findings.
      name: prisma_cloud_findings_only
      predefined:
      - "true"
      - "false"
      supportedModules: []
    - description: CVE ID, package ID, or layer ID. The value needs to be fetched
        from one of the group by API calls.
      name: vulnerability_info_type_id
      supportedModules: []
    - auto: PREDEFINED
      description: Supported vulnerability types.
      name: vulnerability_info_type
      predefined:
      - CVE
      - PACKAGE
      - LAYER
      supportedModules: []
    description: Returns detailed information for the asset findings with the given
      ID.
    name: prisma-cloud-asset-findings-get
    outputs:
    - contextPath: PrismaCloud.AssetFindings.problem.title
      description: The title of the problem.
      type: String
    - contextPath: PrismaCloud.AssetFindings.problem.status.statusCode
      description: The status code of the problem.
      type: Number
    - contextPath: PrismaCloud.AssetFindings.problem.status.reasonPhrase
      description: The reason for the problem.
      type: String
    - contextPath: PrismaCloud.AssetFindings.problem.detail
      description: The details of the problem.
      type: String
    - contextPath: PrismaCloud.AssetFindings.problem.instance
      description: The instance of the problem.
      type: String
    - contextPath: PrismaCloud.AssetFindings.problem.type
      description: The type of the problem.
      type: String
    - contextPath: PrismaCloud.AssetFindings.problem.parameters
      description: The parameters of the problem.
      type: Unknown
    - contextPath: PrismaCloud.AssetFindings.accountId
      description: The account ID of the asset findings.
      type: String
    - contextPath: PrismaCloud.AssetFindings.count
      description: The count of asset findings.
      type: String
    - contextPath: PrismaCloud.AssetFindings.createdOn
      description: The created on of the asset findings.
      type: Number
    - contextPath: PrismaCloud.AssetFindings.customerId
      description: The customer ID of the asset findings.
      type: Number
    - contextPath: PrismaCloud.AssetFindings.cveId
      description: The CVE ID of the asset findings.
      type: String
    - contextPath: PrismaCloud.AssetFindings.description
      description: The description of the asset findings.
      type: String
    - contextPath: PrismaCloud.AssetFindings.externalId
      description: The external ID of the asset findings.
      type: Number
    - contextPath: PrismaCloud.AssetFindings.id
      description: The ID of the asset findings.
      type: String
    - contextPath: PrismaCloud.AssetFindings.normalizedName
      description: The normalized name of the asset findings.
      type: String
    - contextPath: PrismaCloud.AssetFindings.normalizedNames
      description: The normalized names of the asset findings.
      type: String
    - contextPath: PrismaCloud.AssetFindings.nvdUrl
      description: The nvd URL of the asset findings.
      type: String
    - contextPath: PrismaCloud.AssetFindings.link
      description: The link of the asset findings.
      type: String
    - contextPath: PrismaCloud.AssetFindings.rawData
      description: The raw data of the asset findings.
      type: String
    - contextPath: PrismaCloud.AssetFindings.regionId
      description: The region ID of the asset findings.
      type: String
    - contextPath: PrismaCloud.AssetFindings.resourceCloudId
      description: The resource cloud ID of the asset findings.
      type: String
    - contextPath: PrismaCloud.AssetFindings.resourceId
      description: The resource ID of the asset findings.
      type: Number
    - contextPath: PrismaCloud.AssetFindings.resourceUrl
      description: The resource URL of the asset findings.
      type: String
    - contextPath: PrismaCloud.AssetFindings.riskFactors
      description: The risk factors of the asset findings.
      type: String
    - contextPath: PrismaCloud.AssetFindings.rlUpdatedOn
      description: The rl updated on of the asset findings.
      type: Number
    - contextPath: PrismaCloud.AssetFindings.scanId
      description: The scan ID of the asset findings.
      type: String
    - contextPath: PrismaCloud.AssetFindings.score
      description: The score of the asset findings.
      type: String
    - contextPath: PrismaCloud.AssetFindings.severity
      description: The severity of the asset findings.
      type: String
    - contextPath: PrismaCloud.AssetFindings.source
      description: The source of the asset findings.
      type: String
    - contextPath: PrismaCloud.AssetFindings.sourceData
      description: The source data of the asset findings.
      type: Unknown
    - contextPath: PrismaCloud.AssetFindings.status
      description: The status of the asset findings.
      type: String
    - contextPath: PrismaCloud.AssetFindings.title
      description: The title of the asset findings.
      type: String
    - contextPath: PrismaCloud.AssetFindings.type
      description: The type of the asset findings.
      type: String
    - contextPath: PrismaCloud.AssetFindings.packageName
      description: The package name of the asset findings.
      type: String
    - contextPath: PrismaCloud.AssetFindings.updatedOn
      description: The updated on of the asset findings.
      type: Number
    supportedModules: []
  - arguments:
    - description: Unified asset ID or RRN (Restricted Resource Name).
      name: asset_id
      required: true
      supportedModules: []
    - auto: PREDEFINED
      description: External finding types.
      name: finding_type
      predefined:
      - COMPLIANCE_CIS
      - GUARD_DUTY_HOST
      - GUARD_DUTY_IAM
      - INSPECTOR_RBA
      - INSPECTOR_SBP
      - NETWORK_REACHABILITY
      - AZURE_SECURITY_CENTER_ALERTS
      - UNCLASSIFIED
      - COMMAND_AND_CONTROL
      - CREDENTIAL_ACCESS
      - CROSS_ACCOUNT_TRUST
      - DATA_EXFILTRATION
      - DEFENSE_EVASION
      - DISCOVERY
      - HIGH_PRIVILEGED_ROLE
      - INITIAL_ACCESS
      - INTERNET_EXPOSURE
      - KEYS_AND_SECRETS
      - LATERAL_MOVEMENT
      - MALWARE
      - MFA
      - MISCONFIGURATION
      - NETWORK_ANOMALY
      - PRIVILEGE_ESCALATION
      - RECONNAISSANCE
      - RESOURCE_HIJACKING
      - SSH_BRUTE_FORCE
      - UNAUTHORIZED_ACCESS
      - UNENCRYPTED_DATA
      - UNUSED_PRIVILEGES
      - USER_ANOMALY
      - WEAK_PASSWORD
      - SENSITIVE_DATA_EXPOSURE
      - INJECTIONS
      - VULNERABILITY_SCANNING
      - SHELLSHOCK
      - KNOWN_BOTS
      - UNKNOWN_BOTS
      - VIRTUAL_PATCHES
      - WEB_ATTACK
      - BOT_ACTIVITY
      - WEB_SCRAPING
      - CUSTOM
      - VULNERABILITY
      - HOST_VULNERABILITY_CVE
      - CONTAINER_IMAGE_VULNERABILITY_CVE
      - VIRTUAL_IMAGE_VULNERABILITY_CVE
      - SERVERLESS_VULNERABILITY
      - PACKAGE_VULNERABILITY
      supportedModules: []
    - auto: PREDEFINED
      description: Comma-separated list of external finding risk factors.
      isArray: true
      name: risk_factors
      predefined:
      - CRITICAL_SEVERITY
      - HIGH_SEVERITY
      - MEDIUM_SEVERITY
      - HAS_FIX
      - REMOTE_EXECUTION
      - DOS
      - RECENT_VULNERABILITY
      - EXPLOIT_EXISTS
      - ATTACK_COMPLEXITY_LOW
      - ATTACK_VECTOR_NETWORK
      - REACHABLE_FROM_THE_INTERNET
      - LISTENING_PORTS
      - CONTAINER_IS_RUNNING_AS_ROOT
      - NO_MANDATORY_SECURITY_PROFILE_APPLIED
      - RUNNING_AS_PRIVILEGED_CONTAINER
      - PACKAGE_IN_USE
      - DOS_LOW
      - DOS_HIGH
      - EXPLOIT_EXISTS_IN_THE_WILD
      - EXPLOIT_EXISTS_POC
      - SENSITIVE_INFORMATION
      - ROOT_MOUNT
      - RUNTIME_SOCKET
      - HOST_ACCESS
      supportedModules: []
    - description: Timeline item ID.
      name: timeline_item_id
      supportedModules: []
    - description: Comma-separated list of alert IDs.
      isArray: true
      name: alert_ids
      supportedModules: []
    - description: Limit number of records.
      name: limit
      supportedModules: []
    - description: Permissions type.
      name: permission_type
      supportedModules: []
    - description: Next page token.
      name: page_token
      supportedModules: []
    - auto: PREDEFINED
      description: Filter Hyperion findings.
      name: prisma_cloud_findings_only
      predefined:
      - "true"
      - "false"
      supportedModules: []
    - description: CVE ID, package ID, or layer ID. The value needs to be fetched
        from one of the group by API calls.
      name: vulnerability_info_type_id
      supportedModules: []
    - auto: PREDEFINED
      description: Supported vulnerability types.
      name: vulnerability_info_type
      predefined:
      - CVE
      - PACKAGE
      - LAYER
      supportedModules: []
    description: Returns detailed information for the asset vulnerabilities with the
      given ID.
    name: prisma-cloud-asset-vulnerabilities-get
    outputs:
    - contextPath: PrismaCloud.AssetVulnerabilities.problem.title
      description: The title of the problem.
      type: String
    - contextPath: PrismaCloud.AssetVulnerabilities.problem.status.statusCode
      description: The status code of the problem.
      type: Number
    - contextPath: PrismaCloud.AssetVulnerabilities.problem.status.reasonPhrase
      description: The reason for the problem.
      type: String
    - contextPath: PrismaCloud.AssetVulnerabilities.problem.detail
      description: The details of the problem.
      type: String
    - contextPath: PrismaCloud.AssetVulnerabilities.problem.instance
      description: The instance of the problem.
      type: String
    - contextPath: PrismaCloud.AssetVulnerabilities.problem.type
      description: The type of the problem.
      type: String
    - contextPath: PrismaCloud.AssetVulnerabilities.problem.parameters
      description: The parameters of the problem.
      type: Unknown
    - contextPath: PrismaCloud.AssetVulnerabilities.accountId
      description: The account ID of the asset vulnerability.
      type: String
    - contextPath: PrismaCloud.AssetVulnerabilities.count
      description: The count of asset vulnerabilities.
      type: String
    - contextPath: PrismaCloud.AssetVulnerabilities.createdOn
      description: The created on of the asset vulnerability.
      type: Number
    - contextPath: PrismaCloud.AssetVulnerabilities.customerId
      description: The customer ID of the asset vulnerability.
      type: Number
    - contextPath: PrismaCloud.AssetVulnerabilities.cveId
      description: The CVE ID of the asset vulnerability.
      type: String
    - contextPath: PrismaCloud.AssetVulnerabilities.description
      description: The description of the asset vulnerability.
      type: String
    - contextPath: PrismaCloud.AssetVulnerabilities.externalId
      description: The external ID of the asset vulnerability.
      type: Number
    - contextPath: PrismaCloud.AssetVulnerabilities.id
      description: The ID of the asset vulnerability.
      type: String
    - contextPath: PrismaCloud.AssetVulnerabilities.normalizedName
      description: The normalized name of the asset vulnerability.
      type: String
    - contextPath: PrismaCloud.AssetVulnerabilities.normalizedNames
      description: The normalized names of the asset vulnerability.
      type: String
    - contextPath: PrismaCloud.AssetVulnerabilities.nvdUrl
      description: The nvd URL of the asset vulnerability.
      type: String
    - contextPath: PrismaCloud.AssetVulnerabilities.link
      description: The link of the asset vulnerability.
      type: String
    - contextPath: PrismaCloud.AssetVulnerabilities.rawData
      description: The raw data of the asset vulnerability.
      type: String
    - contextPath: PrismaCloud.AssetVulnerabilities.regionId
      description: The region ID of the asset vulnerability.
      type: String
    - contextPath: PrismaCloud.AssetVulnerabilities.resourceCloudId
      description: The resource cloud ID of the asset vulnerability.
      type: String
    - contextPath: PrismaCloud.AssetVulnerabilities.resourceId
      description: The resource ID of the asset vulnerability.
      type: Number
    - contextPath: PrismaCloud.AssetVulnerabilities.resourceUrl
      description: The resource URL of the asset vulnerability.
      type: String
    - contextPath: PrismaCloud.AssetVulnerabilities.riskFactors
      description: The risk factors of the asset vulnerability.
      type: String
    - contextPath: PrismaCloud.AssetVulnerabilities.rlUpdatedOn
      description: The rl updated on of the asset vulnerability.
      type: Number
    - contextPath: PrismaCloud.AssetVulnerabilities.scanId
      description: The scan ID of the asset vulnerability.
      type: String
    - contextPath: PrismaCloud.AssetVulnerabilities.score
      description: The score of the asset vulnerability.
      type: String
    - contextPath: PrismaCloud.AssetVulnerabilities.severity
      description: The severity of the asset vulnerability.
      type: String
    - contextPath: PrismaCloud.AssetVulnerabilities.source
      description: The source of the asset vulnerability.
      type: String
    - contextPath: PrismaCloud.AssetVulnerabilities.sourceData
      description: The source data of the asset vulnerability.
      type: Unknown
    - contextPath: PrismaCloud.AssetVulnerabilities.status
      description: The status of the asset vulnerability.
      type: String
    - contextPath: PrismaCloud.AssetVulnerabilities.title
      description: The title of the asset vulnerability.
      type: String
    - contextPath: PrismaCloud.AssetVulnerabilities.type
      description: The type of the asset vulnerability.
      type: String
    - contextPath: PrismaCloud.AssetVulnerabilities.packageName
      description: The package name of the asset vulnerability.
      type: String
    - contextPath: PrismaCloud.AssetVulnerabilities.updatedOn
      description: The updated on of the asset vulnerability.
      type: Number
    supportedModules: []
  - arguments:
    - description: Unified asset ID or RRN (Restricted Resource Name).
      name: asset_id
      required: true
      supportedModules: []
    - auto: PREDEFINED
      description: External finding types.
      name: finding_type
      predefined:
      - COMPLIANCE_CIS
      - GUARD_DUTY_HOST
      - GUARD_DUTY_IAM
      - INSPECTOR_RBA
      - INSPECTOR_SBP
      - NETWORK_REACHABILITY
      - AZURE_SECURITY_CENTER_ALERTS
      - UNCLASSIFIED
      - COMMAND_AND_CONTROL
      - CREDENTIAL_ACCESS
      - CROSS_ACCOUNT_TRUST
      - DATA_EXFILTRATION
      - DEFENSE_EVASION
      - DISCOVERY
      - HIGH_PRIVILEGED_ROLE
      - INITIAL_ACCESS
      - INTERNET_EXPOSURE
      - KEYS_AND_SECRETS
      - LATERAL_MOVEMENT
      - MALWARE
      - MFA
      - MISCONFIGURATION
      - NETWORK_ANOMALY
      - PRIVILEGE_ESCALATION
      - RECONNAISSANCE
      - RESOURCE_HIJACKING
      - SSH_BRUTE_FORCE
      - UNAUTHORIZED_ACCESS
      - UNENCRYPTED_DATA
      - UNUSED_PRIVILEGES
      - USER_ANOMALY
      - WEAK_PASSWORD
      - SENSITIVE_DATA_EXPOSURE
      - INJECTIONS
      - VULNERABILITY_SCANNING
      - SHELLSHOCK
      - KNOWN_BOTS
      - UNKNOWN_BOTS
      - VIRTUAL_PATCHES
      - WEB_ATTACK
      - BOT_ACTIVITY
      - WEB_SCRAPING
      - CUSTOM
      - VULNERABILITY
      - HOST_VULNERABILITY_CVE
      - CONTAINER_IMAGE_VULNERABILITY_CVE
      - VIRTUAL_IMAGE_VULNERABILITY_CVE
      - SERVERLESS_VULNERABILITY
      - PACKAGE_VULNERABILITY
      supportedModules: []
    - auto: PREDEFINED
      description: Comma-separated list of external finding risk factors.
      isArray: true
      name: risk_factors
      predefined:
      - CRITICAL_SEVERITY
      - HIGH_SEVERITY
      - MEDIUM_SEVERITY
      - HAS_FIX
      - REMOTE_EXECUTION
      - DOS
      - RECENT_VULNERABILITY
      - EXPLOIT_EXISTS
      - ATTACK_COMPLEXITY_LOW
      - ATTACK_VECTOR_NETWORK
      - REACHABLE_FROM_THE_INTERNET
      - LISTENING_PORTS
      - CONTAINER_IS_RUNNING_AS_ROOT
      - NO_MANDATORY_SECURITY_PROFILE_APPLIED
      - RUNNING_AS_PRIVILEGED_CONTAINER
      - PACKAGE_IN_USE
      - DOS_LOW
      - DOS_HIGH
      - EXPLOIT_EXISTS_IN_THE_WILD
      - EXPLOIT_EXISTS_POC
      - SENSITIVE_INFORMATION
      - ROOT_MOUNT
      - RUNTIME_SOCKET
      - HOST_ACCESS
      supportedModules: []
    - description: Timeline item ID.
      name: timeline_item_id
      supportedModules: []
    - description: Comma-separated list of alert IDs.
      isArray: true
      name: alert_ids
      supportedModules: []
    - description: Limit number of records.
      name: limit
      supportedModules: []
    - description: Permissions type.
      name: permission_type
      supportedModules: []
    - description: Next page token.
      name: page_token
      supportedModules: []
    - auto: PREDEFINED
      description: Filter Hyperion findings.
      name: prisma_cloud_findings_only
      predefined:
      - "true"
      - "false"
      supportedModules: []
    - description: CVE ID, package ID, or layer ID. The value needs to be fetched
        from one of the group by API calls.
      name: vulnerability_info_type_id
      supportedModules: []
    - auto: PREDEFINED
      description: Supported vulnerability types.
      name: vulnerability_info_type
      predefined:
      - CVE
      - PACKAGE
      - LAYER
      supportedModules: []
    description: Returns detailed information for the asset alerts with the given
      ID.
    name: prisma-cloud-asset-alerts-get
    outputs:
    - contextPath: PrismaCloud.AssetAlerts.problem.title
      description: The title of the problem.
      type: String
    - contextPath: PrismaCloud.AssetAlerts.problem.status.statusCode
      description: The status code of the problem.
      type: Number
    - contextPath: PrismaCloud.AssetAlerts.problem.status.reasonPhrase
      description: The reason for the problem.
      type: String
    - contextPath: PrismaCloud.AssetAlerts.problem.detail
      description: The details of the problem.
      type: String
    - contextPath: PrismaCloud.AssetAlerts.problem.instance
      description: The instance of the problem.
      type: String
    - contextPath: PrismaCloud.AssetAlerts.problem.type
      description: The type of the problem.
      type: String
    - contextPath: PrismaCloud.AssetAlerts.problem.parameters
      description: The parameters of the problem.
      type: Unknown
    - contextPath: PrismaCloud.AssetAlerts.id
      description: The ID of the asset alert.
      type: String
    - contextPath: PrismaCloud.AssetAlerts.severity
      description: The severity of the asset alert.
      type: String
    - contextPath: PrismaCloud.AssetAlerts.policyId
      description: The policy ID of the asset alert.
      type: String
    - contextPath: PrismaCloud.AssetAlerts.isHyperionPolicy
      description: The hyperion policy of the asset alert.
      type: Boolean
    - contextPath: PrismaCloud.AssetAlerts.policyType
      description: The policy type of the asset alert.
      type: String
    - contextPath: PrismaCloud.AssetAlerts.policyName
      description: The policy name of the asset alert.
      type: String
    - contextPath: PrismaCloud.AssetAlerts.policyDescription
      description: The policy description of the asset alert.
      type: String
    - contextPath: PrismaCloud.AssetAlerts.alertTime
      description: The alert time of the asset alert.
      type: Number
    supportedModules: []
  - arguments:
    - description: Unified asset ID or RRN (Restricted Resource Name).
      name: asset_id
      required: true
      supportedModules: []
    - auto: PREDEFINED
      description: External finding types.
      name: finding_type
      predefined:
      - COMPLIANCE_CIS
      - GUARD_DUTY_HOST
      - GUARD_DUTY_IAM
      - INSPECTOR_RBA
      - INSPECTOR_SBP
      - NETWORK_REACHABILITY
      - AZURE_SECURITY_CENTER_ALERTS
      - UNCLASSIFIED
      - COMMAND_AND_CONTROL
      - CREDENTIAL_ACCESS
      - CROSS_ACCOUNT_TRUST
      - DATA_EXFILTRATION
      - DEFENSE_EVASION
      - DISCOVERY
      - HIGH_PRIVILEGED_ROLE
      - INITIAL_ACCESS
      - INTERNET_EXPOSURE
      - KEYS_AND_SECRETS
      - LATERAL_MOVEMENT
      - MALWARE
      - MFA
      - MISCONFIGURATION
      - NETWORK_ANOMALY
      - PRIVILEGE_ESCALATION
      - RECONNAISSANCE
      - RESOURCE_HIJACKING
      - SSH_BRUTE_FORCE
      - UNAUTHORIZED_ACCESS
      - UNENCRYPTED_DATA
      - UNUSED_PRIVILEGES
      - USER_ANOMALY
      - WEAK_PASSWORD
      - SENSITIVE_DATA_EXPOSURE
      - INJECTIONS
      - VULNERABILITY_SCANNING
      - SHELLSHOCK
      - KNOWN_BOTS
      - UNKNOWN_BOTS
      - VIRTUAL_PATCHES
      - WEB_ATTACK
      - BOT_ACTIVITY
      - WEB_SCRAPING
      - CUSTOM
      - VULNERABILITY
      - HOST_VULNERABILITY_CVE
      - CONTAINER_IMAGE_VULNERABILITY_CVE
      - VIRTUAL_IMAGE_VULNERABILITY_CVE
      - SERVERLESS_VULNERABILITY
      - PACKAGE_VULNERABILITY
      supportedModules: []
    - auto: PREDEFINED
      description: Comma-separated list of external finding risk factors.
      isArray: true
      name: risk_factors
      predefined:
      - CRITICAL_SEVERITY
      - HIGH_SEVERITY
      - MEDIUM_SEVERITY
      - HAS_FIX
      - REMOTE_EXECUTION
      - DOS
      - RECENT_VULNERABILITY
      - EXPLOIT_EXISTS
      - ATTACK_COMPLEXITY_LOW
      - ATTACK_VECTOR_NETWORK
      - REACHABLE_FROM_THE_INTERNET
      - LISTENING_PORTS
      - CONTAINER_IS_RUNNING_AS_ROOT
      - NO_MANDATORY_SECURITY_PROFILE_APPLIED
      - RUNNING_AS_PRIVILEGED_CONTAINER
      - PACKAGE_IN_USE
      - DOS_LOW
      - DOS_HIGH
      - EXPLOIT_EXISTS_IN_THE_WILD
      - EXPLOIT_EXISTS_POC
      - SENSITIVE_INFORMATION
      - ROOT_MOUNT
      - RUNTIME_SOCKET
      - HOST_ACCESS
      supportedModules: []
    - description: Timeline item ID.
      name: timeline_item_id
      supportedModules: []
    - description: Comma-separated list of alert IDs.
      isArray: true
      name: alert_ids
      supportedModules: []
    - description: Limit number of records.
      name: limit
      supportedModules: []
    - description: Permissions type.
      name: permission_type
      supportedModules: []
    - description: Next page token.
      name: page_token
      supportedModules: []
    - auto: PREDEFINED
      description: Filter Hyperion findings.
      name: prisma_cloud_findings_only
      predefined:
      - "true"
      - "false"
      supportedModules: []
    - description: CVE ID, package ID, or layer ID. The value needs to be fetched
        from one of the group by API calls.
      name: vulnerability_info_type_id
      supportedModules: []
    - auto: PREDEFINED
      description: Supported vulnerability types.
      name: vulnerability_info_type
      predefined:
      - CVE
      - PACKAGE
      - LAYER
      supportedModules: []
    description: Returns detailed information for the asset relationships with the
      given ID.
    name: prisma-cloud-asset-relationships-get
    outputs:
    - contextPath: PrismaCloud.AssetRelationships.assetId
      description: The asset ID of the asset relationship.
      type: String
    - contextPath: PrismaCloud.AssetRelationships.relationshipType
      description: The relationship type of the asset relationship.
      type: String
    - contextPath: PrismaCloud.AssetRelationships.assetTypeId
      description: The asset type ID of the asset relationship.
      type: Number
    - contextPath: PrismaCloud.AssetRelationships.assetType
      description: The asset type of the asset relationship.
      type: String
    - contextPath: PrismaCloud.AssetRelationships.assetName
      description: The asset name of the asset relationship.
      type: String
    - contextPath: PrismaCloud.AssetRelationships.externalAssetId
      description: The external asset ID of the asset relationship.
      type: String
    - contextPath: PrismaCloud.AssetRelationships.cloudId
      description: The cloud ID of the asset relationship.
      type: String
    - contextPath: PrismaCloud.AssetRelationships.cloudName
      description: The cloud name of the asset relationship.
      type: String
    - contextPath: PrismaCloud.AssetRelationships.pseudoAsset
      description: The pseudo asset of the asset relationship.
      type: Boolean
    - contextPath: PrismaCloud.AssetRelationships.region
      description: The region of the asset relationship.
      type: String
    - contextPath: PrismaCloud.AssetRelationships.cloudAccountId
      description: The cloud account ID of the asset relationship.
      type: String
    - contextPath: PrismaCloud.AssetRelationships.assetClassId
      description: The asset class ID of the asset relationship.
      type: String
    - contextPath: PrismaCloud.AssetRelationships.assetClass
      description: The asset class of the asset relationship.
      type: String
    - contextPath: PrismaCloud.AssetRelationships.alertsCount.severity
      description: The severity of the alerts of the asset relationship.
      type: String
    - contextPath: PrismaCloud.AssetRelationships.alertsCount.count
      description: The number of alerts of the asset relationships.
      type: Number
    - contextPath: PrismaCloud.AssetRelationships.alertCountBySeverity.severity
      description: The severity of the alerts of the asset relationship.
      type: String
    - contextPath: PrismaCloud.AssetRelationships.alertCountBySeverity.count
      description: The number of alerts by severity of the asset relationship.
      type: Number
    - contextPath: PrismaCloud.AssetRelationships.vulnerabilityCounts
      description: The number of vulnerabilities of the asset relationship.
      type: Unknown
    - contextPath: PrismaCloud.AssetRelationships.problem.title
      description: The title of the problem.
      type: String
    - contextPath: PrismaCloud.AssetRelationships.problem.status.statusCode
      description: The status code of the problem.
      type: Number
    - contextPath: PrismaCloud.AssetRelationships.problem.status.reasonPhrase
      description: The reason for the problem.
      type: String
    - contextPath: PrismaCloud.AssetRelationships.problem.detail
      description: The details of the problem.
      type: String
    - contextPath: PrismaCloud.AssetRelationships.problem.instance
      description: The instance of the problem.
      type: String
    - contextPath: PrismaCloud.AssetRelationships.problem.type
      description: The type of the problem.
      type: String
    - contextPath: PrismaCloud.AssetRelationships.problem.parameters
      description: The parameters of the problem.
      type: Unknown
    supportedModules: []
  - arguments:
    - description: Unified asset ID or RRN (Restricted Resource Name).
      name: asset_id
      required: true
      supportedModules: []
    - auto: PREDEFINED
      description: External finding types.
      name: finding_type
      predefined:
      - COMPLIANCE_CIS
      - GUARD_DUTY_HOST
      - GUARD_DUTY_IAM
      - INSPECTOR_RBA
      - INSPECTOR_SBP
      - NETWORK_REACHABILITY
      - AZURE_SECURITY_CENTER_ALERTS
      - UNCLASSIFIED
      - COMMAND_AND_CONTROL
      - CREDENTIAL_ACCESS
      - CROSS_ACCOUNT_TRUST
      - DATA_EXFILTRATION
      - DEFENSE_EVASION
      - DISCOVERY
      - HIGH_PRIVILEGED_ROLE
      - INITIAL_ACCESS
      - INTERNET_EXPOSURE
      - KEYS_AND_SECRETS
      - LATERAL_MOVEMENT
      - MALWARE
      - MFA
      - MISCONFIGURATION
      - NETWORK_ANOMALY
      - PRIVILEGE_ESCALATION
      - RECONNAISSANCE
      - RESOURCE_HIJACKING
      - SSH_BRUTE_FORCE
      - UNAUTHORIZED_ACCESS
      - UNENCRYPTED_DATA
      - UNUSED_PRIVILEGES
      - USER_ANOMALY
      - WEAK_PASSWORD
      - SENSITIVE_DATA_EXPOSURE
      - INJECTIONS
      - VULNERABILITY_SCANNING
      - SHELLSHOCK
      - KNOWN_BOTS
      - UNKNOWN_BOTS
      - VIRTUAL_PATCHES
      - WEB_ATTACK
      - BOT_ACTIVITY
      - WEB_SCRAPING
      - CUSTOM
      - VULNERABILITY
      - HOST_VULNERABILITY_CVE
      - CONTAINER_IMAGE_VULNERABILITY_CVE
      - VIRTUAL_IMAGE_VULNERABILITY_CVE
      - SERVERLESS_VULNERABILITY
      - PACKAGE_VULNERABILITY
      supportedModules: []
    - auto: PREDEFINED
      description: Comma-separated list of external finding risk factors.
      isArray: true
      name: risk_factors
      predefined:
      - CRITICAL_SEVERITY
      - HIGH_SEVERITY
      - MEDIUM_SEVERITY
      - HAS_FIX
      - REMOTE_EXECUTION
      - DOS
      - RECENT_VULNERABILITY
      - EXPLOIT_EXISTS
      - ATTACK_COMPLEXITY_LOW
      - ATTACK_VECTOR_NETWORK
      - REACHABLE_FROM_THE_INTERNET
      - LISTENING_PORTS
      - CONTAINER_IS_RUNNING_AS_ROOT
      - NO_MANDATORY_SECURITY_PROFILE_APPLIED
      - RUNNING_AS_PRIVILEGED_CONTAINER
      - PACKAGE_IN_USE
      - DOS_LOW
      - DOS_HIGH
      - EXPLOIT_EXISTS_IN_THE_WILD
      - EXPLOIT_EXISTS_POC
      - SENSITIVE_INFORMATION
      - ROOT_MOUNT
      - RUNTIME_SOCKET
      - HOST_ACCESS
      supportedModules: []
    - description: Timeline item ID.
      name: timeline_item_id
      supportedModules: []
    - description: Comma-separated list of alert IDs.
      isArray: true
      name: alert_ids
      supportedModules: []
    - description: Limit number of records.
      name: limit
      supportedModules: []
    - description: Permissions type.
      name: permission_type
      supportedModules: []
    - description: Next page token.
      name: page_token
      supportedModules: []
    - auto: PREDEFINED
      description: Filter Hyperion findings.
      name: prisma_cloud_findings_only
      predefined:
      - "true"
      - "false"
      supportedModules: []
    - description: CVE ID, package ID, or layer ID. The value needs to be fetched
        from one of the group by API calls.
      name: vulnerability_info_type_id
      supportedModules: []
    - auto: PREDEFINED
      description: Supported vulnerability types.
      name: vulnerability_info_type
      predefined:
      - CVE
      - PACKAGE
      - LAYER
      supportedModules: []
    description: Returns detailed information for the asset network with the given
      ID.
    name: prisma-cloud-asset-network-get
    outputs:
    - contextPath: PrismaCloud.AssetNetwork.problem.title
      description: The title of the problem.
      type: String
    - contextPath: PrismaCloud.AssetNetwork.problem.status.statusCode
      description: The status code of the problem.
      type: Number
    - contextPath: PrismaCloud.AssetNetwork.problem.status.reasonPhrase
      description: The reason for the problem.
      type: String
    - contextPath: PrismaCloud.AssetNetwork.problem.detail
      description: The details of the problem.
      type: String
    - contextPath: PrismaCloud.AssetNetwork.problem.instance
      description: The instance of the problem.
      type: String
    - contextPath: PrismaCloud.AssetNetwork.problem.type
      description: The type of the problem.
      type: String
    - contextPath: PrismaCloud.AssetNetwork.problem.parameters
      description: The parameters of the problem.
      type: Unknown
    - contextPath: PrismaCloud.AssetNetwork.id
      description: The ID of the asset network.
      type: Number
    - contextPath: PrismaCloud.AssetNetwork.access
      description: The access of the asset network.
      type: String
    - contextPath: PrismaCloud.AssetNetwork.direction
      description: The direction of the asset network.
      type: String
    - contextPath: PrismaCloud.AssetNetwork.ips
      description: The IPS of the asset network.
      type: String
    - contextPath: PrismaCloud.AssetNetwork.ports
      description: The ports of the asset network.
      type: String
    - contextPath: PrismaCloud.AssetNetwork.protocol
      description: The protocol of the asset network.
      type: String
    - contextPath: PrismaCloud.AssetNetwork.priority
      description: The priority of the asset network.
      type: Number
    supportedModules: []
  dockerimage: demisto/python3:3.12.13.8428455
  isfetch: true
  nativeimage:
  - "8.8"
  - "8.6"
  runonce: false
  script: |
    register_module_line('PrismaCloud v2', 'start', __line__())
    CONSTANT_PACK_VERSION = '4.4.30'
    demisto.debug('pack id = PrismaCloud, pack version = 4.4.30')
    import json
    from copy import deepcopy




    """ CONSTANTS """

    HEADERS = {"Content-Type": "application/json; charset=UTF-8", "Accept": "application/json; charset=UTF-8"}
    REQUEST_CSPM_AUTH_HEADER = "x-redlock-auth"  # Prisma Cloud Security Posture Management
    REQUEST_CCS_AUTH_HEADER = "authorization"  # Prisma Cloud Code Security
    RESPONSE_STATUS_HEADER = "x-redlock-status"

    DATE_FORMAT = "%Y-%m-%dT%H:%M:%SZ"  # ISO8601 format with UTC, default in XSOAR

    RELATIVE_TIME_UNIT_OPTIONS = (
        "hour",
        "day",
        "week",
        "month",
        "year",
    )
    TO_NOW_TIME_UNIT_OPTIONS = (
        "day",
        "week",
        "month",
        "year",
        "epoch",  # account on-boarding
        "login",  # last login
    )
    TIME_FILTER_BASE_CASE = {"type": "to_now", "value": "epoch"}
    ALERT_SEARCH_BASE_TIME_FILTER = {"type": "relative", "value": {"amount": 7, "unit": "day"}}
    ERROR_TOO_MANY_ARGS = (
        'Too many arguments provided. You cannot specify absolute times ("time_range_date_from", '
        '"time_range_date_to") with relative times ("time_range_unit", "time_range_value").'
    )
    ERROR_NOT_ENOUGH_ARGS = (
        'Not enough arguments provided. You cannot specify "time_range_date_from" without '
        '"time_range_date_to", or "time_range_value" without "time_range_unit".'
    )
    ERROR_RELATIVE_TIME_UNIT = f'Time unit for relative time must be one of the following: {", ".join(RELATIVE_TIME_UNIT_OPTIONS)}.'
    ERROR_TO_NOW_TIME_UNIT = f'Time unit for to_now time must be one of the following: {", ".join(TO_NOW_TIME_UNIT_OPTIONS)}'

    MAX_INCIDENTS_TO_FETCH = 200
    FETCH_DEFAULT_TIME = "3 days"
    FETCH_LOOK_BACK_TIME = 20

    MIRROR_DIRECTION_MAPPING = {
        "None": None,
        "Incoming": "In",
        "Outgoing": "Out",
        "Incoming And Outgoing": "Both",
    }

    INCIDENT_INCOMING_MIRROR_ARGS = ["status", "dismissalNote", "reason", "policy.name"]
    INCIDENT_INCOMING_MIRROR_CLOSING_STATUSES = ["dismissed", "resolved", "snoozed"]
    INCIDENT_INCOMING_MIRROR_REOPENING_STATUS = "open"
    INCIDENT_OUTGOING_MIRROR_DISMISSAL_NOTE = "Closed by XSOAR"

    PAGE_NUMBER_DEFAULT_VALUE = 1
    PAGE_SIZE_DEFAULT_VALUE = 50
    PAGE_SIZE_MAX_VALUE = 10000

    DEFAULT_LIMIT = "50"

    PRISMA_TO_XSOAR_SEVERITY = {
        "critical": IncidentSeverity.CRITICAL,
        "high": IncidentSeverity.HIGH,
        "medium": IncidentSeverity.MEDIUM,
        "low": IncidentSeverity.LOW,
        "informational": IncidentSeverity.INFO,
    }

    FINDING_TYPES_OPTIONS = ["guard_duty_host", "guard_duty_iam", "inspector_sbp", "compliance_cis", "host_vulnerability_cve"]
    RISK_FACTORS_OPTIONS = [
        "CRITICAL_SEVERITY",
        "HIGH_SEVERITY",
        "MEDIUM_SEVERITY",
        "HAS_FIX",
        "REMOTE_EXECUTION",
        "DOS",
        "RECENT_VULNERABILITY",
        "EXPLOIT_EXISTS",
        "ATTACK_COMPLEXITY_LOW",
        "ATTACK_VECTOR_NETWORK",
        "REACHABLE_FROM_THE_INTERNET",
        "LISTENING_PORTS",
        "CONTAINER_IS_RUNNING_AS_ROOT",
        "NO_MANDATORY_SECURITY_PROFILE_APPLIED",
        "RUNNING_AS_PRIVILEGED_CONTAINER",
        "PACKAGE_IN_USE",
    ]

    CATEGORIES_OPTIONS = [
        "IAM",
        "Compute",
        "Monitoring",
        "Networking",
        "Kubernetes",
        "General",
        "Storage",
        "Secrets",
        "Public",
        "Vulnerabilities",
        "Drift",
        "BuildIntegrity",
        "Licenses",
    ]
    FILE_TYPES_OPTIONS = [
        "tf",
        "json",
        "yml",
        "yaml",
        "template",
        ".checkov.baseline",
        "hcl",
        "Dockerfile",
        "package.json",
        "package-lock.json",
        "bower.json",
        "pom.xml",
        "build.gradle",
        "build.gradle.kts",
        "gradle.properties",
        "gradle-wrapper.properties",
        "go.sum",
        "go.mod",
        "requirements.txt",
        "METADATA",
        "bicep",
        "Pipfile.lock",
        "Pipfile",
        "yarn.lock",
        "Gemfile",
        "Gemfile.lock",
        "gemspec",
        "env",
        "settings.py",
        "main.py",
        "application.py",
        "config.py",
        "app.js",
        "config.js",
        "dev.js",
        "db.properties",
        "application.properties",
        "private.pem",
        "privatekey.pem",
        "index.php",
        "config.php",
        "config.xml",
        "strings.xml",
        "app.module.ts",
        "environment.ts",
        "tpl",
        "tfvars",
        "unknown",
    ]
    SEARCH_OPTIONS_OPTIONS = ["path", "code"]
    SEARCH_TITLE_OPTIONS = ["title", "constructive_title", "descriptive_title"]
    SEVERITIES_OPTIONS = ["CRITICAL", "HIGH", "MEDIUM", "LOW", "INFO"]
    SOURCE_TYPES_OPTIONS = [
        "Github",
        "Bitbucket",
        "Gitlab",
        "AzureRepos",
        "cli",
        "AWS",
        "Azure",
        "GCP",
        "Docker",
        "githubEnterprise",
        "gitlabEnterprise",
        "bitbucketEnterprise",
        "terraformCloud",
        "githubActions",
        "circleci",
        "codebuild",
        "jenkins",
        "tfcRunTasks",
        "admissionController",
        "terraformEnterprise",
    ]
    STATUSES_OPTIONS = ["Errors", "Suppressed", "Passed", "Fixed"]

    TIME_FIELDS = [
        "firstSeen",
        "lastSeen",
        "alertTime",
        "eventOccurred",
        "lastUpdated",
        "insertTs",
        "createdTs",
        "lastModifiedTs",
        "addedOn",
        "eventTs",
        "createdOn",
        "updatedOn",
        "rlUpdatedOn",
        "lastLoginTs",
    ]

    LICENSE_TYPES = [
        "OSI_APACHE",
        "OSI_ARTISTIC",
        "OSI_BSD",
        "OSI_EFL",
        "OSI_FDL",
        "OSI_LGPL",
        "OSI_ZPL",
        "CC-BY-SA-2.1-JP",
        "GPL-2.0-or-later",
        "AMDPLPA",
        "CC-BY-SA-3.0-DE",
        "ECL-2.0",
        "EPICS",
        "eCos-2.0",
        "GPL-3.0-with-GCC-exception",
        "KiCad-libraries-exception",
        "GFDL-1.3-invariants-or-later",
        "APSL-1.1",
        "MIT",
        "CC-BY-NC-ND-3.0-DE",
        "GPL-3.0",
        "CC-BY-SA-1.0",
        "ADSL",
        "MIT-CMU",
        "Linux-man-pages-copyleft",
        "diffmark",
        "GPL-2.0",
        "HPND",
        "OSL-1.0",
        "ClArtistic",
        "IJG",
        "IPL-1.0",
        "NCGL-UK-2.0",
        "CC-BY-2.5",
        "LGPL-3.0-or-later",
        "LiLiQ-Rplus-1.1",
        "CC0-1.0",
        "Glide",
        "ImageMagick",
        "CECILL-1.1",
        "AGPL-3.0-only",
        "eGenix",
        "ANTLR-PD",
        "CC-BY-NC-SA-4.0",
        "CECILL-C",
        "GFDL-1.3-no-invariants-only",
        "SHL-0.5",
        "MIT-Modern-Variant",
        "CC-BY-3.0-NL",
        "MIT-feh",
        "SMLNJ",
        "CC-BY-ND-2.0",
        "HaskellReport",
        "AGPL-1.0",
        "BitTorrent-1.0",
        "CDL-1.0",
        "SISSL",
        "CC-BY-SA-3.0",
        "C-UDA-1.0",
        "YPL-1.1",
        "AGPL-1.0-or-later",
        "NLOD-2.0",
        "Unlicense",
        "D-FSL-1.0",
        "Linux-OpenIB",
        "GPL-1.0-only",
        "libtiff",
        "Plexus",
        "BSD-1-Clause",
        "MPL-2.0",
        "Intel-ACPI",
        "Barr",
        "OGL-Canada-2.0",
        "ANTLR-PD-fallback",
        "Zed",
        "MIT-open-group",
        "LGPL-2.1-or-later",
        "mpich2",
        "Motosoto",
        "OGDL-Taiwan-1.0",
        "PDDL-1.0",
        "GFDL-1.3-invariants-only",
        "EUPL-1.1",
        "EUPL-1.0",
        "Entessa",
        "CC-BY-NC-ND-2.0",
        "W3C",
        "GFDL-1.2-no-invariants-or-later",
        "Saxpath",
        "GFDL-1.3-only",
        "FreeImage",
        "CNRI-Python",
        "Apache-1.0",
        "OLDAP-1.4",
        "JSON",
        "GPL-3.0-or-later",
        "DSDP",
        "MPL-2.0-no-copyleft-exception",
        "Condor-1.1",
        "Imlib2",
        "iMatix",
        "OLDAP-2.6",
        "Rdisc",
        "LiLiQ-P-1.1",
        "xpp",
        "FDK-AAC",
        "CC-BY-NC-3.0",
        "Jam",
        "GFDL-1.3-no-invariants-or-later",
        "GFDL-1.3-or-later",
        "ICU",
        "LGPL-2.1",
        "AFL-2.1",
        "JasPer-2.0",
        "SSPL-1.0",
        "CC-BY-SA-2.0",
        "BSD-3-Clause-Clear",
        "OSL-2.0",
        "CC-BY-SA-4.0",
        "SISSL-1.2",
        "ODC-By-1.0",
        "ZPL-2.1",
        "QPL-1.0",
        "LGPL-2.0-only",
        "CC-BY-SA-2.5",
        "Zimbra-1.3",
        "MTLL",
        "Eurosym",
        "NPL-1.0",
        "blessing",
        "GFDL-1.3",
        "GPL-1.0+",
        "GFDL-1.1-no-invariants-only",
        "CC-BY-NC-ND-3.0",
        "Xerox",
        "Unicode-TOU",
        "Aladdin",
        "CC-BY-NC-SA-2.5",
        "Artistic-1.0",
        "BSL-1.0",
        "CC-BY-ND-2.5",
        "NetCDF",
        "MulanPSL-2.0",
        "UCL-1.0",
        "PostgreSQL",
        "GFDL-1.1-only",
        "RHeCos-1.1",
        "Sendmail-8.23",
        "psfrag",
        "SNIA",
        "EPL-2.0",
        "0BSD",
        "MPL-1.0",
        "GFDL-1.1-or-later",
        "XFree86-1.1",
        "WTFPL",
        "CDLA-Sharing-1.0",
        "CAL-1.0",
        "CERN-OHL-S-2.0",
        "CC-BY-NC-SA-3.0-DE",
        "CC-BY-NC-1.0",
        "Artistic-2.0",
        "BUSL-1.1",
        "EUPL-1.2",
        "GPL-2.0-with-font-exception",
        "LGPL-2.0+",
        "AGPL-1.0-only",
        "SGI-B-1.0",
        "W3C-20150513",
        "Adobe-2006",
        "xinetd",
        "BSD-3-Clause-No-Military-License",
        "DRL-1.0",
        "LGPL-2.0",
        "MirOS",
        "PolyForm-Small-Business-1.0.0",
        "CDLA-Permissive-2.0",
        "LiLiQ-R-1.1",
        "Vim",
        "curl",
        "OLDAP-2.2.2",
        "CATOSL-1.1",
        "CC-BY-ND-4.0",
        "CC-BY-NC-SA-2.0-UK",
        "APSL-1.0",
        "GPL-2.0-with-classpath-exception",
        "OLDAP-2.0.1",
        "NIST-PD-fallback",
        "Glulxe",
        "NPL-1.1",
        "CC-BY-NC-ND-1.0",
        "CC-BY-NC-2.5",
        "Parity-6.0.0",
        "CC-BY-NC-SA-3.0-IGO",
        "CPAL-1.0",
        "CC-BY-2.5-AU",
        "SWL",
        "LAL-1.2",
        "NRL",
        "OGL-UK-3.0",
        "MS-RL",
        "OSL-2.1",
        "LPL-1.0",
        "OSET-PL-2.1",
        "OFL-1.0-no-RFN",
        "OML",
        "Arphic-1999",
        "BSD-2-Clause",
        "MulanPSL-1.0",
        "EPL-1.0",
        "BSD-4-Clause-Shortened",
        "Elastic-2.0",
        "NLPL",
        "LPPL-1.2",
        "SchemeReport",
        "Multics",
        "Net-SNMP",
        "SHL-0.51",
        "MIT-advertising",
        "GPL-3.0-with-autoconf-exception",
        "MS-PL",
        "wxWindows",
        "ZPL-1.1",
        "ISC",
        "CC-BY-NC-SA-3.0",
        "GPL-2.0-only",
        "Giftware",
        "CPL-1.0",
        "EUDatagrid",
        "SGI-B-1.1",
        "CC-BY-1.0",
        "bzip2-1.0.5",
        "libselinux-1.0",
        "SMPPL",
        "Latex2e",
        "Watcom-1.0",
        "VSL-1.0",
        "CC-BY-NC-SA-1.0",
        "FreeBSD-DOC",
        "Nunit",
        "LPPL-1.0",
        "OLDAP-2.4",
        "TAPR-OHL-1.0",
        "OLDAP-2.3",
        "CECILL-2.0",
        "LPPL-1.3a",
        "Qhull",
        "CNRI-Python-GPL-Compatible",
        "Frameworx-1.0",
        "CDLA-Permissive-1.0",
        "X11-distribute-modifications-variant",
        "EFL-1.0",
        "DOC",
        "GFDL-1.2-or-later",
        "BSD-3-Clause-No-Nuclear-License",
        "LPPL-1.1",
        "CC-BY-3.0-US",
        "TOSL",
        "Spencer-99",
        "copyleft-next-0.3.1",
        "FSFAP",
        "CC-BY-NC-ND-4.0",
        "OLDAP-2.8",
        "Bahyph",
        "Newsletr",
        "CC-BY-NC-4.0",
        "OFL-1.1",
        "TU-Berlin-2.0",
        "GFDL-1.2-invariants-or-later",
        "BSD-2-Clause-NetBSD",
        "Crossword",
        "YPL-1.0",
        "GPL-2.0-with-bison-exception",
        "NIST-PD",
        "IPA",
        "GFDL-1.1-invariants-or-later",
        "CC-BY-NC-ND-3.0-IGO",
        "BSD-Source-Code",
        "BitTorrent-1.1",
        "AFL-3.0",
        "Zend-2.0",
        "GFDL-1.1",
        "HPND-sell-variant",
        "Abstyles",
        "Interbase-1.0",
        "MakeIndex",
        "EFL-2.0",
        "LPL-1.02",
        "OLDAP-2.2",
        "LGPL-3.0-only",
        "LPPL-1.3c",
        "libpng-2.0",
        "Hippocratic-2.1",
        "BSD-3-Clause-No-Nuclear-License-2014",
        "AAL",
        "NOSL",
        "CC-BY-3.0-AT",
        "HTMLTIDY",
        "GPL-1.0-or-later",
        "RPL-1.5",
        "BSD-4-Clause-UC",
        "Wsuipa",
        "Cube",
        "SCEA",
        "IBM-pibs",
        "Borceux",
        "CC-BY-ND-3.0-DE",
        "CC-BY-NC-SA-2.0-FR",
        "Afmparse",
        "CUA-OPL-1.0",
        "CC-BY-SA-3.0-AT",
        "LGPL-2.1+",
        "OLDAP-2.7",
        "GLWTPL",
        "CC-BY-NC-SA-2.0",
        "OCCT-PL",
        "CNRI-Jython",
        "Leptonica",
        "OFL-1.0-RFN",
        "OpenSSL",
        "RSA-MD",
        "TORQUE-1.1",
        "X11",
        "BSD-Protection",
        "JPNIC",
        "App-s2p",
        "GFDL-1.2-only",
        "CPOL-1.02",
        "CC-BY-ND-3.0",
        "GPL-1.0",
        "Zlib",
        "Python-2.0",
        "OLDAP-1.3",
        "Mup",
        "LGPLLR",
        "CC-BY-4.0",
        "OCLC-2.0",
        "OGTSL",
        "DL-DE-BY-2.0",
        "OFL-1.0",
        "GFDL-1.2-invariants-only",
        "Sendmail",
        "CC-BY-NC-3.0-DE",
        "VOSTROM",
        "Beerware",
        "FSFULLR",
        "Fair",
        "BSD-2-Clause-FreeBSD",
        "Community-Spec-1.0",
        "SSH-short",
        "FSFUL",
        "GFDL-1.1-no-invariants-or-later",
        "CrystalStacker",
        "GFDL-1.1-invariants-only",
        "Ruby",
        "BSD-3-Clause-Open-MPI",
        "Baekmuk",
        "Libpng",
        "GD",
        "OLDAP-2.1",
        "Sleepycat",
        "CERN-OHL-P-2.0",
        "GFDL-1.2",
        "CC-BY-2.0",
        "SPL-1.0",
        "OLDAP-1.2",
        "etalab-2.0",
        "TMate",
        "NCSA",
        "NBPL-1.0",
        "Intel",
        "GPL-3.0-only",
        "APSL-2.0",
        "GPL-2.0-with-autoconf-exception",
        "TU-Berlin-1.0",
        "Noweb",
        "SSH-OpenSSH",
        "BSD-3-Clause-Attribution",
        "PSF-2.0",
        "psutils",
        "CERN-OHL-1.2",
        "SimPL-2.0",
        "OLDAP-2.2.1",
        "SGI-B-2.0",
        "GPL-2.0+",
        "COIL-1.0",
        "Naumen",
        "CC-BY-ND-1.0",
        "Unicode-DFS-2016",
        "AFL-1.2",
        "OSL-3.0",
        "OFL-1.1-RFN",
        "SAX-PD",
        "Xnet",
        "AML",
        "Apache-1.1",
        "NAIST-2003",
        "NGPL",
        "ZPL-2.0",
        "OFL-1.1-no-RFN",
        "APSL-1.2",
        "MPL-1.1",
        "BlueOak-1.0.0",
        "Unicode-DFS-2015",
        "PHP-3.01",
        "GL2PS",
        "NTP-0",
        "BSD-4-Clause",
        "TCL",
        "RSCPL",
        "MIT-enna",
        "CERN-OHL-1.1",
        "OSL-1.1",
        "BSD-3-Clause-LBNL",
        "Bitstream-Vera",
        "Adobe-Glyph",
        "MITNFA",
        "CC-BY-3.0-DE",
        "CECILL-1.0",
        "SugarCRM-1.1.3",
        "CAL-1.0-Combined-Work-Exception",
        "BSD-3-Clause",
        "Info-ZIP",
        "LGPL-3.0+",
        "Zimbra-1.4",
        "zlib-acknowledgement",
        "Spencer-94",
        "MIT-0",
        "AGPL-3.0",
        "CC-PDDC",
        "CC-BY-NC-2.0",
        "mplus",
        "ODbL-1.0",
        "RPSL-1.0",
        "APAFML",
        "OGL-UK-1.0",
        "CDDL-1.1",
        "bzip2-1.0.6",
        "LGPL-2.1-only",
        "OGC-1.0",
        "BSD-3-Clause-No-Nuclear-Warranty",
        "ErlPL-1.1",
        "ECL-1.0",
        "CERN-OHL-W-2.0",
        "OGL-UK-2.0",
        "O-UDA-1.0",
        "NTP",
        "NASA-1.3",
        "copyleft-next-0.3.0",
        "TCP-wrappers",
        "Apache-2.0",
        "CC-BY-3.0",
        "CECILL-B",
        "Nokia",
        "GPL-3.0+",
        "GPL-2.0-with-GCC-exception",
        "OPL-1.0",
        "OPUBL-1.0",
        "UPL-1.0",
        "AFL-2.0",
        "LGPL-2.0-or-later",
        "CECILL-2.1",
        "gnuplot",
        "Caldera",
        "PolyForm-Noncommercial-1.0.0",
        "OLDAP-2.0",
        "CDDL-1.0",
        "APL-1.0",
        "dvipdfm",
        "XSkat",
        "Spencer-86",
        "NLOD-1.0",
        "W3C-19980720",
        "BSD-2-Clause-Patent",
        "AMPAS",
        "AGPL-3.0-or-later",
        "RPL-1.1",
        "Parity-7.0.0",
        "OLDAP-1.1",
        "AFL-1.1",
        "Artistic-1.0-cl8",
        "FTL",
        "Dotseqn",
        "CC-BY-NC-ND-2.5",
        "GFDL-1.2-no-invariants-only",
        "PHP-3.0",
        "CC-BY-SA-2.0-UK",
        "BSD-3-Clause-Modification",
        "LAL-1.3",
        "gSOAP-1.3b",
        "StandardML-NJ",
        "NPOSL-3.0",
        "LGPL-3.0",
        "Artistic-1.0-Perl",
        "OLDAP-2.5",
        "BSD-2-Clause-Views",
    ]

    MAX_LIMIT_FOR_CODE_ISSUES = 1000
    INT_DEFAULT_LIMIT = 50
    DEFAULT_PAGE = 0

    """ CLIENT CLASS """


    class Client(BaseClient):
        def __init__(
            self,
            server_url: str,
            verify: bool,
            proxy: bool,
            headers: Dict[str, str],
            username: str,
            password: str,
            mirror_direction: str | None,
            close_incident: bool,
            close_alert: bool,
            is_test_module: bool,
        ):
            super().__init__(base_url=server_url, verify=verify, proxy=proxy, headers=headers)
            self.mirror_direction = mirror_direction
            self.close_incident = close_incident
            self.close_alert = close_alert
            self.retries = 0 if is_test_module else 2
            self.timeout = 15 if is_test_module else self.timeout
            self.generate_auth_token(username, password)

        def generate_auth_token(self, username: str, password: str) -> None:
            """
            Logins and generates a JSON Web Token (JWT) for authorization.
            The token is valid for 10 minutes.
            """
            data = {"username": username, "password": password}
            demisto.debug("Sending request to get the auth token")

            response = self._http_request("POST", "login", json_data=data, retries=self.retries, timeout=self.timeout)
            try:
                token = response.get("token")
                if not token:
                    raise DemistoException(f'Could not retrieve token from server: {response.get("message")}', res=response)
            except ValueError as exception:
                raise DemistoException("Could not parse API response.", exception=exception) from exception

            demisto.debug("Successfully got the auth token")
            self._headers[REQUEST_CSPM_AUTH_HEADER] = token

        def code_issues_list_request(self, body):
            return self._http_request("POST", "/code/api/v2/code-issues/branch_scan", json_data=body)

        def alert_filter_list_request(self):
            return self._http_request("GET", "filter/alert/suggest")

        def alert_search_request(
            self,
            time_range: Dict[str, Any],
            filters: List[str],
            limit: Optional[int] = None,
            detailed: Optional[str] = None,
            page_token: Optional[str] = None,
            sort_by: Optional[List[str]] = None,
        ):
            params = assign_params(detailed=detailed)
            data = remove_empty_values(
                {
                    "limit": limit,
                    "filters": handle_filters(filters),
                    "timeRange": time_range,
                    "sortBy": sort_by,
                    "pageToken": page_token,
                }
            )
            demisto.info(f"Executing Prisma Cloud alert search with payload: {data}")

            return self._http_request("POST", "v2/alert", params=params, json_data=data, retries=self.retries)

        def alert_get_details_request(self, alert_id: str, detailed: Optional[str] = None):
            params = assign_params(detailed=detailed)

            response = self._http_request("GET", f"alert/{alert_id}", params=params)
            self._concatenate_url(response, "resource.url")

            return response

        def _concatenate_url(self, response: Dict[str, Any], url_field: str = "url") -> None:
            """
            Concatenates a url suffix with the base url for the places where only a suffix is returned, so urls we display will be
            clickable and lead to a real place.
            Updates the dict given.
            """
            split_url_field = url_field.split(".")
            if len(split_url_field) > 1:
                self._concatenate_url(response.get(split_url_field[0], {}), ".".join(split_url_field[1:]))

            elif url_field in response:
                url = urljoin(self._base_url, response[url_field]).replace("https://api", "https://app")
                response[url_field] = url

        def alert_dismiss_request(
            self,
            dismissal_note: str,
            time_range: Dict[str, Any],
            alert_ids: Optional[List[str]] = None,
            policy_ids: Optional[List[str]] = None,
            dismissal_time_range: Optional[Dict[str, Any]] = None,
            filters: Optional[List[str]] = None,
        ):
            data = remove_empty_values(
                {
                    "alerts": alert_ids,
                    "policies": policy_ids,
                    "dismissalNote": dismissal_note,
                    "dismissalTimeRange": dismissal_time_range,
                    "filter": {
                        "timeRange": time_range,
                        "filters": handle_filters(filters),
                    },
                }
            )

            self._http_request("POST", "alert/dismiss", json_data=data, resp_type="response")

        def alert_reopen_request(
            self,
            time_range: Dict[str, Any],
            alert_ids: Optional[List[str]] = None,
            policy_ids: Optional[List[str]] = None,
            filters: Optional[List[str]] = None,
        ):
            data = remove_empty_values(
                {
                    "alerts": alert_ids,
                    "policies": policy_ids,
                    "dismissalTimeRange": time_range,
                    "filter": {
                        "timeRange": time_range,
                        "filters": handle_filters(filters),
                    },
                }
            )

            self._http_request("POST", "alert/reopen", json_data=data, resp_type="response")

        def remediation_command_list_request(
            self, time_range: Dict[str, Any], alert_ids: Optional[List[str]] = None, policy_id: Optional[str] = None
        ):
            data = remove_empty_values(
                {
                    "alerts": alert_ids,
                    "filter": {"timeRange": time_range},  # all other filters are ignored by API
                    "policies": [policy_id],
                }
            )

            return self._http_request("POST", "alert/remediation", json_data=data)

        def alert_remediate_request(self, alert_id: str):
            self._http_request("PATCH", f"alert/remediation/{alert_id}", resp_type="response")

        def config_search_request(
            self,
            time_range: Dict[str, Any],
            query: str,
            limit: Optional[int] = None,
            search_id: Optional[str] = None,
            sort_direction: Optional[str] = None,
            sort_field: Optional[str] = None,
            include_resource_json: Optional[str] = "true",
            heuristic_search: Optional[str] = "true",
        ):
            total_items = []
            page_count = 1
            data = remove_empty_values(
                {
                    "id": search_id,
                    "limit": limit,
                    "query": query,
                    "sort": [{"direction": sort_direction, "field": sort_field}],
                    "timeRange": time_range,
                    "withResourceJson": include_resource_json,
                    "heuristicSearch": heuristic_search,
                }
            )

            first_page_response = self._http_request("POST", "search/config", json_data=data)
            first_page_data = first_page_response.get("data", {})

            items = first_page_data.get("items", [])
            total_items.extend(items)
            demisto.debug(f"POST search/config - {len(items)} items were fetched from page {page_count}")

            page_limit = limit - len(items) if limit is not None else 0
            next_page_token = first_page_data.get("nextPageToken")

            while next_page_token and page_limit:
                data["pageToken"] = next_page_token
                data["limit"] = page_limit
                page_count += 1

                response = self._http_request("POST", "search/config/page", json_data=data)
                items = response.get("items", [])
                demisto.debug(f"POST search/config/page - {len(items)} items were fetched from page {page_count}")
                page_limit -= len(items)
                total_items.extend(items)

                next_page_token = response.get("nextPageToken", "")

            return total_items

        def event_search_request(
            self, time_range: Dict[str, Any], query: str, limit: Optional[int] = None, sort_by: Optional[List[Dict[str, str]]] = None
        ):
            data = remove_empty_values(
                {
                    "limit": limit,
                    "query": query,
                    "timeRange": time_range,
                    "sort": sort_by,
                }
            )

            return self._http_request("POST", "search/event", json_data=data)

        def network_search_request(
            self, query: str, time_range: Dict[str, Any], search_id: Optional[str] = None, cloud_type: Optional[str] = None
        ):
            data = remove_empty_values(
                {
                    "cloudType": cloud_type,
                    "id": search_id,
                    "query": query,
                    "timeRange": time_range,
                }
            )

            return self._http_request("POST", "search", json_data=data)

        def resource_get_request(self, rrn: str):
            data = remove_empty_values({"rrn": rrn})

            return self._http_request("POST", "resource", json_data=data)

        def resource_list_request(self, list_type: Optional[str]):
            params = assign_params(listType=list_type)

            return self._http_request("GET", "v1/resource_list", params=params)

        def user_roles_list_request(self, role_id: str):
            return self._http_request("GET", f"user/role/{role_id}")

        def all_user_roles_list_request(self):
            return self._http_request("GET", "user/role")

        def users_list_request(self):
            return self._http_request("GET", "v3/user")

        def account_list_request(self, exclude_account_group_details: str):
            data = remove_empty_values({"excludeAccountGroupDetails": exclude_account_group_details})

            return self._http_request("GET", "cloud", json_data=data)

        def account_status_get_request(self, account_id: str):
            return self._http_request("GET", f"account/{account_id}/config/status")

        def account_owner_list_request(self, account_id: str):
            return self._http_request("GET", f"cloud/{account_id}/owners")

        def host_finding_list_request(
            self, rrn: str, finding_types: Optional[List[str]] = None, risk_factors: Optional[List[str]] = None
        ):
            data = remove_empty_values({"rrn": rrn, "findingType": finding_types, "riskFactors": risk_factors})

            return self._http_request("POST", "resource/external_finding", json_data=data)

        def permission_list_request(self, query: str, limit: int, user_id: Optional[str] = None):
            data = remove_empty_values({"id": user_id, "limit": limit, "query": query})

            return self._http_request("POST", "api/v1/permission", json_data=data)

        def permission_list_next_page_request(self, next_token: str, limit: int):
            data = remove_empty_values({"limit": limit, "pageToken": next_token})

            return self._http_request("POST", "api/v1/permission/page", json_data=data)

        # Prisma Cloud Code Security module requests

        def trigger_scan_request(self):
            headers = self._headers
            headers[REQUEST_CCS_AUTH_HEADER] = headers.pop(REQUEST_CSPM_AUTH_HEADER)

            return self._http_request("POST", "code/api/v1/scans/integrations", headers=headers)

        def error_file_list_request(
            self,
            repository: str,
            source_types: List[str],
            cicd_run_id: Optional[float] = None,
            authors: Optional[List[str]] = None,
            branch: Optional[str] = None,
            categories: Optional[List[str]] = None,
            code_status: Optional[str] = None,
            file_types: Optional[List[str]] = None,
            repository_id: Optional[str] = None,
            search_options: Optional[List[str]] = None,
            search_text: Optional[str] = None,
            search_title: Optional[str] = None,
            severities: Optional[List[str]] = None,
            tags: Optional[List[str]] = None,
            statuses: Optional[List[str]] = None,
        ):
            data = remove_empty_values(
                {
                    "CICDRunId": cicd_run_id,
                    "authors": authors,
                    "branch": branch,
                    "categories": categories,
                    "codeStatus": [code_status],
                    "fileTypes": file_types,
                    "repository": repository,
                    "repositoryId": repository_id,
                    "severities": severities,
                    "sourceTypes": source_types,
                    "tags": handle_tags(tags),
                    "types": statuses,
                }
            )
            if search_title:
                data["search"] = {"options": search_options, "text": search_text, "title": search_title}
            elif search_options or search_text:
                data["search"] = {"options": search_options, "text": search_text}

            headers = self._headers
            headers[REQUEST_CCS_AUTH_HEADER] = headers.pop(REQUEST_CSPM_AUTH_HEADER)

            return self._http_request("POST", "code/api/v1/errors/files", json_data=data, headers=headers)

        def access_key_creation(self, args: Dict[str, Any]) -> Dict[str, Any]:
            """
            Create an access key using the provided arguments.

            Args:
            - args (Dict[str, Any]): A dictionary containing the required parameters for key creation.
                - name (str): required
                - expires-on (str): optional
            Returns:
            - Dict[str, Any]: A dictionary containing the response from the API request to create the access key.
            """
            payload = {"name": args.get("name")}
            if args.get("expires-on"):
                dateparser_datetime_object = dateparser.parse(args.get("expires-on", ""), settings={"PREFER_DATES_FROM": "future"})
                if isinstance(dateparser_datetime_object, datetime):
                    payload["expiresOn"] = dateparser_datetime_object.strftime("%s") + "000"  # API require milliseconds timestamp
            return self._http_request(method="POST", url_suffix="/access_keys", data=json.dumps(payload))

        def get_access_key_by_id(self, args: Dict[str, Any]) -> Dict[str, Any]:
            return self._http_request(method="GET", url_suffix=f"/access_keys/{args.get('access-key')}")

        def get_access_keys_list(self) -> List[Dict[str, Any]]:
            return self._http_request(method="GET", url_suffix="/access_keys")

        def patch_access_key_disable(self, access_key: str) -> Dict[str, Any]:
            return self._http_request(method="PATCH", url_suffix=f"/access_keys/{access_key}/status/false", resp_type="content")

        def patch_access_key_enable(self, access_key: str) -> Dict[str, Any]:
            return self._http_request(method="PATCH", url_suffix=f"/access_keys/{access_key}/status/true", resp_type="content")

        def access_key_deletion(self, access_key: str) -> Dict[str, Any]:
            return self._http_request(method="DELETE", url_suffix=f"/access_keys/{access_key}", resp_type="content")

        def get_asset_by_id(self, params: Dict[str, Any]) -> Dict[str, Any]:
            return self._http_request(method="POST", url_suffix="/uai/v1/asset", json_data=params)


    """ HELPER FUNCTIONS """


    def format_url(url: str) -> str:
        """
        Formats the URL from the regular one (with 'app') to the API one (with 'api'), in order to support providing both options.
        """
        return urljoin(url.replace("https://app", "https://api"), "")


    def extract_nested_values(readable_response: dict, nested_headers: Dict[str, str]) -> None:
        """
        Extracts nested fields from readable_response according to nested_headers keys,
        and names them according to nested_headers values.
        Updates readable_response in place.
        """
        for nested_name, new_name in nested_headers.items():
            nested_name_parts = nested_name.split(".")

            nested_value = readable_response
            for index, part in enumerate(nested_name_parts):
                nested_value = nested_value.get(part)  # type: ignore[assignment]
                if index == (len(nested_name_parts) - 1):
                    readable_response[new_name] = nested_value
                elif not nested_value:
                    break


    def change_timestamp_to_datestring_in_dict(readable_response: dict) -> None:
        """
        Changes the values of the time fields in the given response from epoch timestamp to human readable date format.
        """
        for field in TIME_FIELDS:
            if epoch_value := readable_response.get(field):
                # a value of -1 means that it never happened, so we leave the field empty
                readable_response[field] = timestamp_to_datestring(epoch_value, DATE_FORMAT) if epoch_value != -1 else None


    def convert_date_to_unix(date_str: str) -> int:
        """
        Convert the given string to milliseconds since epoch.
        """
        if not (date := dateparser.parse(date_str, settings={"TIMEZONE": "UTC"})):
            raise DemistoException(f'The date "{date_str}" given is not valid.')
        return int(date.timestamp() * 1000)


    def handle_time_filter(
        base_case: Optional[Dict[str, Any]] = None,
        unit_value: Optional[str] = None,
        amount_value: Optional[int] = None,
        time_from: Optional[str] = None,
        time_to: Optional[str] = None,
    ) -> Dict[str, Any]:
        """
        Create the relevant time filter to be sent in the POST request body, under "timeRange".
        This doesn't deal with the way the time range should be sent in the GET request parameters.
        """
        if (time_from or time_to) and (unit_value or amount_value):
            raise DemistoException(ERROR_TOO_MANY_ARGS)
        elif (time_from and not time_to) or (amount_value and not unit_value):
            raise DemistoException(ERROR_NOT_ENOUGH_ARGS)

        if unit_value:
            if amount_value:
                # amount is only for relative time - defines a window of time from a given point of time in the past until now
                if unit_value not in RELATIVE_TIME_UNIT_OPTIONS:
                    raise DemistoException(ERROR_RELATIVE_TIME_UNIT)
                return {"type": "relative", "value": {"amount": arg_to_number(amount_value), "unit": unit_value}}

            else:
                # using to_now time - represents a window of time from the start of the time unit given until now
                if unit_value not in TO_NOW_TIME_UNIT_OPTIONS:
                    raise DemistoException(ERROR_TO_NOW_TIME_UNIT)
                return {"type": "to_now", "value": unit_value}

        elif time_to:
            # using absolute time
            if time_from:
                return {
                    "type": "absolute",
                    "value": {"startTime": convert_date_to_unix(time_from), "endTime": convert_date_to_unix(time_to)},
                }
            else:
                # alert dismissal requires only an end time in the future
                return {"type": "absolute", "value": {"endTime": convert_date_to_unix(time_to)}}

        return base_case or TIME_FILTER_BASE_CASE


    def handle_filters(filters: Optional[List[str]]) -> List[Dict[str, Any]]:
        """
        Creates the list of filters in the format that the request expects.
        The filters are given from the user as a comma-separated list, in the format of filtername=filtervalue.
        """
        filters_to_send = []
        for filter_ in filters or []:
            split_filter = filter_.split("=")
            if len(split_filter) != 2 or not split_filter[0] or not split_filter[1]:
                raise DemistoException(
                    'Filters should be in the format of "filtername1=filtervalue1,filtername2=filtervalue2". '
                    f'The filter "{filter_}" doesn\'t meet this requirement.'
                )
            filters_to_send.append({"name": split_filter[0], "operator": "=", "value": split_filter[1]})
        return filters_to_send


    def handle_tags(tags: Optional[List[str]]) -> List[Dict[str, Any]]:
        """
        Creates the list of tags in the format that the request expects.
        The tags are given from the user as a comma-separated list, in the format of tagkey=tagvalue.
        """
        tags_to_send = []
        if tags:
            for tag in tags:
                split_tag = tag.split("=")
                if len(split_tag) != 2 or not split_tag[0] or not split_tag[1]:
                    raise DemistoException(
                        'Tags should be in the format of "tagkey1=tagvalue1,tagkey2=tagvalue2". '
                        f'The tag "{tag}" doesn\'t meet this requirement.'
                    )
                tags_to_send.append({"key": split_tag[0], "value": split_tag[1]})
        return tags_to_send


    def validate_array_arg(array_arg: List[str], arg_name: str, arg_options: List[str]) -> None:
        """
        Validates that all comma-separated provided arg values are in the available options.
        """
        if not set(array_arg).issubset(arg_options):
            raise DemistoException(f'{arg_name} values are unexpected, must be of the following: {", ".join(arg_options)}.')


    def error_file_list_command_args_validation(
        source_types: List[str],
        categories: List[str],
        statuses: List[str],
        file_types: List[str],
        search_options: List[str],
        severities: List[str],
    ) -> None:
        """
        Validates values for all multi-select args in 'prisma-cloud-error-file-list' command.
        """
        validate_array_arg(categories, "Categories", CATEGORIES_OPTIONS)
        validate_array_arg(file_types, "File types", FILE_TYPES_OPTIONS)
        validate_array_arg(search_options, "Search options", SEARCH_OPTIONS_OPTIONS)
        validate_array_arg(severities, "Severities", SEVERITIES_OPTIONS)
        validate_array_arg(source_types, "Source types", SOURCE_TYPES_OPTIONS)
        validate_array_arg(statuses, "Statuses", STATUSES_OPTIONS)


    def remove_empty_values(value_to_reduce: Union[Dict[str, Any], List, Any]):
        """
        Removes empty values from given dict or list and from the nested dicts and lists in it.
        """
        if isinstance(value_to_reduce, dict):
            reduced_dict = {}
            for key, value in value_to_reduce.items():
                if reduced_nested_value := remove_empty_values(value):
                    reduced_dict[key] = reduced_nested_value
            return reduced_dict

        elif isinstance(value_to_reduce, list):
            reduced_list = []
            for item in value_to_reduce:
                if reduced_nested_value := remove_empty_values(item):
                    reduced_list.append(reduced_nested_value)
            return reduced_list

        return value_to_reduce


    def get_response_status_header(response: requests.Response) -> str:
        """
        The status of the error raised from Prisma Cloud appears in the response header.
        This function returns the status header from the response got from Prisma Cloud.
        """
        if hasattr(response, "headers"):
            return response.headers.get(RESPONSE_STATUS_HEADER, "")
        return ""


    def calculate_offset(page_size: int, page_number: int) -> tuple[int, int]:
        """
        Prisma Cloud receives offset and limit arguments. To follow our convention, we receive page_size and page_number arguments and
        calculate the offset from them.
        The offset is the start point from which to retrieve values, zero based. It starts at 0.
        NOTICE: Currently the offset argument is not working in the API, so this is not used until further update by Prisma Cloud.

        :param page_size: The number of results to show in one page.
        :param page_number: The page number to show, starts at 1.
        """
        if page_size > PAGE_SIZE_MAX_VALUE:
            raise DemistoException(f'Maximum "page_size" value is {PAGE_SIZE_MAX_VALUE} (got {page_size}).')

        return page_size, page_size * (page_number - 1)


    def extract_namespace(response_items: List[Dict[str, Any]]):
        """
        Extract namespaces from resource list items.
        """
        for item in response_items:
            for member in item.get("members", []):  # members is a list of dict or strs
                if isinstance(member, str | int):
                    continue
                if item_namespace := member.get("namespaces", []):
                    item["namespaces"] = item_namespace
                    break


    def remove_additional_resource_fields(items):
        if items:
            for current_item in list(items):
                data = current_item.get("data", {})

                disks = data.get("disks", [])
                for current_disk in disks:
                    if "shieldedInstanceInitialState" in current_disk:
                        del current_disk["shieldedInstanceInitialState"]

                metadata = data.get("metadata", {})
                metadata_items = metadata.get("items", [])
                for current_metadata_item in list(metadata_items):
                    if "key" in current_metadata_item and current_metadata_item["key"] == "configure-sh":
                        metadata_items.remove(current_metadata_item)


    """ FETCH AND MIRRORING HELPER FUNCTIONS """


    def translate_severity(alert: Dict[str, Any]) -> float:
        """
        Translate alert severity to XSOAR
        """
        severity = alert.get("policy", {}).get("severity")
        return PRISMA_TO_XSOAR_SEVERITY.get(severity, IncidentSeverity.UNKNOWN)


    def expire_stored_ids(fetched_ids: Dict[str, int], updated_last_run_time: int, look_back: int) -> Dict[str, int]:
        """
        Expires stored ids when their alert time will not be fetched in the next fetch.

        Args:
            fetched_ids: Dict from fetched ids to the epoch alert time from Prisma Cloud.
            updated_last_run_time: epoch time for next fetch
            look_back: minutes to add to the next fetch time

        Returns:
            The dict of fetched ids that their time is after the next fetch time.
        """
        if not fetched_ids:
            return {}
        cleaned_cache = {}

        next_fetch_epoch = add_look_back(updated_last_run_time, look_back * 3)  # in case look_back needs to be increased
        # to avoid duplicate incidents, make sure that when increasing this value, to increase it up to a multiplier of 3 each time

        for fetched_id, alert_time in fetched_ids.items():
            if alert_time >= next_fetch_epoch:  # keep if it is later
                cleaned_cache[fetched_id] = alert_time
        return cleaned_cache


    def calculate_fetch_time_range(
        now: int, first_fetch: str, look_back: int = 0, last_run_time: Optional[int] = None
    ) -> Dict[str, Any]:
        if last_run_time:
            last_run_time = add_look_back(int(last_run_time), look_back)
        else:  # first fetch
            last_run_time = convert_date_to_unix(first_fetch)

        return {"type": "absolute", "value": {"startTime": last_run_time, "endTime": now}}


    def add_look_back(last_run_epoch_time: int, look_back_minutes: int) -> int:
        look_back_epoch = look_back_minutes * 60 * 1000
        return last_run_epoch_time - look_back_epoch


    def fetch_request(
        client: Client, fetched_ids: Dict[str, int], filters: List[str], limit: int, now: int, time_range: Dict[str, Any]
    ) -> tuple[List[Dict[str, Any]], Dict[str, int], int]:
        response = client.alert_search_request(
            time_range=time_range,
            filters=filters,
            detailed="true",
            sort_by=["alertTime:asc"],  # adding sort by 'id:asc' doesn't work
            limit=limit + len(fetched_ids),
        )
        response_items = response.get("items", [])
        demisto.debug(f"Finished request, got {len(response_items)} items")
        updated_last_run_time_epoch = response_items[-1].get("alertTime") if response_items else now
        incidents = filter_alerts(client, fetched_ids, response_items, limit)

        # there is a 'nextPageToken' value even if we already got all the results
        while len(incidents) < limit and response.get("nextPageToken") and response.get("items"):
            # only page_token is being used, also sending other arguments because it is not stated clearly in the
            # API documentation
            response = client.alert_search_request(
                time_range=time_range,
                filters=filters,
                detailed="true",
                sort_by=["alertTime:asc"],
                limit=limit + len(fetched_ids),
                page_token=response.get("nextPageToken"),
            )
            response_items = response.get("items", [])
            demisto.debug(f"Finished request, got {len(response_items)} items.")
            updated_last_run_time_epoch = response_items[-1].get("alertTime") if response_items else updated_last_run_time_epoch
            incidents.extend(filter_alerts(client, fetched_ids, response_items, limit, len(incidents)))

        return incidents, fetched_ids, updated_last_run_time_epoch


    def filter_alerts(
        client: Client, fetched_ids: Dict[str, int], response_items: List[Dict[str, Any]], limit: int, num_of_prev_incidents: int = 0
    ) -> List[Dict[str, Any]]:
        incidents = []

        for alert in response_items:
            alert_id = alert.get("id")
            if alert_id in fetched_ids:
                demisto.debug(f"Alert {alert_id} already fetched. Skipping.")
                # Update alert time if changed (Prisma Cloud may update alert time for the same alert we already fetched,
                # potentially causing us to re-fetch it).
                if fetched_ids[alert_id] != alert["alertTime"]:
                    fetched_ids[alert_id] = alert["alertTime"]
                    demisto.debug(f"Overwriting alert time in lookback for alert {alert_id} to be {alert['alertTime']}")
                continue

            demisto.debug(f'Processing new fetched alert {alert.get("id")}.')
            add_mirroring_fields(client, alert)
            incidents.append(alert_to_incident_context(alert))
            fetched_ids[str(alert["id"])] = int(alert["alertTime"])

            if len(incidents) + num_of_prev_incidents >= limit:
                break

        return incidents


    def add_mirroring_fields(client: Client, alert: Dict):
        """
        Updates the given Prisma Cloud fetched alert to hold the needed mirroring fields.

        Args:
            client: Demisto client.
            alert: The Prisma Cloud fetched alert.
        """
        alert["mirror_direction"] = client.mirror_direction
        alert["mirror_instance"] = demisto.integrationInstance()


    def alert_to_incident_context(alert: Dict[str, Any]) -> Dict[str, Any]:
        incident_context = {
            "name": alert.get("policy", {}).get("name", "No policy") + " - " + alert.get("id"),
            "occurred": timestamp_to_datestring(alert.get("alertTime"), DATE_FORMAT),
            "severity": translate_severity(alert),
            "rawJSON": json.dumps(alert),
        }
        demisto.debug(
            f'New PrismaCloud incident is: name: {incident_context["name"]}, occurred: '
            f'{incident_context["occurred"]}, severity: {incident_context["severity"]}.'
        )
        return incident_context


    def get_remote_alert_data(client: Client, remote_alert_id: str) -> tuple[Dict, Dict]:
        """
        Called every time get-remote-data command runs on an alert.
        Gets the details of the relevant alert entity from the remote system (Prisma Cloud).
        Takes from the entity only the relevant incoming mirroring fields, and returns the updated_object for the incident
        selected to mirror in.

        Args:
            client: Demisto client.
            remote_alert_id: The id of the mirrored Prisma Cloud alert.

        Returns: The raw alert's details, and a dictionary contains only the mirrored fields and their values.
        """
        alert_details = client.alert_get_details_request(alert_id=remote_alert_id, detailed="true")

        updated_object: Dict[str, Any] = {}
        for field in INCIDENT_INCOMING_MIRROR_ARGS:
            mirrored_field_value = demisto.get(alert_details, field, "")
            if "." in field:  # field is nested (currently it is only the policy.name field)
                # policy.name field was added to the mirrored fields cause this is the field that is used in the default classifier to
                # classify incident types. Without it, all mirrored alerts will be changed to the default incident type which is
                # 'Prisma Cloud'.
                split_field = field.split(".")
                updated_object[split_field[0]] = {split_field[1]: mirrored_field_value}
            else:
                updated_object[field] = mirrored_field_value
        return alert_details, updated_object


    def close_incident_in_xsoar(remote_alert_id: str, mirrored_status: str, mirrored_dismissal_note: str) -> Dict:
        """
        Closes an XSOAR incident.

        Args:
            remote_alert_id: The id of the mirrored Prisma Cloud alert to be closed.
            mirrored_status: The status of the mirrored Prisma alert.
            mirrored_dismissal_note: The dismissal note of the mirrored Prisma alert.

        Returns: An entry object with relevant data for closing an XSOAR incident.
        """
        demisto.debug(f"Prisma Alert {remote_alert_id} was closed. Closing the mirrored incident in XSOAR")
        if mirrored_status == "resolved" and mirrored_dismissal_note == "":
            mirrored_dismissal_note = "resolved"

        closing_time = datetime.now()
        parsed_closing_time = closing_time.strftime(DATE_FORMAT)

        entry = {
            "Type": EntryType.NOTE,
            "Contents": {
                "dbotIncidentClose": True,
                "rawCloseReason": mirrored_status,
                "closeReason": f"Alert was {mirrored_status} on Prisma Cloud.",
                "closeNotes": mirrored_dismissal_note,
                "closed": parsed_closing_time,
            },
            "ContentsFormat": EntryFormat.JSON,
        }
        return entry


    def reopen_incident_in_xsoar(remote_alert_id: str):
        """
        Reopens an XSOAR incident.

        Args:
            remote_alert_id:  The id of the mirrored Prisma Cloud alert to be reopened.

        Returns: An entry object with relevant data for reopening an XSOAR incident.
        """
        demisto.debug(f"Prisma Alert {remote_alert_id} was reopened. Reopening the incident on XSOAR.")
        entry = {"Type": EntryType.NOTE, "Contents": {"dbotIncidentReopen": True}, "ContentsFormat": EntryFormat.JSON}
        return entry


    def set_xsoar_incident_entries(updated_object: Dict[str, Any], remote_alert_id: str) -> Dict | None:
        """
        Extracts the status of the mirrored Prisma alert, and close/reopen the matched XSOAR incident in accordance.
        Args:
            updated_object: A dictionary contains the mirrored relevant fields.
            remote_alert_id: The id of the mirrored Prisma Cloud alert.

        Returns: An entry object with relevant data for closing or reopening an XSOAR incident.
        """
        mirrored_status = updated_object.get("status")
        if mirrored_status in set(INCIDENT_INCOMING_MIRROR_CLOSING_STATUSES):  # closing incident
            mirrored_dismissal_note = updated_object.get("dismissalNote", "")
            entry = close_incident_in_xsoar(remote_alert_id, mirrored_status, mirrored_dismissal_note)
            return entry
        elif mirrored_status == INCIDENT_INCOMING_MIRROR_REOPENING_STATUS:  # re-opening incident
            entry = reopen_incident_in_xsoar(remote_alert_id)
            return entry
        return None


    def close_alert_in_prisma_cloud(client: Client, ids: List[str], delta: Dict[str, Any]):
        """
        Close (Dismiss) an alert in Prisma Cloud.

        Args:
            client: Demisto client.
            ids: The IDs of the alerts to be dismissed.
            delta: A dictionary of fields that changed from the last update - containing only the changed fields.

        Returns: None.

        """
        close_notes = delta.get("closeNotes")
        close_reason = delta.get("closeReason")
        dismissal_note = f"{INCIDENT_OUTGOING_MIRROR_DISMISSAL_NOTE} - Closing Reason: {close_reason}, Closing Notes: {close_notes}."
        time_filter = handle_time_filter(base_case=TIME_FILTER_BASE_CASE)

        client.alert_dismiss_request(dismissal_note=dismissal_note, time_range=time_filter, alert_ids=ids)


    def reopen_alert_in_prisma_cloud(client: Client, ids: List[str]):
        """
        Re-open an alert in Prisma Cloud.

        Args:
            client: Demisto client.
            ids: The IDs of the alerts to be re-opened.

        Returns: None.

        """
        time_filter = handle_time_filter(base_case=TIME_FILTER_BASE_CASE)
        client.alert_reopen_request(time_range=time_filter, alert_ids=ids)


    def whether_to_close_in_prisma_cloud(user_selection: bool, delta: Dict[str, Any]) -> bool:
        """
        Closing in the remote system should happen only when both:
            1. The user asked for it.
            2. One of the closing fields appears in the delta.

        The second condition is mandatory so we will not send a closing request at all of the mirroring requests that happen
        after closing an incident (in case where the incident is updated so there is a delta, but it is not the status
        that was changed).

        Args:
            user_selection: True if the user wants to mirror out closing of an incident ('close_alert' integration parameter),
                            otherwise - False.
            delta: A dictionary of fields that changed from the last update - containing only the changed fields.

        Returns: True - if closing the prisma alert is needed, False - otherwise.

        """
        closing_fields = {"closeReason", "closingUserId", "closeNotes"}
        return user_selection and any(field in delta for field in closing_fields)


    def whether_to_reopen_in_prisma_cloud(user_selection: bool, delta: Dict[str, Any]) -> bool:
        """
        Re-opening in the remote system should happen only when both:
            1. The user asked for it.
            2. The delta contains the 'closingUserId' field.

        The second condition is mandatory so we will not send a re-opening request in case where the incident is updated so there is
        a delta, but it is not the status that was changed.

        Args:
            user_selection: True if the user wants to mirror out re-opening of an incident ('close_alert' integration parameter),
                            otherwise - False.
            delta: A dictionary of fields that changed from the last update - containing only the changed fields.

        Returns: True - if re-opening the prisma alert is needed, False - otherwise.

        """
        return user_selection and "closingUserId" in delta


    def update_remote_alert(client: Client, delta: Dict[str, Any], inc_status: IncidentStatus, incident_id: str):
        """
        Updates the remote prisma alert according to the changes of the mirrored xsaor incident.
        The possible updates are closing or re-opening the alert.

        When closing an incident in XSOAR the possible reasons are: False Positive, Duplicate, Other and Resolved.
        As a result, the remote Prisma alert will be closed with a 'Dismissed' status, and the reason and close notes will be added to
        the 'Reason' field of the remote alert.
        * It is not possible to close an incident as 'Resolved' in XSOAR and mark it as 'Resolved' in Prisma,
        it will be closed with a 'dismissed' status even if the selected reason is 'Resolved'.

        Args:
            client: Demisto client.
            delta: A dictionary of fields that changed from the last update - containing only the changed fields.
            inc_status: The status of the incident.
            incident_id: Contains the value of the dbotMirrorId field, which represents the ID of the alert in prisma.

        Returns: None.
        """
        # XSOAR incident was closed - closing the mirrored prisma alert
        if inc_status == IncidentStatus.DONE and whether_to_close_in_prisma_cloud(client.close_alert, delta):
            demisto.debug(f"Closing incident with remote ID {incident_id} in remote system.")
            close_alert_in_prisma_cloud(client, [incident_id], delta)
            demisto.debug(f"Remote Incident: {incident_id} was updated successfully.")

        # XSOAR incident was re-opened - re-opening the mirrored prisma alert
        elif inc_status == IncidentStatus.ACTIVE and whether_to_reopen_in_prisma_cloud(client.close_alert, delta):
            demisto.debug(f"Reopening incident with remote ID {incident_id} in remote system.")
            reopen_alert_in_prisma_cloud(client, [incident_id])
            demisto.debug(f"Remote Incident: {incident_id} was updated successfully.")

        else:
            demisto.debug(f"Skipping the update of remote incident {incident_id} as it has not closed or re-opened in XSOAR.")


    """ V1 DEPRECATED COMMAND FUNCTIONS to support backwards compatibility """

    RISK_GRADE_NOT_SUPPORTED_MSG = (
        'In the new API version of Prisma Cloud, "risk-grade" argument is not supported '
        "and therefore removed from the available command arguments."
    )


    def get_v1_filters(args: Dict[str, Any]) -> List[str]:
        """
        Transform V1 arguments to V2 filters list.
        """
        filters = []
        args_name_to_filter_name = {
            "alert-status": "alert.status",
            "policy-name": "policy.name",
            "policy-label": "policy.label",
            "policy-compliance-standard": "policy.complianceStandard",
            "cloud-account": "cloud.account",
            "cloud-account-id": "cloud.accountId",
            "cloud-region": "cloud.region",
            "alert-rule-name": "alertRule.name",
            "resource-id": "resource.id",
            "resource-name": "resource.name",
            "resource-type": "resource.type",
            "alert-id": "alert.id",
            "cloud-type": "cloud.type",
            "policy-type": "policy.type",
            "policy-severity": "policy.severity",
        }
        for arg_name, filter_name in args_name_to_filter_name.items():
            if arg_value := args.get(arg_name):
                filters.append(f"{filter_name}={arg_value}")

        return filters


    def alert_to_v1_context(alert: Any, args: Dict[str, Any]) -> Dict[str, Any]:
        """
        This was copied from V1 and should not be maintained.
        Transform a single alert to context struct.
        """
        ec = {
            "ID": alert.get("id"),
            "Status": alert.get("status"),
            "AlertTime": alert.get("alertTime"),
            "Policy": {
                "ID": demisto.get(alert, "policy.policyId"),
                "Name": demisto.get(alert, "policy.name"),
                "Type": demisto.get(alert, "policy.policyType"),
                "Severity": demisto.get(alert, "policy.severity"),
                "Remediable": demisto.get(alert, "policy.remediable"),
            },
            "Resource": {
                "ID": demisto.get(alert, "resource.id"),
                "Name": demisto.get(alert, "resource.name"),
                "Account": demisto.get(alert, "resource.account"),
                "AccountID": demisto.get(alert, "resource.accountId"),
            },
        }
        if "resource_keys" in args:
            # if resource_keys argument was given, include those items from resource.data
            extra_keys = demisto.getArg("resource_keys")
            resource_data = {}
            keys = extra_keys.split(",")
            for key in keys:
                resource_data[key] = demisto.get(alert, f"resource.data.{key}")

            ec["Resource"]["Data"] = resource_data

        if alert.get("alertRules"):
            ec["AlertRules"] = [alert_rule.get("name") for alert_rule in alert.get("alertRules")]

        return ec


    def format_v1_response(response: Any) -> Any:
        """
        This was copied from V1 and should not be maintained.
        """
        if response and isinstance(response, dict):
            response = {pascalToSpace(key).replace(" ", ""): format_v1_response(value) for key, value in response.items()}
        elif response and isinstance(response, list):
            response = [format_v1_response(item) for item in response]
        return response


    def alert_search_v1_command(
        client: Client, args: Dict[str, Any], return_v1_output: bool
    ) -> Union[CommandResults, List[Union[CommandResults, str]], Dict]:
        """
        This command is for supporting backwards compatibility, to make transition to V2 easier for users with custom playbooks.
        """
        new_args = {
            "time_range_unit": args.get("time-range-unit"),
            "time_range_value": args.get("time-range-value"),
            "time_range_date_from": args.get("time-range-date-from"),
            "time_range_date_to": args.get("time-range-date-to"),
            "filters": get_v1_filters(args),
            "limit": args.get("limit", DEFAULT_LIMIT),
            "detailed": "true",
        }

        command_results = alert_search_command(client, new_args)
        if return_v1_output:
            response = command_results.raw_response

            context_path = "Redlock.Alert(val.ID === obj.ID)"
            context: dict = {context_path: []}
            for alert in response:  # type: ignore[attr-defined]
                context[context_path].append(alert_to_v1_context(alert, args))
            context["Redlock.Metadata.CountOfAlerts"] = len(response)  # type: ignore[arg-type]

            command_results = command_results.to_context()
            command_results["EntryContext"].update(context)  # type: ignore[index]

        if args.get("risk-grade"):
            return [RISK_GRADE_NOT_SUPPORTED_MSG, command_results]

        return command_results


    def alert_get_details_v1_command(client: Client, args: Dict[str, Any], return_v1_output: bool) -> Union[CommandResults, Dict]:
        """
        This command is for supporting backwards compatibility, to make transition to V2 easier for users with custom playbooks.
        """
        new_args = {"alert_id": args.get("alert-id")}

        command_results = alert_get_details_command(client, new_args)
        if return_v1_output:
            response = command_results.raw_response
            command_results = command_results.to_context()
            command_results["EntryContext"].update(  # type: ignore[index]
                {"Redlock.Alert(val.ID === obj.ID)": alert_to_v1_context(response, args)}
            )
        return command_results


    def alert_dismiss_v1_command(
        client: Client, args: Dict[str, Any], return_v1_output: bool
    ) -> Union[CommandResults, List[Union[CommandResults, str]], Dict]:
        """
        This command is for supporting backwards compatibility, to make transition to V2 easier for users with custom playbooks.
        """
        new_args = {
            "alert_ids": args.get("alert-id"),
            "policy_ids": args.get("policy-id"),
            "dismissal_note": args.get("dismissal-note"),
            "snooze_value": args.get("snooze-value"),
            "snooze_unit": args.get("snooze-unit"),
            "time_range_unit": args.get("time-range-unit"),
            "time_range_value": args.get("time-range-value"),
            "time_range_date_from": args.get("time-range-date-from"),
            "time_range_date_to": args.get("time-range-date-to"),
            "filters": get_v1_filters(args),
        }

        command_results = alert_dismiss_command(client, new_args)
        if return_v1_output and args.get("alert-id"):
            command_results = command_results.to_context()
            command_results["EntryContext"].update({"Redlock.DismissedAlert.ID": args.get("alert-id")})  # type: ignore[index]

        if args.get("risk-grade"):
            return [RISK_GRADE_NOT_SUPPORTED_MSG, command_results]

        return command_results


    def alert_reopen_v1_command(
        client: Client, args: Dict[str, Any], return_v1_output: bool
    ) -> Union[CommandResults, List[Union[CommandResults, str]], Dict]:
        """
        This command is for supporting backwards compatibility, to make transition to V2 easier for users with custom playbooks.
        """
        new_args = {
            "alert_ids": args.get("alert-id"),
            "policy_ids": args.get("policy-id"),
            "time_range_unit": args.get("time-range-unit"),
            "time_range_value": args.get("time-range-value"),
            "time_range_date_from": args.get("time-range-date-from"),
            "time_range_date_to": args.get("time-range-date-to"),
            "filters": get_v1_filters(args),
        }

        command_results = alert_reopen_command(client, new_args)
        if return_v1_output and args.get("alert-id"):
            command_results = command_results.to_context()
            command_results["EntryContext"].update({"Redlock.ReopenedAlert.ID": args.get("alert-id")})  # type: ignore[index]

        if args.get("risk-grade"):
            return [RISK_GRADE_NOT_SUPPORTED_MSG, command_results]

        return command_results


    def remediation_command_list_v1_command(
        client: Client, args: Dict[str, Any], return_v1_output: bool
    ) -> Union[CommandResults, Dict]:
        """
        This command is for supporting backwards compatibility, to make transition to V2 easier for users with custom playbooks.
        """
        new_args = {"alert_ids": args.get("alert-id"), "all_results": "true"}

        command_results = remediation_command_list_command(client, new_args)
        if return_v1_output:
            response = command_results.raw_response

            context = []
            for alert in response:  # type: ignore[attr-defined]
                details = {
                    "ID": alert.get("alertId"),
                    "Remediation": {"CLI": alert.get("CLIScript"), "Description": alert.get("description")},
                }
                context.append(details)

            command_results = command_results.to_context()
            command_results["EntryContext"].update({"Redlock.Alert(val.ID == obj.ID)": context})  # type: ignore[index]
        return command_results


    def rql_config_search_v1_command(client: Client, args: Dict[str, Any], return_v1_output: bool) -> Union[CommandResults, Dict]:
        """
        This command is for supporting backwards compatibility, to make transition to V2 easier for users with custom playbooks.
        """
        query = f'{args.get("rql")} limit search records to {args.get("limit", "1")}'
        new_args = {
            "query": query,
            "limit": args.get("limit", "1"),
        }

        command_results = config_search_command(client, new_args)
        if return_v1_output:
            rql_data = {"Query": query, "Response": format_v1_response(command_results.raw_response)}

            command_results = command_results.to_context()
            command_results["EntryContext"].update({"Redlock.RQL(val.Query === obj.Query)": rql_data})  # type: ignore[index]
        return command_results


    def config_search_v1_command(client: Client, args: Dict[str, Any], return_v1_output: bool) -> Union[CommandResults, Dict]:
        """
        This command is for supporting backwards compatibility, to make transition to V2 easier for users with custom playbooks.
        """
        new_args = {
            "query": args.get("query"),
            "limit": args.get("limit", "100"),
            "time_range_unit": args.get("time-range-unit"),
            "time_range_value": args.get("time-range-value"),
            "time_range_date_from": args.get("time-range-date-from"),
            "time_range_date_to": args.get("time-range-date-to"),
        }

        command_results = config_search_command(client, new_args)
        if return_v1_output:
            response = command_results.raw_response
            command_results = command_results.to_context()
            command_results["EntryContext"].update({"Redlock.Asset(val.id == obj.id)": response})  # type: ignore[index]
        return command_results


    def event_search_v1_command(client: Client, args: Dict[str, Any], return_v1_output: bool) -> Union[CommandResults, Dict]:
        """
        This command is for supporting backwards compatibility, to make transition to V2 easier for users with custom playbooks.
        """
        new_args = {
            "query": args.get("query"),
            "limit": args.get("limit", "100"),
            "time_range_unit": args.get("time-range-unit"),
            "time_range_value": args.get("time-range-value"),
            "time_range_date_from": args.get("time-range-date-from"),
            "time_range_date_to": args.get("time-range-date-to"),
        }

        command_results = event_search_command(client, new_args)
        if return_v1_output:
            response = command_results.raw_response
            command_results = command_results.to_context()
            command_results["EntryContext"].update({"Redlock.Event(val.id == obj.id)": response})  # type: ignore[index]
        return command_results


    def network_search_v1_command(client: Client, args: Dict[str, Any], return_v1_output: bool) -> Union[CommandResults, Dict]:
        """
        This command is for supporting backwards compatibility, to make transition to V2 easier for users with custom playbooks.
        """
        new_args = {
            "query": args.get("query"),
            "cloud_type": args.get("cloud-type"),
            "time_range_unit": args.get("time-range-unit"),
            "time_range_value": args.get("time-range-value"),
            "time_range_date_from": args.get("time-range-date-from"),
            "time_range_date_to": args.get("time-range-date-to"),
        }

        command_results = network_search_command(client, new_args)
        if return_v1_output:
            response = command_results.raw_response
            command_results = command_results.to_context()
            command_results["EntryContext"].update(  # type: ignore[index]
                {
                    "Redlock.Network.Node(val.id == obj.id)": response.get("nodes", []),  # type: ignore[attr-defined]
                    "Redlock.Network.Connection(val.id == obj.from)": response.get("connections", []),  # type: ignore[attr-defined]
                }
            )
        return command_results


    def alert_filter_list_v1_command(client: Client, args: Dict[str, Any], return_v1_output: bool) -> Union[CommandResults, Dict]:
        """
        This command is for supporting backwards compatibility, to make transition to V2 easier for users with custom playbooks.
        """
        return alert_filter_list_command(client)


    """ COMMAND FUNCTIONS """


    def alert_filter_list_command(client: Client) -> CommandResults:
        response = client.alert_filter_list_request()
        readable_response = [
            {"filterName": filter_name, "options": filter_values.get("options"), "staticFilter": filter_values.get("staticFilter")}
            for filter_name, filter_values in response.items()
        ]

        command_results = CommandResults(
            outputs_prefix="PrismaCloud.AlertFilters",
            outputs_key_field="filterName",
            readable_output=tableToMarkdown("Filter Options:", readable_response, removeNull=True, headerTransform=pascalToSpace),
            outputs=readable_response,
            raw_response=readable_response,
        )
        return command_results


    def alert_search_command(client: Client, args: Dict[str, Any]) -> CommandResults:
        filters = argToList(args.get("filters"))
        detailed = args.get("detailed", "true")
        limit = arg_to_number(args.get("limit", DEFAULT_LIMIT))
        next_token = args.get("next_token")
        time_filter = handle_time_filter(
            base_case=ALERT_SEARCH_BASE_TIME_FILTER,
            unit_value=args.get("time_range_unit"),
            amount_value=arg_to_number(args.get("time_range_value")),
            time_from=args.get("time_range_date_from"),
            time_to=args.get("time_range_date_to"),
        )
        sort_by = [f'{sort_field}:{args.get("sort_direction")}'] if (sort_field := args.get("sort_field")) else None

        response = client.alert_search_request(time_filter, filters, limit, detailed, next_token, sort_by)
        response_items = response.get("items", [])
        next_page_token = response.get("nextPageToken")
        for response_item in response_items:
            change_timestamp_to_datestring_in_dict(response_item)

        readable_responses = deepcopy(response_items)
        nested_headers = {
            "id": "Alert ID",
            "policy.policyId": "Policy ID",
            "policy.policyType": "Policy Type",
            "policy.systemDefault": "Is Policy System Default",
            "policy.remediable": "Is Policy Remediable",
            "policy.name": "Policy Name",
            "policy.deleted": "Is Policy Deleted",
            "policy.recommendation": "Policy Recommendation",
            "policy.description": "Policy Description",
            "policy.severity": "Policy Severity",
            "policy.remediation.description": "Policy Remediation Description",
            "policy.remediation.cliScriptTemplate": "Policy Remediation CLI Script",
            "resource.resourceType": "Resource Type",
            "resource.name": "Resource Name",
            "resource.account": "Resource Account",
            "resource.cloudType": "Resource Cloud Type",
            "resource.rrn": "Resource RRN",
        }
        for readable_response in readable_responses:
            extract_nested_values(readable_response, nested_headers)

        headers = ["Alert ID", "reason", "status", "alertTime", "firstSeen", "lastSeen", "lastUpdated"] + list(
            nested_headers.values()
        )[1:]
        output = {
            "PrismaCloud.AlertPageToken(val.nextPageToken)": {"nextPageToken": next_page_token},  # values are overridden
            "PrismaCloud.Alert(val.id && val.id == obj.id)": response_items,  # values are appended to list based on id
        }
        command_results = CommandResults(
            readable_output=f'Showing {len(readable_responses)} of {response.get("totalRows", 0)} results:\n'
            + tableToMarkdown("Alerts Details:", readable_responses, headers=headers, removeNull=True, headerTransform=pascalToSpace)
            + f"### Next Page Token:\n{next_page_token}",
            outputs=output,
            raw_response=response_items,
        )
        return command_results


    def alert_get_details_command(client: Client, args: Dict[str, Any]) -> CommandResults:
        alert_id = args.get("alert_id")
        detailed = args.get("detailed", "true")

        response = client.alert_get_details_request(str(alert_id), detailed)
        change_timestamp_to_datestring_in_dict(response)

        readable_response = deepcopy(response)
        nested_headers = {
            "id": "Alert ID",
            "policy.policyId": "Policy ID",
            "policy.policyType": "Policy Type",
            "policy.systemDefault": "Is Policy System Default",
            "policy.remediable": "Is Policy Remediable",
            "policy.name": "Policy Name",
            "policy.recommendation": "Policy Recommendation",
            "policy.description": "Policy Description",
            "policy.severity": "Policy Severity",
            "policy.remediation.description": "Policy Remediation Description",
            "policy.remediation.cliScriptTemplate": "Policy Remediation CLI Script",
            "policy.labels": "Policy Labels",
            "resource.resourceType": "Resource Type",
            "resource.account": "Resource Account",
            "resource.cloudType": "Resource Cloud Type",
            "resource.rrn": "Resource RRN",
            "resource.id": "Resource ID",
            "resource.accountId": "Resource Account ID",
            "resource.regionId": "Resource Region ID",
            "resource.resourceApiName": "Resource Api Name",
            "resource.url": "Resource Url",
        }
        headers = ["Alert ID", "reason", "status", "alertTime", "firstSeen", "lastSeen", "lastUpdated", "eventOccurred"] + list(
            nested_headers.values()
        )[1:]
        extract_nested_values(readable_response, nested_headers)

        command_results = CommandResults(
            outputs_prefix="PrismaCloud.Alert",
            outputs_key_field="id",
            readable_output=tableToMarkdown(
                f"Alert {alert_id} Details:",
                readable_response,
                headers=headers,
                removeNull=True,
                url_keys=["Resource Url"],
                headerTransform=pascalToSpace,
            ),
            outputs=response,
            raw_response=response,
        )
        return command_results


    def alert_dismiss_command(client: Client, args: Dict[str, Any]) -> CommandResults:
        alert_ids = argToList(args.get("alert_ids"))
        policy_ids = argToList(args.get("policy_ids"))
        if not alert_ids and not policy_ids:
            raise DemistoException('You must provide either "alert_ids" or "policy_ids" for dismissing alerts.')

        dismissal_note = args.get("dismissal_note")
        filters = argToList(args.get("filters"))

        snooze_value = arg_to_number(args.get("snooze_value"))
        snooze_unit = args.get("snooze_unit")
        dismissal_time_filter = (
            handle_time_filter(unit_value=snooze_unit, amount_value=snooze_value) if snooze_value and snooze_unit else None
        )
        time_filter = handle_time_filter(
            base_case=dismissal_time_filter or TIME_FILTER_BASE_CASE,
            unit_value=args.get("time_range_unit"),
            amount_value=arg_to_number(args.get("time_range_value")),
            time_from=args.get("time_range_date_from"),
            time_to=args.get("time_range_date_to"),
        )

        client.alert_dismiss_request(str(dismissal_note), time_filter, alert_ids, policy_ids, dismissal_time_filter, filters)

        command_results = CommandResults(
            readable_output=(
                f"### Alerts snoozed successfully.\nSnooze note: {dismissal_note}."
                if dismissal_time_filter
                else f"### Alerts dismissed successfully.\nDismissal note: {dismissal_note}."
            )
        )
        return command_results


    def alert_reopen_command(client: Client, args: Dict[str, Any]) -> CommandResults:
        alert_ids = argToList(args.get("alert_ids"))
        policy_ids = argToList(args.get("policy_ids"))
        if not alert_ids and not policy_ids:
            raise DemistoException('You must provide either "alert_ids" or "policy_ids" for re-opening alerts.')

        filters = argToList(args.get("filters"))
        time_filter = handle_time_filter(
            base_case=TIME_FILTER_BASE_CASE,
            unit_value=args.get("time_range_unit"),
            amount_value=arg_to_number(args.get("time_range_value")),
            time_from=args.get("time_range_date_from"),
            time_to=args.get("time_range_date_to"),
        )

        client.alert_reopen_request(time_filter, alert_ids, policy_ids, filters)

        command_results = CommandResults(readable_output="### Alerts re-opened successfully.")
        return command_results


    def remediation_command_list_command(client: Client, args: Dict[str, Any]) -> CommandResults:
        alert_ids = argToList(args.get("alert_ids"))
        policy_id = args.get("policy_id")
        if not alert_ids and not policy_id:
            raise DemistoException('You must provide either "alert_ids" or "policy_id".')
        limit = arg_to_number(args.get("limit", DEFAULT_LIMIT))
        all_results = argToBoolean(args.get("all_results", "false"))

        time_filter = handle_time_filter(
            base_case=TIME_FILTER_BASE_CASE,
            unit_value=args.get("time_range_unit"),
            amount_value=arg_to_number(args.get("time_range_value")),
            time_from=args.get("time_range_date_from"),
            time_to=args.get("time_range_date_to"),
        )

        try:
            response = client.remediation_command_list_request(time_filter, alert_ids, policy_id)
            description = response.get("cliDescription")
            script_impact = response.get("scriptImpact")
            readable_response = [
                {"description": description, "scriptImpact": script_impact, "alertId": alert_id, "CLIScript": cli_script}
                for alert_id, cli_script in response.get("alertIdVsCliScript", {}).items()
            ]
            total_response_amount = len(readable_response)
            if not all_results and limit and readable_response:
                demisto.debug(f"Returning results only up to limit={limit}, from {len(readable_response)} results returned.")
                readable_response = readable_response[:limit]

        except DemistoException as de:
            if not (hasattr(de, "res") and hasattr(de.res, "status_code")):
                raise
            if de.res.status_code == 400:
                return CommandResults(
                    readable_output="Policy type disallowed using this remediation api. Cannot remediate multiple policy alerts."
                )
            return CommandResults(
                readable_output=f'Remediation unavailable{" for the time given" if time_filter != TIME_FILTER_BASE_CASE else ""}.'
            )

        command_results = CommandResults(
            outputs_prefix="PrismaCloud.AlertRemediation",
            outputs_key_field="alertId",
            readable_output=f"Showing {len(readable_response)} of {total_response_amount} results:\n"
            + tableToMarkdown("Remediation Command List:", readable_response, removeNull=True, headerTransform=pascalToSpace),
            outputs=readable_response,
            raw_response=readable_response,
        )
        return command_results


    def alert_remediate_command(client: Client, args: Dict[str, Any]) -> CommandResults:
        alert_id = args.get("alert_id")

        try:
            client.alert_remediate_request(str(alert_id))
            readable_response = {"alertId": alert_id, "successful": True}  # context output that states the remediation was successful
            readable_output = f"Alert {alert_id} remediated successfully."

        except DemistoException as de:
            if not (hasattr(de, "res") and hasattr(de.res, "status_code")):
                raise

            status = json.loads(get_response_status_header(de.res))
            status = status if isinstance(status, dict) else status[0]

            error_value = status.get("subject", "")
            failure_reason = str(status.get("i18nKey", "")).replace("_", " ")

            readable_response = {"alertId": alert_id, "successful": False, "failureReason": failure_reason, "errorValue": error_value}
            readable_output = tableToMarkdown(
                f"Remediation Failed For Alert {alert_id}:", readable_response, removeNull=True, headerTransform=pascalToSpace
            )
            if de.res.status_code == 406:
                return_error(
                    message=f"Remediation Failed For Alert {alert_id}.\n"
                    f"Failure reason: {failure_reason}.\nError value: {error_value}",
                    error=de,
                    outputs={"PrismaCloud.AlertRemediation(val.alertId === obj.alertId)": readable_response},
                )

        command_results = CommandResults(
            outputs_prefix="PrismaCloud.AlertRemediation",
            outputs_key_field="alertId",
            readable_output=readable_output,
            outputs=readable_response,
            raw_response=readable_response,
        )
        return command_results


    def config_search_command(client: Client, args: Dict[str, Any]) -> CommandResults:
        query = args.get("query")
        heuristic_search = args.get("heuristic_search", "true")
        limit = arg_to_number(args.get("limit", DEFAULT_LIMIT))
        time_filter = handle_time_filter(
            base_case=TIME_FILTER_BASE_CASE,
            unit_value=args.get("time_range_unit"),
            amount_value=arg_to_number(args.get("time_range_value")),
            time_from=args.get("time_range_date_from"),
            time_to=args.get("time_range_date_to"),
        )
        search_id = args.get("search_id")

        sort_direction = args.get("sort_direction", "desc")
        sort_field = args.get("sort_field", "insertTs")
        if any([sort_direction, sort_field]) and not all([sort_direction, sort_field]):
            raise DemistoException("Both sort direction and field must be specified if sorting.")

        include_resource_json = args.get("include_resource_json", "true")
        include_additional_resource_fields = argToBoolean(args.get("include_additional_resource_fields", "false"))

        demisto.debug(
            f"Searching for config with the following params: {query=}, {limit=}, {time_filter=}, {include_resource_json=},"
            f" {include_additional_resource_fields=}, {heuristic_search=}"
        )
        response_items = client.config_search_request(
            time_filter, str(query), limit, search_id, sort_direction, sort_field, include_resource_json, heuristic_search
        )
        if not include_additional_resource_fields:
            remove_additional_resource_fields(response_items)

        for response_item in response_items:
            change_timestamp_to_datestring_in_dict(response_item)

        headers = [
            "name",
            "id",
            "cloudType",
            "service",
            "accountName",
            "regionName",
            "deleted",
            "accountId",
            "assetId",
            "createdTs",
            "insertTs",
            "regionId",
            "resourceType",
            "rrn",
        ]
        command_results = CommandResults(
            outputs_prefix="PrismaCloud.Config",
            outputs_key_field="assetId",
            readable_output=f"Showing {len(response_items)} results:\n"
            + tableToMarkdown(
                "Configuration Details:", response_items, headers=headers, removeNull=True, headerTransform=pascalToSpace
            ),
            outputs=response_items,
            raw_response=response_items,
        )
        return command_results


    def event_search_command(client: Client, args: Dict[str, Any]) -> CommandResults:
        query = args.get("query")
        limit = arg_to_number(args.get("limit", DEFAULT_LIMIT))
        time_filter = handle_time_filter(
            base_case=TIME_FILTER_BASE_CASE,
            unit_value=args.get("time_range_unit"),
            amount_value=arg_to_number(args.get("time_range_value")),
            time_from=args.get("time_range_date_from"),
            time_to=args.get("time_range_date_to"),
        )
        sort_by = (
            [
                {
                    "field": sort_field,
                    "direction": args.get("sort_direction"),
                }
            ]
            if (sort_field := args.get("sort_field"))
            else None
        )

        response = client.event_search_request(time_filter, str(query), limit, sort_by)
        response_items = response.get("data", {}).get("items", [])
        for response_item in response_items:
            change_timestamp_to_datestring_in_dict(response_item)

        headers = [
            "subject",
            "accountName",
            "name",
            "source",
            "ip",
            "eventTs",
            "countryName",
            "stateName",
            "cityName",
            "location",
            "account",
            "regionId",
            "type",
            "id",
            "role",
            "accessKeyUsed",
            "success",
            "internal",
        ]
        command_results = CommandResults(
            outputs_prefix="PrismaCloud.Event",
            outputs_key_field="id",
            readable_output=f'Showing {len(response_items)} of {response.get("data", {}).get("totalRows", 0)} results:\n'
            + tableToMarkdown("Event Details:", response_items, headers=headers, removeNull=True, headerTransform=pascalToSpace),
            outputs=response_items,
            raw_response=response_items,
        )
        return command_results


    def network_search_command(client: Client, args: Dict[str, Any]) -> CommandResults:
        query = args.get("query")
        search_id = args.get("search_id")
        cloud_type = args.get("cloud_type")
        time_filter = handle_time_filter(
            base_case=TIME_FILTER_BASE_CASE,
            unit_value=args.get("time_range_unit"),
            amount_value=arg_to_number(args.get("time_range_value")),
            time_from=args.get("time_range_date_from"),
            time_to=args.get("time_range_date_to"),
        )

        response = client.network_search_request(str(query), time_filter, search_id, cloud_type)
        response_items = response.get("data", {})
        nodes = response_items.get("nodes", [])
        connections = response_items.get("connections", [])

        output = {
            "PrismaCloud.Network.Node(val.id && val.id == obj.id)": nodes,  # values are appended to list based on id
            "PrismaCloud.Network.Connection(val.from && val.from == obj.from && val.to && val.to == obj.to)": connections,
            # values are appended to list based on 'from' and 'to' keys
        }
        command_results = CommandResults(
            readable_output="## Network Details\n"
            + tableToMarkdown(
                "Nodes:",
                nodes,
                headers=["id", "name", "ipAddr", "grouped", "suspicious", "vulnerable"],
                removeNull=True,
                headerTransform=pascalToSpace,
            )
            + tableToMarkdown(
                "Connections:",
                connections,
                headers=["from", "to", "label", "suspicious"],
                removeNull=True,
                headerTransform=pascalToSpace,
            ),
            outputs=output,
            raw_response=response_items,
        )
        return command_results


    def trigger_scan_command(client: Client) -> CommandResults:
        response = client.trigger_scan_request()

        command_results = CommandResults(
            readable_output=tableToMarkdown("Trigger Scan Results:", response, removeNull=False, headerTransform=pascalToSpace),
        )
        return command_results


    def error_file_list_command(client: Client, args: Dict[str, Any]) -> CommandResults:
        cicd_run_id = args.get("cicd_run_id")
        if cicd_run_id:
            arg_to_number(cicd_run_id)  # for input validation or readable errors
            cicd_run_id = float(cicd_run_id)
        authors = argToList(args.get("authors"))
        branch = args.get("branch")
        categories = argToList(args.get("categories"))
        code_status = args.get("code_status")
        file_types = argToList(args.get("file_types"))
        repository = args.get("repository")
        repository_id = args.get("repository_id")
        search_options = argToList(args.get("search_options"))
        search_text = args.get("search_text", "")
        search_title = args.get("search_title")
        severities = argToList(args.get("severities"))
        source_types = argToList(args.get("source_types"))
        tags = argToList(args.get("tags"))
        statuses = argToList(args.get("statuses"))
        limit = arg_to_number(args.get("limit", DEFAULT_LIMIT))
        all_results = argToBoolean(args.get("all_results", "false"))

        error_file_list_command_args_validation(source_types, categories, statuses, file_types, search_options, severities)

        response = client.error_file_list_request(
            repository=str(repository),
            source_types=source_types,
            cicd_run_id=cicd_run_id,
            authors=authors,
            branch=branch,
            categories=categories,
            code_status=code_status,
            file_types=file_types,
            repository_id=repository_id,
            search_options=search_options,
            search_text=search_text,
            search_title=search_title,
            severities=severities,
            tags=tags,
            statuses=statuses,
        )
        response_items = response.get("data", [])
        total_response_amount = len(response_items)
        if not all_results and limit and response_items:
            demisto.debug(f"Returning results only up to {limit=}, from {len(response_items)} results returned.")
            response_items = response_items[:limit]

        headers = ["filePath", "suppressedErrorsCount", "passedCount", "openErrorsCount", "errorsCount", "fixedCount", "type"]
        command_results = CommandResults(
            outputs_prefix="PrismaCloud.ErrorFile",
            outputs_key_field="filePath",
            readable_output=f"Showing {len(response_items)} of {total_response_amount} results:\n"
            + tableToMarkdown(
                "Files Error Details:",
                response_items,
                headers=headers,
                removeNull=True,
                url_keys=["url"],
                headerTransform=pascalToSpace,
            ),
            outputs=response_items,
            raw_response=response_items,
        )
        return command_results


    def resource_get_command(client: Client, args: Dict[str, Any]) -> CommandResults:
        rrn = args.get("rrn")

        response = client.resource_get_request(str(rrn))
        change_timestamp_to_datestring_in_dict(response)

        headers = [
            "rrn",
            "id",
            "name",
            "url",
            "accountId",
            "accountName",
            "cloudType",
            "regionId",
            "regionName",
            "service",
            "resourceType",
            "insertTs",
            "deleted",
            "vpcId",
            "vpcName",
            "tags",
            "riskGrade",
            "hasNetwork",
            "hasExternalFinding",
            "hasExternalIntegration",
            "allowDrillDown",
            "hasExtFindingRiskFactors",
        ]
        command_results = CommandResults(
            outputs_prefix="PrismaCloud.Resource",
            outputs_key_field="rrn",
            readable_output=tableToMarkdown(
                "Resource Details:", response, headers=headers, removeNull=True, url_keys=["url"], headerTransform=pascalToSpace
            ),
            outputs=response,
            raw_response=response,
        )
        return command_results


    def resource_list_command(client: Client, args: Dict[str, Any]) -> CommandResults:
        list_type = args.get("list_type")
        namespace = args.get("namespace")
        limit = arg_to_number(args.get("limit", DEFAULT_LIMIT))
        all_results = argToBoolean(args.get("all_results", "false"))

        response_items = client.resource_list_request(list_type)
        extract_namespace(response_items)
        if namespace and response_items:
            response_items = [item for item in response_items if namespace in item.get("namespaces", [])]

        total_response_amount = len(response_items)
        if not all_results and limit and response_items:
            demisto.debug(f"Returning results only up to {limit=}, from {len(response_items)} results returned.")
            response_items = response_items[:limit]

        for response_item in response_items:
            change_timestamp_to_datestring_in_dict(response_item)

        headers = ["name", "id", "resourceListType", "description", "lastModifiedBy"]
        command_results = CommandResults(
            outputs_prefix="PrismaCloud.ResourceList",
            outputs_key_field="id",
            readable_output=f"Showing {len(response_items)} of {total_response_amount} results:\n"
            + tableToMarkdown("Resources Details:", response_items, headers=headers, removeNull=True, headerTransform=pascalToSpace),
            outputs=response_items,
            raw_response=response_items,
        )
        return command_results


    def user_roles_list_command(client: Client, args: Dict[str, Any]) -> CommandResults:
        role_id = args.get("role_id")
        resource_list_name = args.get("resource_list_name")
        limit = arg_to_number(args.get("limit", DEFAULT_LIMIT))
        all_results = argToBoolean(args.get("all_results", "false"))

        if role_id:
            response_items = client.user_roles_list_request(str(role_id))
            response_items = [response_items] if response_items else []
        else:
            response_items = client.all_user_roles_list_request()

        # reduce response to hold only items that are associated with resource_list_name
        if resource_list_name and response_items:
            response_items = [
                item
                for item in response_items
                if resource_list_name in [resource.get("name") for resource in item.get("resourceLists", [])]
            ]

        total_response_amount = len(response_items)
        if not all_results and limit and response_items:
            demisto.debug(f"Returning results only up to {limit=}, from {len(response_items)} results returned.")
            response_items = response_items[:limit]

        for response_item in response_items:
            change_timestamp_to_datestring_in_dict(response_item)

        headers = ["name", "id", "roleType", "description"]
        command_results = CommandResults(
            outputs_prefix="PrismaCloud.UserRoles",
            outputs_key_field="id",
            readable_output=f"Showing {len(response_items)} of {total_response_amount} results:\n"
            + tableToMarkdown("User Roles Details:", response_items, headers=headers, removeNull=True, headerTransform=pascalToSpace),
            outputs=response_items,
            raw_response=response_items,
        )
        return command_results


    def users_list_command(client: Client, args: Dict[str, Any]) -> CommandResults:
        usernames = argToList(args.get("usernames"))
        limit = arg_to_number(args.get("limit", DEFAULT_LIMIT))
        all_results = argToBoolean(args.get("all_results", "false"))

        response_items = client.users_list_request()
        # reduce response to hold only items that are associated with the provided usernames
        if usernames and response_items:
            response_items = [item for item in response_items if item.get("username") in usernames]

        total_response_amount = len(response_items)
        if not all_results and limit and response_items:
            demisto.debug(f"Returning results only up to {limit=}, from {len(response_items)} results returned.")
            response_items = response_items[:limit]

        for response_item in response_items:
            change_timestamp_to_datestring_in_dict(response_item)
            roles = []
            for role in response_item.get("roles", []):
                roles.append(role.get("name"))
            response_item["roles names"] = roles

        headers = ["displayName", "email", "enabled", "username", "type", "roles names"]
        command_results = CommandResults(
            outputs_prefix="PrismaCloud.Users",
            outputs_key_field="username",
            readable_output=f"Showing {len(response_items)} of {total_response_amount} results:\n"
            + tableToMarkdown("Users Details:", response_items, headers=headers, removeNull=True, headerTransform=pascalToSpace),
            outputs=response_items,
            raw_response=response_items,
        )
        return command_results


    def account_list_command(client: Client, args: Dict[str, Any]) -> CommandResults:
        exclude_account_group_details = args.get("exclude_account_group_details", "false")
        limit = arg_to_number(args.get("limit", DEFAULT_LIMIT))
        all_results = argToBoolean(args.get("all_results", "false"))

        response = client.account_list_request(exclude_account_group_details)
        total_response_amount = len(response)
        if not all_results and limit and response:
            demisto.debug(f"Returning results only up to {limit=}, from {len(response)} results returned.")
            response = response[:limit]
        for response_item in response:
            change_timestamp_to_datestring_in_dict(response_item)

        headers = [
            "accountId",
            "name",
            "cloudType",
            "accountType",
            "enabled",
            "addedOn",
            "lastModifiedTs",
            "lastModifiedBy",
            "storageScanEnabled",
            "protectionMode",
            "ingestionMode",
            "deploymentType",
            "status",
        ]
        command_results = CommandResults(
            outputs_prefix="PrismaCloud.Account",
            outputs_key_field="accountId",
            readable_output=f"Showing {len(response)} of {total_response_amount} results:\n"
            + tableToMarkdown("Accounts Details:", response, headers=headers, removeNull=True, headerTransform=pascalToSpace),
            outputs=response,
            raw_response=response,
        )
        return command_results


    def account_status_get_command(client: Client, args: Dict[str, Any]) -> CommandResults:
        account_ids = argToList(args.get("account_ids"))

        responses = []
        for account_id in account_ids:
            if response := client.account_status_get_request(account_id):
                response[0]["accountId"] = account_id
                responses.append(response[0])

        headers = ["accountId", "name", "status", "message", "remediation"]
        command_results = CommandResults(
            outputs_prefix="PrismaCloud.Account",
            outputs_key_field="accountId",
            readable_output=tableToMarkdown(
                "Accounts Status Details:", responses, headers=headers, removeNull=True, headerTransform=pascalToSpace
            ),
            outputs=responses,
            raw_response=responses,
        )
        return command_results


    def account_owner_list_command(client: Client, args: Dict[str, Any]) -> CommandResults:
        account_ids = argToList(args.get("account_ids"))

        responses = []
        for account_id in account_ids:
            response = client.account_owner_list_request(account_id)
            responses.append({"accountId": account_id, "emails": response})

        command_results = CommandResults(
            outputs_prefix="PrismaCloud.Account",
            outputs_key_field="accountId",
            readable_output=tableToMarkdown("Accounts Owner Details:", responses, removeNull=True, headerTransform=pascalToSpace),
            outputs=responses,
            raw_response=responses,
        )
        return command_results


    def host_finding_list_command(client: Client, args: Dict[str, Any]) -> CommandResults:
        rrn = args.get("rrn")
        limit = arg_to_number(args.get("limit", DEFAULT_LIMIT))
        all_results = argToBoolean(args.get("all_results", "false"))

        finding_types = argToList(args.get("finding_types"))
        finding_types = [finding_type.lower() for finding_type in finding_types]
        validate_array_arg(finding_types, "Finding types", FINDING_TYPES_OPTIONS)

        risk_factors = argToList(args.get("risk_factors"))
        risk_factors = [risk_factor.upper() for risk_factor in risk_factors]
        validate_array_arg(risk_factors, "Risk factors", RISK_FACTORS_OPTIONS)

        response = client.host_finding_list_request(str(rrn), finding_types, risk_factors)
        total_response_amount = len(response)
        if not all_results and limit and response:
            demisto.debug(f"Returning results only up to {limit=}, from {len(response)} results returned.")
            response = response[:limit]
        for response_item in response:
            change_timestamp_to_datestring_in_dict(response_item)
        readable_responses = deepcopy(response)

        nested_headers = {
            "sourceData.accountId": "Source Data Account ID",
            "sourceData.arn": "ARN",
        }
        headers = [
            "accountId",
            "regionId",
            "findingId",
            "type",
            "source",
            "severity",
            "status",
            "createdOn",
            "updatedOn",
            "normalizedNames",
            "scanId",
            "resourceCloudId",
            "Source Data Account ID",
            "ARN",
            "title",
            "description",
            "resourceUrl",
        ]
        for readable_response in readable_responses:
            extract_nested_values(readable_response, nested_headers)

        command_results = CommandResults(
            outputs_prefix="PrismaCloud.HostFinding",
            outputs_key_field="findingId",
            readable_output=f"Showing {len(readable_responses)} of {total_response_amount} results:\n"
            + tableToMarkdown(
                "Host Finding Details:",
                readable_responses,
                headers=headers,
                removeNull=True,
                url_keys=["resourceUrl"],
                headerTransform=pascalToSpace,
            ),
            outputs=response,
            raw_response=response,
        )
        return command_results


    def permission_list_command(client: Client, args: Dict[str, Any]) -> CommandResults:
        user_id = args.get("user_id")
        query = args.get("query")
        limit = arg_to_number(args.get("limit", DEFAULT_LIMIT))
        next_token = args.get("next_token")

        if not query and not next_token:
            raise DemistoException('You must provide either "query" or "next_token" for getting permission list.')
        if next_token and (user_id or query):
            raise DemistoException('You can\'t provide "next_token" with "user_id" or "query".')

        if query:
            response = client.permission_list_request(query, limit, user_id)  # type: ignore[arg-type]
            response = response.get("data", {})
        else:
            response = client.permission_list_next_page_request(str(next_token), limit)  # type: ignore[arg-type]
        response_items = response.get("items", [])
        next_page_token = response.get("nextPageToken")

        readable_responses = deepcopy(response_items)
        nested_headers = {
            "destCloudType": "destinationCloudType",
            "destCloudServiceName": "destinationCloudServiceName",
            "destResourceType": "destinationResourceType",
        }
        for readable_response in readable_responses:
            extract_nested_values(readable_response, nested_headers)

        headers = [
            "id",
            "sourceCloudType",
            "sourceCloudAccount",
            "sourceResourceId",
            "destinationCloudType",
            "destinationCloudServiceName",
            "destinationResourceType",
            "effectiveActionName",
            "grantedByCloudType",
            "grantedByCloudPolicyId",
            "grantedByCloudPolicyName",
            "grantedByCloudPolicyType",
            "grantedByCloudPolicyRrn",
            "grantedByCloudEntityId",
            "grantedByCloudEntityName",
            "grantedByCloudEntityRrn",
        ]
        output = {
            "PrismaCloud.PermissionPageToken(val.nextPageToken)": {"nextPageToken": next_page_token},  # values are overridden
            "PrismaCloud.Permission(val.id && val.id == obj.id)": response_items,  # values are appended to list based on id
        }
        command_results = CommandResults(
            readable_output=f'Showing {len(readable_responses)} of {response.get("totalRows", 0)} results:\n'
            + tableToMarkdown(
                "Permissions Details:", readable_responses, headers=headers, removeNull=True, headerTransform=pascalToSpace
            )
            + f"### Next Page Token:\n{next_page_token}",
            outputs=output,
            raw_response=response_items,
        )
        return command_results


    def fetch_incidents(
        client: Client, last_run: Dict[str, Any], params: Dict[str, Any]
    ) -> tuple[List[Dict[str, Any]], Dict[str, int], int]:
        """
        Retrieve new incidents periodically based on pre-defined instance parameters
        """
        last_run_time = last_run.get("time")
        now = convert_date_to_unix("now")
        first_fetch = params.get("first_fetch", FETCH_DEFAULT_TIME)
        look_back = arg_to_number(params.get("look_back", FETCH_LOOK_BACK_TIME)) or FETCH_LOOK_BACK_TIME
        time_range = calculate_fetch_time_range(now, first_fetch, look_back, last_run_time)

        fetched_ids = last_run.get("fetched_ids", {})
        limit = arg_to_number(params.get("max_fetch", MAX_INCIDENTS_TO_FETCH)) or MAX_INCIDENTS_TO_FETCH
        filters = argToList(params.get("filters"))

        incidents, fetched_ids, updated_last_run_time = fetch_request(client, fetched_ids, filters, limit, now, time_range)
        updated_last_run_time = max(convert_date_to_unix(first_fetch), updated_last_run_time)
        demisto.debug(f"Fetched {len(incidents)} incidents, {updated_last_run_time=}")
        ids_to_insert = expire_stored_ids(fetched_ids, updated_last_run_time, look_back)

        return incidents, ids_to_insert, updated_last_run_time


    def access_key_create_command(client: Client, args: Dict[str, Any]) -> CommandResults:
        """
        Perform an API request to create an access key using the provided client.

        Args:
        - client (Client): An instance of the client used to make the API request.
        - args (Dict[str, Any]): A dictionary containing any additional arguments required for the API request.

        Returns:
        - CommandResults: An object containing the raw response and table representation of the access key.
        """
        access_key = client.access_key_creation(args)
        markdown_table = tableToMarkdown(
            "PrismaCloud Access Key Creation", access_key, headers=["id", "secretKey"], headerTransform=pascalToSpace
        )
        return CommandResults(
            outputs_prefix="PrismaCloud.AccessKeys",
            outputs_key_field="id",
            outputs=access_key,
            raw_response=access_key,
            readable_output=markdown_table,
        )


    def get_access_keys_command(client: Client, args: Dict[str, Any]) -> CommandResults:
        """
        Get information about specific access key or a list of all access keys.

        Args:
        - client (Client): An instance of the client used to make the API request.
        - args (Dict[str, Any]): A dictionary that may contain access-key ID argument for specific access-key

        Returns:
        - CommandResults: An object containing the response and table representation of the access key(s).
        """
        return get_access_key_by_id(client, args) if args.get("access-key") else get_access_keys_list(client, args)


    def get_access_key_by_id(client: Client, args: Dict[str, Any]) -> CommandResults:
        """
        Get specific information about an access key using the provided client.

        Args:
        - client (Client): An instance of the client used to make the API request.
        - args (Dict[str, Any]): A dictionary containing access-key ID arguments required for the API request.

        Returns:
        - CommandResults: An object containing the raw response and table representation of the access key information.
        """
        access_key = client.get_access_key_by_id(args)
        if access_key.get("expiresOn"):
            access_key["expiresOn"] = timestamp_to_datestring(access_key.get("expiresOn"), DATE_FORMAT)

        markdown_table = tableToMarkdown(
            "PrismaCloud Access Key", access_key, headers=["id", "name", "expiresOn"], headerTransform=pascalToSpace
        )
        return CommandResults(
            outputs_prefix="PrismaCloud.AccessKeys",
            outputs_key_field="id",
            outputs=access_key,
            raw_response=access_key,
            readable_output=markdown_table,
        )


    def get_access_keys_list(client: Client, args: Dict[str, Any]) -> CommandResults:
        """
        Get a list of access keys using the provided client.

        Args:
        - client (Client): An instance of the client used to make the API request.
        - args (Dict[str, Any]): A dictionary containing any additional arguments required for the API request, including an optional
         'limit' to restrict the number of access keys returned.

        Returns:
        - CommandResults: An object containing the raw response and table representation of the access key list.
        """
        access_keys = client.get_access_keys_list()
        limit = int(args.get("limit", DEFAULT_LIMIT))
        limited_access_keys_list = access_keys[:limit]
        for access_key in limited_access_keys_list:
            access_key["createdTs"] = timestamp_to_datestring(access_key.get("createdTs"), DATE_FORMAT)
            access_key["lastUsedTime"] = timestamp_to_datestring(access_key.get("lastUsedTime"), DATE_FORMAT)
            access_key["expiresOn"] = (
                timestamp_to_datestring(access_key.get("expiresOn"), DATE_FORMAT) if access_key.get("expiresOn") != 0 else ""
            )
            access_key["roleId"] = access_key.get("role", {}).get("id", "")
            access_key["roleName"] = access_key.get("role", {}).get("name", "")

        markdown_table = tableToMarkdown(
            "PrismaCloud Access Keys",
            limited_access_keys_list,
            headers=[
                "id",
                "name",
                "createdBy",
                "createdTs",
                "lastUsedTime",
                "status",
                "expiresOn",
                "roleId",
                "roleName",
                "roleType",
                "username",
            ],
            headerTransform=pascalToSpace,
        )

        return CommandResults(
            outputs_prefix="PrismaCloud.AccessKeys",
            outputs_key_field="id",
            outputs=access_keys,
            raw_response=access_keys,
            readable_output=markdown_table,
        )


    def access_key_disable_command(client: Client, args: Dict[str, Any]) -> CommandResults:
        """
        Disable an access key using the provided client.

        Args:
        - client (Client): An instance of the client used to make the API request.
        - args (Dict[str, Any]): A dictionary containing the 'access-key' ID for disabling the access key.

        Returns:
        - CommandResults: An object containing the readable output to announce that the access key has been disabled.
        """
        access_key = args.get("access-key", "")
        client.patch_access_key_disable(access_key)
        return CommandResults(
            readable_output=f"Access key {access_key} was disabled successfully",
        )


    def access_key_enable_command(client: Client, args: Dict[str, Any]) -> CommandResults:
        """
        Enable an access key using the provided client.

        Args:
        - client (Client): An instance of the client used to make the API request.
        - args (Dict[str, Any]): A dictionary containing the 'access-key' ID for enabling the access key.

        Returns:
        - CommandResults: An object containing the readable output to announce that the access key has been enabled.
        """
        access_key = args.get("access-key", "")
        client.patch_access_key_enable(access_key)
        return CommandResults(
            readable_output=f"Access key {access_key} was enabled successfully",
        )


    def access_key_delete_command(client: Client, args: Dict[str, Any]) -> CommandResults:
        """
        Delete an access key using the provided client.

        Args:
        - client (Client): An instance of the client used to make the API request.
        - args (Dict[str, Any]): A dictionary containing the 'access-key' ID for deleting the access key.

        Returns:
        - CommandResults: An object containing the readable output to announce that the access key has been deleted.
        """
        access_key = args.get("access-key", "")
        client.access_key_deletion(access_key)
        return CommandResults(
            readable_output=f"Access key {access_key} was successfully deleted successfully",
        )


    """ MIRRORING COMMANDS """


    def get_modified_remote_data_command(
        client: Client, args: Dict[str, str], params: Dict[str, Any]
    ) -> GetModifiedRemoteDataResponse:
        """
        Gets the modified remote alerts IDs.

        Args:
            client: Demisto client.
            args:
                last_update: The last time we retrieved modified incidents.
            params: Demisto params.

        Returns: GetModifiedRemoteDataResponse object, which contains a list of the retrieved alerts IDs.
        """
        remote_args = GetModifiedRemoteDataArgs(args)
        last_update = remote_args.last_update
        parsed_date = dateparser.parse(last_update, settings={"TIMEZONE": "UTC"})  # convert to utc format
        if not parsed_date:
            raise DemistoException(f"could not parse {last_update}")
        last_update_timestamp = parsed_date.strftime(DATE_FORMAT)
        demisto.debug(f"Remote arguments last_update in UTC is {last_update_timestamp}")

        detailed = "false"
        sort_by = ["alertTime:asc"]
        time_filter = handle_time_filter(time_from=last_update, time_to="now")
        filters = argToList(params.get("filters"))

        # According to the PM of prisma cloud the following filter provide us with all the alerts that their status has been changed.
        # It is not yet documented in the Prisma Cloud API reference - for more info see this issue:
        # https://jira-hq.paloaltonetworks.local/browse/CIAC-5504.
        filters.append("timeRange.type=ALERT_STATUS_UPDATED")

        # Removes any status-related filter to retrieve all the relevant statuses (open, resolved, dismissed and snoozed)
        for status_filter in ["alert.status=open", "alert.status=dismissed", "alert.status=snoozed", "alert.status=resolved"]:
            if status_filter in set(filters):
                filters.remove(status_filter)

        response = client.alert_search_request(time_range=time_filter, filters=filters, detailed=detailed, sort_by=sort_by)
        response_items = response.get("items", [])
        modified_records_ids = [str(item.get("id")) for item in response_items]
        demisto.debug(f"Detected {len(modified_records_ids)} modified alerts in Prisma Cloud")

        return GetModifiedRemoteDataResponse(modified_records_ids)


    def get_remote_data_command(client: Client, args: Dict[str, Any]) -> GetRemoteDataResponse:
        """
        Returns an updated remote alert.

        Args:
            client: Demisto client.
            args:
                id: alert id to retrieve.
                lastUpdate: when was the last time we retrieved data.

        Returns: GetRemoteDataResponse object, which contains the alert data to update.
        """
        remote_args = GetRemoteDataArgs(args)
        remote_alert_id = remote_args.remote_incident_id
        entries = []

        try:
            demisto.debug(
                f"Performing get-remote-data command with incident id: {remote_alert_id} "
                f"and last_update: {remote_args.last_update}"
            )
            mirrored_data, updated_object = get_remote_alert_data(client, remote_alert_id)
            if updated_object and client.close_incident:
                demisto.debug(f"Update incident {remote_alert_id} with fields: {updated_object}")
                entry = set_xsoar_incident_entries(updated_object, remote_alert_id)
                if entry:
                    entries.append(entry)

            if not updated_object:
                demisto.debug(f"No delta was found for incident id: {remote_alert_id}.")

            return GetRemoteDataResponse(mirrored_object=updated_object, entries=entries)

        except Exception as e:
            demisto.debug(f"Error in Prisma Cloud v2 incoming mirror for incident: {remote_alert_id}\nError message: {e!s}")

            if not mirrored_data:
                mirrored_data = {"id": remote_alert_id}
            mirrored_data["in_mirror_error"] = str(e)

            return GetRemoteDataResponse(mirrored_object=mirrored_data, entries=[])


    def update_remote_system_command(client: Client, args: Dict[str, Any]) -> str:
        """
        Mirrors out local changes (closing or reopening of an xsoar incident) to the remote system (Prisma Cloud).

        To determine a closing of an XSOAR incident that should be mirrored to Prisma we check that:
            1. parsed_args.incident_changed = True (meaning the incident has changed).
            2. Incident status is Done (=2).
            3. The delta contains at least one of the following fields: 'closeReason', 'closingUserId', 'closeNotes'.

        To determine a re-opening of an XSOAR incident that should be mirrored to Prisma we check that:
            1. parsed_args.incident_changed = True (meaning the incident has changed).
            2. Incident status is Active (=1).
            3. The delta contains the 'closingUserId'.

        These conditions should be sufficient.

        In any other case, the change in XSOAR won't be mirrored to the remote system.

        Args:
            client: Demisto client.
            args: A dictionary containing the data regarding a modified incident, including: data, entries,
                  incident_changed, remote_incident_id, inc_status and delta.

        Returns: The remote incident id that was modified.
        """
        parsed_args = UpdateRemoteSystemArgs(args)
        delta = parsed_args.delta
        remote_incident_id = parsed_args.remote_incident_id

        try:
            if parsed_args.incident_changed:
                incident_status = parsed_args.inc_status
                demisto.debug(
                    f"Performing update_remote_system command with incident id {remote_incident_id}, incident status"
                    f" {incident_status} and delta {delta}."
                )
                update_remote_alert(client, delta, incident_status, remote_incident_id)

            else:
                demisto.debug(f"Skipping the update of remote incident {remote_incident_id} as it has not changed.")

        except Exception as e:
            demisto.error(f"Error in Prisma Cloud V2 outgoing mirror for incident {remote_incident_id}. Error message: {e!s}")

        return remote_incident_id


    def validate_code_issues_list_args(args):
        license_type = args.get("license_type")
        args.get("term")
        search_scopes = args.get("search_scopes")
        search_term = args.get("search_term")
        page = args.get("page")
        page_size = arg_to_number(args.get("page_size"))

        if (page and not page_size) or (page_size and not page):
            raise DemistoException("Please provide both `page` and `page_size` arguments.")

        if page_size and page_size > MAX_LIMIT_FOR_CODE_ISSUES:
            raise DemistoException("`Page_size` argument can't be more than 1000.")

        if license_type and license_type not in LICENSE_TYPES:
            raise DemistoException(
                "Invalid license type. For the list of valid license types go to- https://pan.dev/prisma-cloud/api/code/get-periodic-findings/#request"
            )

        if search_scopes and not search_term:
            raise DemistoException("The `search_term` argument is required when specifying `search_scopes`.")

        # Create a copy of the dictionary excluding `limit`, `scopes`, and `term`
        filtered_args = {
            key: value for key, value in args.items() if key not in ["limit", "search_scopes", "search_term", "page", "page_size"]
        }

        # Ensure there is at least one valid filtering argument
        if not filtered_args:
            raise DemistoException(
                "At least one filtering argument is required, excluding `search_scopes`, `search_term`, and `limit`. For example, \
        `fixable_only` or 'branch`"
            )


    def get_labels(labels) -> Optional[List]:
        """
        Converts the labels from a code issue into a list of strings,
        handling both string lists and lists of label dictionaries
        """
        if labels:
            res = []
            for label in labels:
                if isinstance(label, str):
                    res.append(label)
                else:
                    res.append(label.get("label"))
            return res
        return None


    def code_issues_list_request_body(
        fixable_only: Optional[bool] = None,
        search_scopes: Optional[List[str]] = [],
        search_term: Optional[str] = None,
        branch: Optional[str] = None,
        check_status: Optional[str] = None,
        git_users: Optional[List[str]] = [],
        iac_categories: Optional[List[str]] = [],
        iac_labels: Optional[List[str]] = [],
        file_types: Optional[List[str]] = [],
        repositories: Optional[List[str]] = [],
        secrets_risk_factors: Optional[List[str]] = [],
        severities: Optional[List[str]] = [],
        vulnerability_risk_factors: Optional[List[str]] = [],
        iac_tags: Optional[List[str]] = [],
        license_type: Optional[List[str]] = [],
        code_categories: Optional[List[str]] = [],
        limit: Optional[float] = 50,
        offset: Optional[float] = 0,
    ):
        return assign_params(
            filters=assign_params(
                branch=branch,
                checkStatus=check_status,
                codeCategories=code_categories,
                fileTypes=file_types,
                fixableOnly=fixable_only,
                gitUsers=git_users,
                iacCategories=iac_categories,
                iacLabels=iac_labels,
                iacTags=iac_tags,
                licenseType=license_type,
                repositories=repositories,
                secretsRiskFactors=secrets_risk_factors,
                severities=severities,
                vulnerabilityRiskFactors=vulnerability_risk_factors,
            ),
            search=assign_params(scopes=search_scopes, term=search_term),
            limit=limit,
            offset=offset,
        )


    def code_issues_list_command(client, args):
        validate_code_issues_list_args(args)

        fixable_only = argToBoolean(args.get("fixable_only", False))
        search_scopes = argToList(args.get("search_scopes"))
        search_term = args.get("search_term")
        branch = args.get("branch")
        check_status = args.get("check_status")
        git_users = argToList(args.get("git_users"))
        iac_categories = argToList(args.get("iac_categories"))
        iac_labels = argToList(args.get("iac_labels"))
        file_types = argToList(args.get("file_types"))
        repositories = argToList(args.get("repositories"))
        secrets_risk_factors = argToList(args.get("secrets_risk_factors"))
        severities = argToList(args.get("severities"))
        vulnerability_risk_factors = argToList(args.get("vulnerability_risk_factors"))
        iac_tags = argToList(args.get("iac_tags"))
        license_type = argToList(args.get("license_type"))
        code_categories = argToList(args.get("code_categories"))
        limit = arg_to_number(args.get("limit")) or INT_DEFAULT_LIMIT
        page = arg_to_number(args.get("page")) or DEFAULT_PAGE
        page_size = arg_to_number(args.get("page_size"))

        limit_for_request = min(limit, MAX_LIMIT_FOR_CODE_ISSUES)

        # pagination
        if page_size:
            limit_for_request = page_size
            offset = page * page_size
            limit = page_size
        else:
            offset = 0

        has_next = True

        res_issues: List[Dict] = []
        issues_for_readable_output = []
        while len(res_issues) < limit and has_next:
            body = code_issues_list_request_body(
                fixable_only=fixable_only,
                search_scopes=search_scopes,
                search_term=search_term,
                branch=branch,
                check_status=check_status,
                git_users=git_users,
                iac_categories=iac_categories,
                iac_labels=iac_labels,
                file_types=file_types,
                repositories=repositories,
                secrets_risk_factors=secrets_risk_factors,
                severities=severities,
                vulnerability_risk_factors=vulnerability_risk_factors,
                iac_tags=iac_tags,
                license_type=license_type,
                code_categories=code_categories,
                limit=limit_for_request,
                offset=offset,
            )

            response = client.code_issues_list_request(body)

            res_issues.extend(response["data"])

            for issue in response["data"]:
                issues_for_readable_output.append(
                    {
                        "Repository": issue.get("repository"),
                        "First Detected": issue["firstDetected"],
                        "Policy": issue["policy"],
                        "Severity": issue["severity"],
                        "Labels": get_labels(issue.get("labels")),
                        "Repository Source": issue.get("repositorySource"),
                    }
                )

            has_next = response["hasNext"]
            offset = len(res_issues)

        res_issues = res_issues[0:limit]
        issues_for_readable_output = issues_for_readable_output[0:limit]

        headers = ["Repository", "First Detected", "Policy", "Severity", "Labels", "Repository Source"]
        readable_output = tableToMarkdown("Issues list:", issues_for_readable_output, headers, removeNull=True)
        return CommandResults(
            outputs_prefix="PrismaCloud.CodeIssue", outputs=res_issues, readable_output=readable_output, raw_response=res_issues
        )


    def get_asset_command(client: Client, args: Dict[str, Any]) -> CommandResults:
        """
        Retrieve information about an asset using the provided client.

        Args:
        - client (Client): An instance of the client used to make the API request.
        - args (Dict[str, Any]): A dictionary containing the 'asset-id' ID for retrieving the asset information.

        Returns:
        - CommandResults: An object containing the information about the asset.
        """
        asset_id = args.get("asset_id")
        finding_type = args.get("finding_type")
        risk_factors = argToList(args.get("risk_factors"))
        timeline_item_id = args.get("timeline_item_id")
        alert_ids = argToList(args.get("alert_ids"))
        limit = arg_to_number(args.get("limit")) or INT_DEFAULT_LIMIT
        permission_type = args.get("permission_type")
        page_token = args.get("page_token")
        findings_only = argToBoolean(args.get("prisma_cloud_findings_only", False))
        vulnerability_info_type_id = args.get("vulnerability_info_type_id")
        vulnerability_info_type = args.get("vulnerability_info_type")

        data = assign_params(
            assetId=asset_id,
            type="asset",
            findingType=finding_type,
            riskFactors=risk_factors,
            timelineItemId=timeline_item_id,
            alertIds=alert_ids,
            limit=limit,
            permissionType=permission_type,
            pageToken=page_token,
            prismaCloudFindingsOnly=findings_only,
            vulnerabilityInfoTypeId=vulnerability_info_type_id,
            vulnerabilityInfoType=vulnerability_info_type,
        )
        response = client.get_asset_by_id(data)
        outputs = response.get("data", {}).get("asset", {})

        return CommandResults(
            outputs_prefix="PrismaCloud.Asset",
            outputs_key_field="id",
            outputs=outputs,
            readable_output=tableToMarkdown(
                "Asset",
                outputs,
                headerTransform=pascalToSpace,
                sort_headers=False,
            ),
            raw_response=response,
        )


    def get_asset_generic_command(client: Client, args: Dict[str, Any]) -> Dict[str, Any]:
        """
        Retrieve specific information about an asset using the provided client.

        Args:
        - client (Client): An instance of the client used to make the API request.
        - args (Dict[str, Any]): A dictionary containing the 'asset-id' ID for retrieving the asset information.

        Returns:
        - Dict[str, Any]: A dictionary containing the specific information about the asset.
        """
        asset_id = args.get("asset_id")
        asset_type = args.get("type")
        finding_type = args.get("finding_type")
        risk_factors = argToList(args.get("risk_factors"))
        timeline_item_id = args.get("timeline_item_id")
        alert_ids = argToList(args.get("alert_ids"))
        limit = arg_to_number(args.get("limit")) or INT_DEFAULT_LIMIT
        permission_type = args.get("permission_type")
        page_token = args.get("page_token")
        findings_only = argToBoolean(args.get("prisma_cloud_findings_only", False))
        vulnerability_info_type_id = args.get("vulnerability_info_type_id")
        vulnerability_info_type = args.get("vulnerability_info_type")

        data = assign_params(
            assetId=asset_id,
            type=asset_type,
            findingType=finding_type,
            riskFactors=risk_factors,
            timelineItemId=timeline_item_id,
            alertIds=alert_ids,
            limit=limit,
            permissionType=permission_type,
            pageToken=page_token,
            prismaCloudFindingsOnly=findings_only,
            vulnerabilityInfoTypeId=vulnerability_info_type_id,
            vulnerabilityInfoType=vulnerability_info_type,
        )
        response = client.get_asset_by_id(data)

        return response


    def get_asset_findings_command(client: Client, args: Dict[str, Any]) -> CommandResults:
        """
        Retrieve information about an asset findings using the provided client.

        Args:
        - client (Client): An instance of the client used to make the API request.
        - args (Dict[str, Any]): A dictionary containing the 'asset-id' ID for retrieving the asset information.

        Returns:
        - CommandResults: An object containing the information about the asset findings.
        """
        asset_id = args.get("asset_id")
        finding_type = args.get("finding_type")
        risk_factors = argToList(args.get("risk_factors"))
        timeline_item_id = args.get("timeline_item_id")
        alert_ids = argToList(args.get("alert_ids"))
        limit = arg_to_number(args.get("limit")) or INT_DEFAULT_LIMIT
        permission_type = args.get("permission_type")
        page_token = args.get("page_token")
        findings_only = argToBoolean(args.get("prisma_cloud_findings_only", False))
        vulnerability_info_type_id = args.get("vulnerability_info_type_id")
        vulnerability_info_type = args.get("vulnerability_info_type")

        data = assign_params(
            assetId=asset_id,
            type="findings",
            findingType=finding_type,
            riskFactors=risk_factors,
            timelineItemId=timeline_item_id,
            alertIds=alert_ids,
            limit=limit,
            permissionType=permission_type,
            pageToken=page_token,
            prismaCloudFindingsOnly=findings_only,
            vulnerabilityInfoTypeId=vulnerability_info_type_id,
            vulnerabilityInfoType=vulnerability_info_type,
        )
        response = client.get_asset_by_id(data)
        outputs = response.get("data", {}).get("asset", {}).get("findings")

        return CommandResults(
            outputs_prefix="PrismaCloud.AssetFindings",
            outputs_key_field="id",
            outputs=outputs,
            readable_output=tableToMarkdown(
                "Asset Findings",
                outputs,
                headerTransform=pascalToSpace,
                sort_headers=False,
            ),
            raw_response=response,
        )


    def get_asset_vulnerabilities_command(client: Client, args: Dict[str, Any]) -> CommandResults:
        """
        Retrieve information about an asset vulnerabilities using the provided client.

        Args:
        - client (Client): An instance of the client used to make the API request.
        - args (Dict[str, Any]): A dictionary containing the 'asset-id' ID for retrieving the asset information.

        Returns:
        - CommandResults: An object containing the information about the asset vulnerabilities.
        """
        asset_id = args.get("asset_id")
        finding_type = args.get("finding_type")
        risk_factors = argToList(args.get("risk_factors"))
        timeline_item_id = args.get("timeline_item_id")
        alert_ids = argToList(args.get("alert_ids"))
        limit = arg_to_number(args.get("limit")) or INT_DEFAULT_LIMIT
        permission_type = args.get("permission_type")
        page_token = args.get("page_token")
        findings_only = argToBoolean(args.get("prisma_cloud_findings_only", False))
        vulnerability_info_type_id = args.get("vulnerability_info_type_id")
        vulnerability_info_type = args.get("vulnerability_info_type")

        data = assign_params(
            assetId=asset_id,
            type="vulnerabilities",
            findingType=finding_type,
            riskFactors=risk_factors,
            timelineItemId=timeline_item_id,
            alertIds=alert_ids,
            limit=limit,
            permissionType=permission_type,
            pageToken=page_token,
            prismaCloudFindingsOnly=findings_only,
            vulnerabilityInfoTypeId=vulnerability_info_type_id,
            vulnerabilityInfoType=vulnerability_info_type,
        )
        response = client.get_asset_by_id(data)
        outputs = response.get("data", {}).get("asset", {}).get("vulnerabilities")

        return CommandResults(
            outputs_prefix="PrismaCloud.AssetVulnerabilities",
            outputs_key_field="id",
            outputs=outputs,
            readable_output=tableToMarkdown(
                "Asset Vulnerabilities",
                outputs,
                headerTransform=pascalToSpace,
                sort_headers=False,
            ),
            raw_response=response,
        )


    def get_asset_alerts_command(client: Client, args: Dict[str, Any]) -> CommandResults:
        """
        Retrieve information about an asset alerts using the provided client.

        Args:
        - client (Client): An instance of the client used to make the API request.
        - args (Dict[str, Any]): A dictionary containing the 'asset-id' ID for retrieving the asset information.

        Returns:
        - CommandResults: An object containing the information about the asset alerts.
        """
        asset_id = args.get("asset_id")
        finding_type = args.get("finding_type")
        risk_factors = argToList(args.get("risk_factors"))
        timeline_item_id = args.get("timeline_item_id")
        alert_ids = argToList(args.get("alert_ids"))
        limit = arg_to_number(args.get("limit")) or INT_DEFAULT_LIMIT
        permission_type = args.get("permission_type")
        page_token = args.get("page_token")
        findings_only = argToBoolean(args.get("prisma_cloud_findings_only", False))
        vulnerability_info_type_id = args.get("vulnerability_info_type_id")
        vulnerability_info_type = args.get("vulnerability_info_type")

        data = assign_params(
            assetId=asset_id,
            type="alerts",
            findingType=finding_type,
            riskFactors=risk_factors,
            timelineItemId=timeline_item_id,
            alertIds=alert_ids,
            limit=limit,
            permissionType=permission_type,
            pageToken=page_token,
            prismaCloudFindingsOnly=findings_only,
            vulnerabilityInfoTypeId=vulnerability_info_type_id,
            vulnerabilityInfoType=vulnerability_info_type,
        )
        response = client.get_asset_by_id(data)
        outputs = response.get("data", {}).get("asset", {}).get("alerts")

        return CommandResults(
            outputs_prefix="PrismaCloud.AssetAlerts",
            outputs_key_field="id",
            outputs=outputs,
            readable_output=tableToMarkdown(
                "Asset Alerts",
                outputs,
                headerTransform=pascalToSpace,
                sort_headers=False,
            ),
            raw_response=response,
        )


    def get_asset_relationships_command(client: Client, args: Dict[str, Any]) -> CommandResults:
        """
        Retrieve information about an asset relationships using the provided client.

        Args:
        - client (Client): An instance of the client used to make the API request.
        - args (Dict[str, Any]): A dictionary containing the 'asset-id' ID for retrieving the asset information.

        Returns:
        - CommandResults: An object containing the information about the asset relationships.
        """
        asset_id = args.get("asset_id")
        finding_type = args.get("finding_type")
        risk_factors = argToList(args.get("risk_factors"))
        timeline_item_id = args.get("timeline_item_id")
        alert_ids = argToList(args.get("alert_ids"))
        limit = arg_to_number(args.get("limit")) or INT_DEFAULT_LIMIT
        permission_type = args.get("permission_type")
        page_token = args.get("page_token")
        findings_only = argToBoolean(args.get("prisma_cloud_findings_only", False))
        vulnerability_info_type_id = args.get("vulnerability_info_type_id")
        vulnerability_info_type = args.get("vulnerability_info_type")

        data = assign_params(
            assetId=asset_id,
            type="relationships",
            findingType=finding_type,
            riskFactors=risk_factors,
            timelineItemId=timeline_item_id,
            alertIds=alert_ids,
            limit=limit,
            permissionType=permission_type,
            pageToken=page_token,
            prismaCloudFindingsOnly=findings_only,
            vulnerabilityInfoTypeId=vulnerability_info_type_id,
            vulnerabilityInfoType=vulnerability_info_type,
        )
        response = client.get_asset_by_id(data)
        outputs = response.get("data", {}).get("asset", {}).get("relationships")

        return CommandResults(
            outputs_prefix="PrismaCloud.AssetRelationships",
            outputs_key_field="id",
            outputs=outputs,
            readable_output=tableToMarkdown(
                "Asset Relationships",
                outputs,
                headerTransform=pascalToSpace,
                sort_headers=False,
            ),
            raw_response=response,
        )


    def get_asset_network_command(client: Client, args: Dict[str, Any]) -> CommandResults:
        """
        Retrieve information about an asset network using the provided client.

        Args:
        - client (Client): An instance of the client used to make the API request.
        - args (Dict[str, Any]): A dictionary containing the 'asset-id' ID for retrieving the asset information.

        Returns:
        - CommandResults: An object containing the information about the asset network.
        """
        asset_id = args.get("asset_id")
        finding_type = args.get("finding_type")
        risk_factors = argToList(args.get("risk_factors"))
        timeline_item_id = args.get("timeline_item_id")
        alert_ids = argToList(args.get("alert_ids"))
        limit = arg_to_number(args.get("limit")) or INT_DEFAULT_LIMIT
        permission_type = args.get("permission_type")
        page_token = args.get("page_token")
        findings_only = argToBoolean(args.get("prisma_cloud_findings_only", False))
        vulnerability_info_type_id = args.get("vulnerability_info_type_id")
        vulnerability_info_type = args.get("vulnerability_info_type")

        data = assign_params(
            assetId=asset_id,
            type="network",
            findingType=finding_type,
            riskFactors=risk_factors,
            timelineItemId=timeline_item_id,
            alertIds=alert_ids,
            limit=limit,
            permissionType=permission_type,
            pageToken=page_token,
            prismaCloudFindingsOnly=findings_only,
            vulnerabilityInfoTypeId=vulnerability_info_type_id,
            vulnerabilityInfoType=vulnerability_info_type,
        )
        response = client.get_asset_by_id(data)
        outputs = response.get("data", {}).get("asset", {}).get("network")

        return CommandResults(
            outputs_prefix="PrismaCloud.AssetNetwork",
            outputs_key_field="id",
            outputs=outputs,
            readable_output=tableToMarkdown(
                "Asset Network",
                outputs,
                headerTransform=pascalToSpace,
                sort_headers=False,
            ),
            raw_response=response,
        )


    """ TEST MODULE """


    def test_module(client: Client, params: Dict[str, Any]) -> str:
        if params.get("isFetch"):
            max_fetch = arg_to_number(params.get("max_fetch"), arg_name="Maximum number of incidents to fetch")
            if max_fetch and (max_fetch > MAX_INCIDENTS_TO_FETCH or max_fetch <= 0):
                return f"Maximum number of incidents to fetch must be between 1 and {MAX_INCIDENTS_TO_FETCH} ({max_fetch} provided)."

            filters = argToList(params.get("filters"))
            # check the filters format
            handle_filters(filters)  # raises if invalid, otherwise we don't use the value

            # check the filters names
            filters_available_names = client.alert_filter_list_request().keys()
            for filter_ in filters:
                filter_name = filter_.split("=")[0]
                if filter_name not in filters_available_names:
                    return (
                        f'Filter "{filter_name}" is not one of the available filters. '
                        f'The available filters names can be found by running "prisma-cloud-alert-filter-list" command.'
                    )

            look_back = arg_to_number(params.get("look_back"), arg_name="Time in minutes to look back when fetching incidents")
            if look_back and look_back < 0:
                return "Time in minutes to look back when fetching incidents must be a positive value, greater than or equal to zero."
            time_range = calculate_fetch_time_range(
                now=convert_date_to_unix("now"), first_fetch=params.get("first_fetch", FETCH_DEFAULT_TIME)
            )
            alerts = client.alert_search_request(
                time_range=time_range, filters=filters, detailed="true", limit=max_fetch, sort_by=["alertTime:asc"]
            ).get("items", [])
            if not alerts:
                return (
                    "The connection succeeded, but no alerts were found for the provided filters and first fetch time. "
                    "To pass the test, increase the first fetch time to a time when there were alerts at."
                )

        # Authorization is done in client.generate_auth_token
        return "ok"


    """ MAIN FUNCTION """


    def main() -> None:
        params: Dict[str, Any] = demisto.params()
        args: Dict[str, Any] = demisto.args()
        url = format_url(str(params.get("url")))
        verify_certificate: bool = not params.get("insecure", False)
        proxy = params.get("proxy", False)

        username = params.get("credentials", {}).get("identifier")
        password = params.get("credentials", {}).get("password")

        mirror_direction = MIRROR_DIRECTION_MAPPING.get(params.get("mirror_direction", "None"))
        close_incident = argToBoolean(params.get("close_incident", False))
        close_alert = argToBoolean(params.get("close_alert", False))

        return_v1_output = params.get("output_old_format", False)

        command = demisto.command()
        demisto.debug(f"Command being called is {command}")
        is_test_module: bool = command == "test-module"

        try:
            client: Client = Client(
                url,
                verify_certificate,
                proxy,
                headers=HEADERS,
                username=username,
                password=password,
                mirror_direction=mirror_direction,
                close_incident=close_incident,
                close_alert=close_alert,
                is_test_module=is_test_module,
            )
            commands_without_args = {
                "prisma-cloud-alert-filter-list": alert_filter_list_command,
                "prisma-cloud-trigger-scan": trigger_scan_command,
            }
            commands_with_args = {
                "prisma-cloud-alert-search": alert_search_command,
                "prisma-cloud-alert-get-details": alert_get_details_command,
                "prisma-cloud-alert-dismiss": alert_dismiss_command,
                "prisma-cloud-alert-reopen": alert_reopen_command,
                "prisma-cloud-remediation-command-list": remediation_command_list_command,
                "prisma-cloud-alert-remediate": alert_remediate_command,
                "prisma-cloud-config-search": config_search_command,
                "prisma-cloud-event-search": event_search_command,
                "prisma-cloud-network-search": network_search_command,
                "prisma-cloud-error-file-list": error_file_list_command,
                "prisma-cloud-resource-get": resource_get_command,
                "prisma-cloud-resource-list": resource_list_command,
                "prisma-cloud-user-roles-list": user_roles_list_command,
                "prisma-cloud-users-list": users_list_command,
                "prisma-cloud-account-list": account_list_command,
                "prisma-cloud-account-status-get": account_status_get_command,
                "prisma-cloud-account-owner-list": account_owner_list_command,
                "prisma-cloud-host-finding-list": host_finding_list_command,
                "prisma-cloud-permission-list": permission_list_command,
                "get-remote-data": get_remote_data_command,
                "update-remote-system": update_remote_system_command,
                "prisma-cloud-access-key-create": access_key_create_command,
                "prisma-cloud-access-keys-list": get_access_keys_command,
                "prisma-cloud-access-key-disable": access_key_disable_command,
                "prisma-cloud-access-key-enable": access_key_enable_command,
                "prisma-cloud-access-key-delete": access_key_delete_command,
                "prisma-cloud-asset-get": get_asset_command,
                "prisma-cloud-asset-generic-get": get_asset_generic_command,
                "prisma-cloud-asset-findings-get": get_asset_findings_command,
                "prisma-cloud-asset-alerts-get": get_asset_alerts_command,
                "prisma-cloud-asset-vulnerabilities-get": get_asset_vulnerabilities_command,
                "prisma-cloud-asset-relationships-get": get_asset_relationships_command,
                "prisma-cloud-asset-network-get": get_asset_network_command,
            }
            commands_v1 = {
                "redlock-search-alerts": alert_search_v1_command,
                "redlock-get-alert-details": alert_get_details_v1_command,
                "redlock-dismiss-alerts": alert_dismiss_v1_command,
                "redlock-reopen-alerts": alert_reopen_v1_command,
                "redlock-get-remediation-details": remediation_command_list_v1_command,
                "redlock-get-rql-response": rql_config_search_v1_command,
                "redlock-search-config": config_search_v1_command,
                "redlock-search-event": event_search_v1_command,
                "redlock-search-network": network_search_v1_command,
                "redlock-list-alert-filters": alert_filter_list_v1_command,
            }

            if command in commands_without_args:
                return_results(commands_without_args[command](client))
            elif command in commands_with_args:
                return_results(commands_with_args[command](client, args))
            elif command in commands_v1:
                return_results(commands_v1[command](client, args, return_v1_output))
            elif command == "test-module":
                return_results(test_module(client, params))
            elif command == "fetch-incidents":
                last_run = demisto.getLastRun()
                incidents, fetched_ids, last_run_time = fetch_incidents(client, last_run, params)
                demisto.incidents(incidents)
                demisto.setLastRun({"fetched_ids": fetched_ids, "time": last_run_time})
            elif command == "get-modified-remote-data":
                return_results(get_modified_remote_data_command(client, args, params))
            elif command == "prisma-cloud-code-issues-list":
                return_results(code_issues_list_command(client, args))

            else:
                raise NotImplementedError(f"{command} command is not implemented.")

        except Exception as e:
            error_msg = str(e)
            if hasattr(e, "res"):
                error_msg += get_response_status_header(e.res)  # type: ignore[attr-defined]
                if hasattr(e.res, "status_code") and e.res.status_code == 401:  # type: ignore[attr-defined]
                    error_msg = (
                        "Authentication failed. "
                        "Check that the Server URL parameter is correct and validate your credentials.\n" + error_msg
                    )
            return_error(error_msg, error=e)


    """ ENTRY POINT """

    if __name__ in ("__main__", "builtin", "builtins"):
        main()

    register_module_line('PrismaCloud v2', 'end', __line__())
  subtype: python3
  type: python
sectionorder:
- Connect
- Collect
signature: 
system: true
