args:
- description: The unique numeric ID of the issue (integer only, e.g. 12345). Do not
    pass placeholder values such as 'n/a', 'unknown', or any non-numeric string.
  isArray: true
  name: issue_id
  supportedModules: []
- auto: PREDEFINED
  description: The severity of the issue.
  isArray: true
  name: severity
  predefined:
  - low
  - medium
  - high
  - critical
  supportedModules: []
- auto: PREDEFINED
  description: Account type.
  isArray: true
  name: Identity_type
  predefined:
  - ANONYMOUS
  - APPLICATION
  - COMPUTE
  - FEDERATED_IDENTITY
  - SERVICE
  - SERVICE_ACCOUNT
  - TEMPORARY_CREDENTIALS
  - TOKEN
  - UNKNOWN
  - USER
  supportedModules: []
- description: The issue name.
  isArray: true
  name: issue_name
  supportedModules: []
- description: The issue source.
  isArray: true
  name: issue_source
  predefined:
  - XDR Agent
  - XDR Analytics
  - XDR Analytics BIOC
  - PAN NGFW
  - XDR BIOC
  - XDR IOC
  - Threat Intelligence
  - XDR Managed Threat Hunting
  - Correlation
  - Prisma Cloud
  - Prisma Cloud Compute
  - ASM
  - IoT Security
  - Custom Alert
  - Health
  - SaaS Attachments
  - Attack Path
  - Cloud Network Analyzer
  - IaC Scanner
  - CAS Secret Scanner
  - CI/CD Risks
  - CLI Scanner
  - CIEM Scanner
  - API Traffic Monitor
  - API Posture Scanner
  - Agentless Disk Scanner
  - Kubernetes Scanner
  - Compute Policy
  - CSPM Scanner
  - CAS CVE Scanner
  - CAS License Scanner
  - Secrets Scanner
  - SAST Scanner
  - Data Policy
  - Attack Surface Test
  - Package Operational Risk
  - Vulnerability Policy
  - AI Security Posture
  supportedModules: []
- description: 'A comma-seperated list of all relevant SHA256 hash fields for processes
    and file macros involved in the issue: Initiator SHA256 actor process image, CGO
    SHA256 hash of causality actor process image, Target process SHA256 of action
    process image, OS Parent SHA256 hash of the OS actor process image, File Macro
    SHA256 hash of the action file macro.'
  isArray: true
  name: sha256
  supportedModules: []
- deprecated: true
  description: Initiator SHA256 actor process image.
  hidden: true
  isArray: true
  name: actor_process_image_sha256
  supportedModules: []
- deprecated: true
  description: CGO SHA256 hash of causality actor process image.
  hidden: true
  isArray: true
  name: causality_actor_process_image_sha256
  supportedModules: []
- deprecated: true
  description: Target process SHA256 of action process image.
  hidden: true
  isArray: true
  name: action_process_image_sha256
  supportedModules: []
- defaultValue: source_insert_ts
  description: The field by which to sort the results.
  isArray: true
  name: sort_field
  supportedModules: []
- auto: PREDEFINED
  description: The order in which to sort the results.
  name: sort_order
  predefined:
  - DESC
  - ASC
  supportedModules: []
- defaultValue: "0"
  deprecated: true
  description: The starting offset for pagination (integer only, e.g. 0, 50, 100).
  name: offset
  supportedModules: []
- defaultValue: "50"
  deprecated: true
  description: The maximum number of issues to return (integer only, e.g. 50, 100).
  name: limit
  supportedModules: []
- defaultValue: "0"
  description: The page number for the issues to return for pagination. (integer only,
    e.g. 0, 50, 100).
  name: page
  supportedModules: []
- defaultValue: "50"
  description: The number of issues to return per page. (integer only, e.g. 50, 100).
  name: page_size
  supportedModules: []
- description: Supports epoch timestamp and simplified extended ISO format (YYYY-MM-DDThh:mm:ss).
  name: start_time
  supportedModules: []
- description: Supports epoch timestamp and simplified extended ISO format (YYYY-MM-DDThh:mm:ss).
    If start time is provided without end_time it will be from start_time until now.
  name: end_time
  supportedModules: []
- description: The category of the issue.
  isArray: true
  name: issue_category
  supportedModules: []
- description: The domain of the issue.
  isArray: true
  name: issue_domain
  predefined:
  - Health
  - Hunting
  - IT
  - Posture
  - Security
  supportedModules: []
- description: The description of the issue.
  isArray: true
  name: issue_description
  supportedModules: []
- deprecated: true
  description: OS Parent SHA256 hash of the OS actor process image.
  hidden: true
  isArray: true
  name: os_actor_process_image_sha256
  supportedModules: []
- deprecated: true
  description: File Macro SHA256 hash of the action file macro.
  hidden: true
  isArray: true
  name: action_file_macro_sha256
  supportedModules: []
- description: The progress status.
  isArray: true
  name: status
  predefined:
  - New
  - In Progress
  - Resolved
  supportedModules: []
- description: Not progress status.
  isArray: true
  name: not_status
  predefined:
  - New
  - In Progress
  - Resolved
  supportedModules: []
- description: The assets ids related to the issue.
  isArray: true
  name: asset_ids
  supportedModules: []
- description: |
    The assignee of the issue. Accepts a comma-separated list.
    Use "unassigned" for unassigned issues or "assigned" for all assigned issues.
  isArray: true
  name: assignee
  supportedModules: []
comment: Searches Cortex Issues.
commonfields:
  id: SearchIssuesAgentix
  version: -1
contentitemexportablefields:
  contentitemfields:
    definitionid: ""
    fromServerVersion: 5.0.0
    isoverridable: false
    itemVersion: 1.22.45
    packID: CommonScripts
    packName: Common Scripts
    packPropagationLabels:
    - all
    prevname: ""
    propagationLabels: []
    supportedModules:
    - xsiam
    - agentix
    - cloud
    - cloud_posture
    - cloud_runtime_security
    - edr
    toServerVersion: ""
dependson:
  must:
  - core-get-issues
dockerimage: demisto/python3:3.12.13.10116658
enabled: true
engineinfo: {}
internal: true
mainengineinfo: {}
name: SearchIssuesAgentix
nativeimage:
- "8.8"
- "8.6"
outputs:
- contextPath: Core.Issue.internal_id
  description: The internal ID of the issue.
  type: String
- contextPath: Core.Issue.case_ids
  description: The case IDs associated with the issue.
  type: String
- contextPath: Core.Issue.issue_source
  description: The source of the issue.
  type: String
- contextPath: Core.Issue.endpoint_id
  description: The endpoint ID associated with the issue.
  type: List
- contextPath: Core.Issue.status.progress
  description: The progress status of the issue.
  type: String
- contextPath: Core.Issue.assigned_to_pretty
  description: The pretty name of the user assigned to the issue.
  type: String
- contextPath: Core.Issue.assigned_to
  description: The user assigned to the issue.
  type: String
- contextPath: Core.Issue.asset_ids
  description: The asset IDs related to the issue.
  type: String
- contextPath: Core.Issue.action_file_macro_sha256
  description: File Macro SHA256 hash of the action file macro.
  type: String
- contextPath: Core.Issue.os_actor_process_image_sha256
  description: OS Parent SHA256 hash of the OS actor process image.
  type: String
- contextPath: Core.Issue.issue_domain
  description: The domain of the issue.
  type: String
- contextPath: Core.Issue.action_process_image_sha256
  description: Action process image SHA256 hash.
  type: String
- contextPath: Core.Issue.causality_actor_process_image_sha256
  description: Causality actor process image SHA256 hash.
  type: String
- contextPath: Core.Issue.actor_process_image_sha256
  description: Actor process image SHA256 hash.
  type: String
- contextPath: Core.Issue.source_insert_ts
  description: The detection timestamp.
  type: Number
- contextPath: Core.Issue.issue_name
  description: The name of the issue.
  type: String
- contextPath: Core.Issue.severity
  description: The severity of the issue.
  type: String
- contextPath: Core.Issue.issue_category
  description: The category of the issue.
  type: String
- contextPath: Core.Issue.issue_name
  description: The issue name.
  type: String
- contextPath: Core.Issue.issue_description
  description: The issue description.
  type: String
- contextPath: Core.Issue.Identity_type
  description: The identity type of the account.
  type: String
- contextPath: Core.IssueMetadata.returned_count
  description: The actual number of issues that match all filter criteria and returned
    in this specific response.
  type: Number
- contextPath: Core.IssueMetadata.filtered_count
  description: The total number of issues in the system that match all filter criteria.
  type: Number
pswd: ""
restrictioncenter: {}
runas: DBotWeakRole
runonce: false
script: |
  register_module_line('SearchIssuesAgentix', 'start', __line__())
  CONSTANT_PACK_VERSION = '1.22.45'
  demisto.debug('pack id = CommonScripts, pack version = 1.22.45')
  import json



  EQ = "EQ"
  OUTPUT_KEYS = [
      "internal_id",
      "severity",
      "Identity_type",
      "issue_name",
      "issue_source",
      "case_ids",
      "agent_id",
      "actor_process_image_sha256",
      "causality_actor_process_image_sha256",
      "action_process_image_sha256",
      "issue_category",
      "issue_domain",
      "issue_description",
      "os_actor_process_image_sha256",
      "action_file_macro_sha256",
      "status.progress",
      "asset_ids",
      "assigned_to_pretty",
      "assigned_to",
      "source_insert_ts",
  ]

  SEARCH_SHA256_FIELDS = [
      "actor_process_image_sha256",
      "causality_actor_process_image_sha256",
      "action_process_image_sha256",
      "os_actor_process_image_sha256",
      "action_file_macro_sha256",
  ]


  NUMERIC_ARGS = {"page", "page_size"}
  NUMERIC_LIST_ARGS = {"issue_id"}


  def remove_empty_string_values(args: dict) -> dict:
      """Remove empty/invalid values from the args dictionary.
      - Removes keys with empty string values.
      - For numeric args, removes non-numeric values to prevent be3 crashes (e.g. 'n/a', 'invalid_offset').
      """

      def is_valid(key, value):
          if value == "":
              return False
          if key in NUMERIC_ARGS:
              return str(value).strip().isdigit()
          if key in NUMERIC_LIST_ARGS:
              parts = argToList(value)
              return bool(parts) and all(str(part).strip().isdigit() for part in parts)
          return True

      return {key: value for key, value in args.items() if is_valid(key, value)}


  def create_sha_search_field_query(sha_search_field: str, search_type: str, sha_list: list[str]) -> Optional[dict]:
      """
      Given a list of sha256 values, builds a query of this form: { "AND": [ { {"OR": [{"SEARCH_FIELD": sha_search_field,
      "SEARCH_TYPE": search_type ,"SEARCH_VALUE": sha_list[0]} , .... , {"SEARCH_FIELD": sha_search_field,"SEARCH_TYPE":
      search_type ,"SEARCH_VALUE": sha_list[-1]} ]} } ] }

      """
      if not sha_list:
          return None
      or_operator_list = []
      for sha in sha_list:
          or_operator_list.append({"SEARCH_FIELD": sha_search_field, "SEARCH_TYPE": search_type, "SEARCH_VALUE": sha})
      return {"AND": [{"OR": or_operator_list}]}


  def prepare_sha256_custom_field(args: dict) -> Optional[str]:
      """
      Builds a structured query from a list of SHA256 values and assigns it to the 'custom_filter' field in the given args.

      The function:
      - Extracts the 'sha256' argument (as a string or list).
      - For each predefined SHA256 search field, constructs a query block:
          - Uses 'EQ'
          - Each SHA is mapped to an OR clause per field.
      - Combines all field-specific queries under a top-level OR.
      - Adds the final query as a JSON string to args['custom_filter'].

      Example structure added to args["custom_filter"]:
      {
          "OR": [
              {
                  "AND": [
                      {
                          "OR": [
                              {
                                  "SEARCH_FIELD": "actor_process_image_sha256",
                                  "SEARCH_TYPE": "EQ",
                                  "SEARCH_VALUE": "abc"
                              }
                          ]
                      }
                  ]
              },
              {
                  "AND": [
                      {
                          "OR": [
                              {
                                  "SEARCH_FIELD": "causality_actor_process_image_sha256",
                                  "SEARCH_TYPE": "EQ",
                                  "SEARCH_VALUE": "xyz"
                              }
                          ]
                      }
                  ]
              }
          ]
      }
      """
      sha256 = argToList(args.pop("sha256", ""))
      if not sha256:
          return None
      or_operator_list: list[dict] = []
      for sha_search_field in SEARCH_SHA256_FIELDS:
          sha_search_field_query = create_sha_search_field_query(sha_search_field, EQ, sha256)
          if sha_search_field_query:
              or_operator_list.append(sha_search_field_query)
      return json.dumps({"OR": or_operator_list})


  def main():  # pragma: no cover
      try:
          args: dict = demisto.args()
          # Return only specific fields to the context.
          args["output_keys"] = ",".join(OUTPUT_KEYS)

          sha256_custom_field = prepare_sha256_custom_field(args)
          if sha256_custom_field:
              args["custom_filter"] = sha256_custom_field

          if issue_domain := args.get("issue_domain"):
              args["issue_domain"] = f"DOMAIN_{issue_domain.upper().replace(' ', '_')}"

          args = remove_empty_string_values(args)

          demisto.debug(f"Calling core-get-issues with arguments: {args}")
          results = demisto.executeCommand("core-get-issues", args)[0]  # type: ignore

          if is_error(results):
              error = get_error(results)
              demisto.debug("error: " + error)
              raise DemistoException(f"Failed to execute the core-get-issues command {error}")

          context = results.get("EntryContext", {}).get("Core.Issue(val.internal_id && val.internal_id == obj.internal_id)")
          human_readable: str = results.get("HumanReadable", "")

          for item in context or []:
              if "agent_id" in item:
                  item["endpoint_id"] = item.pop("agent_id")

          return_results(
              CommandResults(
                  outputs=context,
                  outputs_prefix="Core.Issue",
                  outputs_key_field="internal_id",
                  readable_output=human_readable,
              )
          )

      except DemistoException as error:
          return_error(f"Failed to execute SearchIssues. Error:\n{error}", error)


  if __name__ in ("__main__", "__builtin__", "builtins"):  # pragma: no cover
      main()

  register_module_line('SearchIssuesAgentix', 'end', __line__())
scripttarget: 0
signature: 
subtype: python3
system: true
tags:
- Utility
type: python
